# Commit 4 · `planner.ts` build extraction (working aid, not an authority)

> # ⛔ THE STAFF-EDGE DUTY IS STRUCK (D20)
> **Owner ruling D20 (Ali, 2026-09-17), just below D19: every report, statutory filing, admin count, finance or insights
> figure and harm/AML detector treats a house bot's account exactly like any player's account, and every admin-only
> house tool is dropped.** Struck here, marked in place: the staff-edge duty in §1's pass order, §1.9's staff-edge
> bullet, §3's missing item 7, §5's `test:house-bot-reports` TGT-39 and §6 open point 8 (C5-SPEC rulings 218–223;
> C4-SPEC ruling 78). Nothing else in this file is touched by D20. Read PROGRESS.md "OWNER RULING D20" and
> `plans/house-bots/C5-D20-REPLAN.md` first.

> # ⛔ THE HOLDER SUMMARY IS STRUCK (D19)
> **Owner ruling D19 (Ali, 2026-09-16): nothing about house bots reaches a player or the holder.** The holder receives no
> house-bot notice or email at all, and every alert goes to admins only (D19c). Struck here, marked in place: §1.9's
> holder hourly summary, its cap and its emitter (C4-SPEC rulings 149, 153). The admins' summary and every other duty
> stand. Read PROGRESS.md "OWNER RULING D19".

> Extracted read-only on 2026-09-15 (fourth session, OMEGA-COMPILE01) at `d69ac076`, for §5 step 5. The documents cited win; re-derive every `file:line`. Prefixes: `04:` amendments, `02:` sealed flows, `P:` PLAN, `01:` scenario register, `C4:` C4-SPEC; `dal` = `src/lib/server/house-bot-dal.ts`; `hb/` = `src/lib/server/house-bot/`. **Since this extraction:** `blackout.ts {countResolveClaim}` and the P3 purge in `retention.ts` are BUILT (`90d5e795`, `409bba07`, `19dcfe40`) — items 1.7 MISSING, 1.15 and §3 MISSING #1/#16 are done. Open points in §6 become C4-SPEC rulings before code.

## 0. Wiring as built
- `hb/engine.ts:222-235` `planOnce` takes `acquireLeadership("house-bot", {leaseMs: 45_000, strictWrite: true})` (constants.ts:48, :547), then `ticks.plannerTick(ctx)`; every 15 s after a 20 s first delay, guarded by `plannerBusy`; `TickContext {state, instanceId}` (engine.ts:68).
- Mirror `worker.ts`: export `plannerTicks(alerts: EngineAlerts): Pick<EngineTicks,"plannerTick">` (ruling 46: alerts required). planner.ts must not take the lease again. Engine cases for the planner are §17.

## 1. Pass order and duties
N1's fixed order (04:2880-2892, every effect conditional with RETURNING): 1 deadline · 2 STALE (before POISON) · 3 POISON · 4 press passes · 5 audit lease repair · 6 A8 alert repair · 7 `endTargets` · 8 once a minute: oversight · 9 hourly: summaries; ~~staff edge on the first EAT day of a month~~. Every other duty below has no slot (open point 1). ⛔ **Superseded by D20 (Ali, 2026-09-17):** step 9 has no staff-edge duty; the staff-edge alert is struck (C5-SPEC rulings 218–223).

1.1 **Deadline** — PENDING past `deadlineAt` → EXPIRED(CUTOFF) (04:2881; P:224): `expirePastDeadline()` (dal:1491; PG :3487-3491; mem :2387-2392), no event/audit/alert. ENG-16 (01:1176) also wants CLAIMED-past-deadline rows (open point 2).
1.2 **STALE** (04:2882-2885; P:803): PENDING `staleAt <= now()`; CLAIMED staff-chosen `staleAt + 5 s < now()` whatever `claimedUntil`; CLAIMED other kinds `claimedUntil < now() AND staleAt <= now()` → EXPIRED(STALE); frees `hbi_manual_live_market_uq` within one pass (04:2828). `expireStale()` (dal:1493; PG :3492-3501) covers all three; `PLANNER_STALE_EXPIRY_GRACE_SEC = 5`.
1.3 **POISON** (04:2886, 04:317): CLAIMED `claimedUntil < now() AND attempts >= 3` → FAILED(POISON) + AlertOnce `poison:<intentId>` (constants.ts:662) via `notifyAdminsHouseBotAlert` (ADMINs, bell + email, P:386), href `?tab=activity&range=all&outcome=failed&intent=<id>` (C13 04:1065). `poison()` returns ids (dal:1495). `house_bot.poison` is allowlisted (constants.ts:767; 04:520) but A19 says "no row per intent" (04:523) — open point 3. Missing: an exported `alertOnce` (outcomes.ts:97-110 is private); the emitter (step 9).
1.4 **Press passes** (04:2912-2916): INTERRUPTED — `pressStore.interruptStale()` (dal:1563; CHECKING older than 120 s → REFUSED 'INTERRUPTED'; no audit, no bell 04:3298). DONE — `pressStore.doneTerminalQueued()` (dal:1561; QUEUED with a terminal intent); run after passes 1–3 (TGT-31 01:3442).
1.5 **Audit lease repair** (04:2917-2920, :2244-2247, :4079): presses >60 s since `updatedAt`, `auditId` null, lease null/expired — ENTER_NOW QUEUED/DONE (`house_bot.enter_now {botId, holderUserId, marketId, intentId, side, stakeTzs, entryCondition, outcome, reason}`), ENTER_NOW REFUSED INFO_BLACKOUT/OWNER_POSITION (`house_bot.enter_now_refused {botId, marketId, code}`), TARGET_ADD/UPDATE/REMOVE and STAFF_CANCEL DONE with an event carrying `payload.pressId` (`target_added {botId, marketId, targetId, delayMinSec, delayMaxSec, timingFrom, reactTo, reason}`, `target_updated {botId, marketId, targetId, changes, reason}`, `target_removed {botId, marketId, targetId, outcome, cancelled[], reason}`, `staff_intent_cancelled {botId, marketId, intentId, side, stakeTzs}` — 04:2802-2803, :4086-4089; all COMPLIANCE, keys in `HOUSE_AUDIT_PAYLOAD_KEYS` constants.ts:771-799). Uses `listAuditRepair` (dal:1568) → `claimAuditLease` (:1565) → audit → `setAuditId` (:1566); rebuilds via `listForPress` (:1437). Missing: a press-audit builder shared with commit 7's actions; the actor (open point 4).
1.6 **A8 alert repair** (04:254-256, :2921-2923): PLACED, `alertedAt` null, `finishedAt` > 30 s → `listAlertRepair` (dal:1504) → `markAlerted` (:1502) → `alerts.placed` (staff-chosen → `notifyAdminsHouseBotStaffChosen`, uncapped; others `notifyAdminsHouseBotBet`; routing inside the step-9 emitter). Test: a kill after commit repaired within 45 s (04:261; ENG-04 01:1084).
1.7 **`endTargets`** (N2 §4 step 9, 04:3997-4006): per ACTIVE target (`targetStore.listActive()`, bounded by `gTargetsMaxActive` ≤ 200), first matching cause via conditional `endActive(targetId, cause)` (dal:1525) + TARGET_ENDED event `{targetId, endCause}` (N2 §2 04:3889; no audit A19; no alert 04:4175): 1 `reactTo=FIRST` and a PLACED reaction → DONE (`countPlacedForTarget`); 2 market missing → MARKET_GONE; 3 not LIVE → MARKET_CLOSED; 4 `reopenedAt` → MARKET_REOPENED; 5 `infoBlackout(id, {countResolveClaim:false})` blocked → INFO_BLACKOUT (a young claim alone never ends a target); 6 `scopeCode(view)` non-null, F1 denied, or `rulesCover(rules, view, null)` false → OUT_OF_SCOPE; 7 DB `now() ≥ lastReactableStakeAt + SWEEP_LOOKBACK_MS` (from `effectiveTargetTiming(target, view.exitRates, guardsFor(rules,"MARKET"), cutoffOf(view), nowIso, target.effectiveFrom)`, rules.ts:3237) → CUTOFF_PASSED. `targeting.enabled=false`, unparseable rules and a non-ACTIVE bot leave targets ACTIVE and inert (04:4005). planner.ts is a pinned importer of blackout.ts (04:2599).
1.8 **Oversight, once a minute** (`oversight.ts`; only planner imports it; exempt by name from info-edge, 04:2609-2612):
- Staff stake voided (04:2924-2927, :3279): markets with a PLACED staff-chosen stake finished in the last 30 days (`hbi_staff_finished_idx`, `OVERSIGHT_LOOKBACK_DAYS`), market VOIDED or `reopenedAt` later than the stake → AlertOnce `staff-stake-voided:<marketId>` to every `houseBotAlertRecipients()`, bell + email; copy "Market “{title}” holding a staff-chosen house stake ({SIDE} TZS {x}, Bot “{label}”) was {voided | reopened} at {HH:MM} EAT." → `/admin/markets/<marketId>`.
- Staff stake self-decided (04:2928-2937, :3280, :3284-3289; ruling 29): AuditLog 30 days via `@@index([targetType, targetId])`, actions `market.adjudicated`, `market.resolve.bulk` (targetType Batch, markets in payload), `market.resolve.bulk_override`, `market.emergency_void`, `market.reopened`, `objection.upheld`, `objection.rejected`; `requestedBy` recomputed from intents (MANUAL `requestedById`) and targets (`createdById`) placed before the audit row; actor ∈ requestedBy → AlertOnce `staff-stake-self-decided:<marketId>:<resolved|voided|reopened|objection_upheld|objection_rejected>`; copy "Market “{title}” holding a house stake chosen by {name} was … by {name} at {HH:MM} EAT. This is a record only; nothing was refused." Never a refusal.
- Missing: (a) an intent read of PLACED staff-chosen rows by `finishedAt` window (id, marketId, houseBotId, side, stakeTzs, finishedAt, requestedById, targetId) — `staffChosenPlaced` returns sums, `listFeed` filters `createdAt`; (b) an audit reader by `(targetType, targetIds, actions, since)` (`getAuditByActionsDurable` audit.ts:626 lacks both); (c) a void time for the copy (open point 9). Cadence mechanism (open point 7).
1.9 **Hourly** — summaries (C13 04:1062; P:382, :389; N1 §7 04:3277; ENG-25): admins `notifyAdminsHouseBotHourSummary` (bell; cap `bellAlertsPerHour`, live throttle runtime `global.countInHour`; beyond-the-cap count excludes staff-chosen rows, adds "{s} staff-chosen stakes were alerted one by one"; key `summary:admins:all:<EAT hour>`; href P:382 vs C7 04:1929 — open point 11)~~; holder `notifyHouseBotOwnerHourSummary` (bell only, ruling 17; cap `holderNoticesPerHour`, 0 = summary only; staff-chosen included; early return while `isLockedOut`; "50pick placed N liquidity stakes (TZS X) from your account between HH:00 and HH:00" → `/positions`; key `summary:holder:<botId>:<EAT hour>`)~~. Missing: (a) PLACED counts/TZS per bot for an EAT-hour window by `finishedAt`, split staff-chosen/automatic (twins); (b) a previous-hour key unit (`EAT_KEY_UNITS` clock.ts:61 lacks it; open point 10); (c) ~~both emitters~~. ⛔ **Superseded by D19 (Ali, 2026-09-16):** there is no holder summary: `notifyHouseBotOwnerHourSummary` and `holderNoticesPerHour` are deleted (D19c; C4-SPEC rulings 149, 153), so only the admins' summary emitter is built.
- ~~Staff edge (04:2938-2945, :3290-3295, :3281; ruling 29): first EAT day of a month, hourly, for the month just ended; per officer settled = won+lost+refunded, win rate = won/(won+lost), net = payouts+refunds−stakes "from `book.ts`, the same function as R1's scorecard"; baseline automated win rate on polls (`kind<>'MANUAL' AND "targetId" IS NULL`); AlertOnce `staff-edge:<officerId>:<YYYY-MM>` (`previousMonth` unit) when settled ≥ 10 and (`gStaffEdgeWinRatePts` set, baseline has won/lost, gap ≥ pts) or (`gStaffEdgeNetTzs` set, net ≥ it); both NULL → nothing; href `/admin/reports?tab=library&range=custom&from=<YYYY-MM-01>&to=<YYYY-MM-last>`. Missing: a per-officer settled book (commit 5 owns the book split; open point 8).~~ ⛔ **Superseded by D20 (Ali, 2026-09-17):** the staff-edge alert, R1's flag and TGT-39 are struck (C5-SPEC rulings 218–223; C4-SPEC ruling 78), so nothing in this bullet is built.
1.10 **Realised-loss stops** (P:207-210, :116, :154, :224, :275; ENG-14 01:1163; ENG-26 01:1259; A24 04:623): realised = settled stakes − returned, per EAT-day cohort by placement day. Per bot: `houseDayBooks(eatDayKey(dbNowMs))` (book.ts:101, one GROUP BY); ACTIVE bot with `capDailyLossTzs != null && realisedLossTzs >= cap` → `stopBot(botId, {to:"AUTO_PAUSED", cause:"DAILY_LOSS_STOP"})` (same predicate as Start, eligibility.ts:367-372; non-void, targets stay ACTIVE). Global: `houseDayBook(day, null).realisedLossTzs >= gCapDailyLossTzs` and enabled → `switchOff({cause:"GLOBAL_LOSS_STOP"})` (conditional) → `cancelLive({all:true},"MASTER_OFF")` → SWITCH_OFF event `{cause}` → audit → switch alert (A19 order; 02:374-380). Missing: a generalised `engineSwitchOff` (today ENGINE_FAULT/ENGINE_ERRORS + `alerts.security` only, outcomes.ts:180-194); `EngineAlerts` channels for switch/summaries/oversight; audit naming (open point 12). EVENT_KINDS stays 30. Tests: two planners → one event, one alert (04:629); CA-11 (01:1472); P:515.
1.11 **F5 `revalidateLive()`** (04:1701-1703; runs every pass even with master OFF; keyed by runtime `boundsHash` of global min/max, chain bounds, the `bet.place` rule): can no longer place any bet (stakeMin > live max, stakeMax < live min, OPENER range outside live bounds, `gCapPerMarketTzs` < live min, min gap below the live floor) → AUTO_PAUSED(RULES_INVALID{field}), 1 alert per bot per hash; bounds that only narrow → no pause, one AlertOnce per hash ("Platform maximum stake is now TZS 5,000; Bot A's max TZS 10,000 is clamped. Review Rules."). N1 §5 (04:3075-3082): Enter now stake or `capStaffChosenDailyTzs` below the live minimum → not paused, one AlertOnce per bot/field/hash; names targets when `targeting.enabled`. A7 (04:238): stakeMin below the platform minimum → AUTO_PAUSED(RULES_INVALID{stakeMinTzs}). Keys `bounds-invalid:<botId>:<hash>`, `bounds-clamp:<botId>:<hash>`, `bounds-cant-fit:<botId>:<field>:<hash>` (constants.ts:686-689). Uses `rulesStartProblems` (rules.ts:2479, :2501-2510), `minGapFloorSec` (:131), runtime `boundsHash` (dal:178), `stopBot(…, {field})`. Missing: live stake-bounds/chain-bounds/`bet.place` loaders and the hash. Tests 04:1706-1709.
1.12 **Rules parse outcomes** (04:185, :1098-1099, :1683-1690): OUTDATED → AUTO_PAUSED(RULES_OUTDATED) ("Bot A is paused: its rules were saved in an older format. Open Rules, review the converted values, Save, then Start."); INVALID → AUTO_PAUSED(RULES_INVALID, field); FROM_FUTURE → skip, no pause; lasting > 10 min (runtime `rulesFutureSince` dal:180, `RULES_FUTURE_ALERT_AFTER_MS`) or `limitsSchemaVersion` ahead → AlertOnce `rules-future:<botId|global>:<v>` ("Bot A is idle: its rules were saved by a newer 50pick build (v{n}). Nothing will be placed until that build is back."). Never converts. Cache `loadParseContext` per pass. Tests: v0 → OUTDATED and JSON byte-identical after 3 passes; v99 for 11 min on an injected clock → 1 alert, status unchanged, 0 fires; TGT-27(b).
1.13 **C8 cooling-off end bell** (04:836; ruling 16): one admin bell AlertOnce `bot:<id>:RG_ENDED:<coolingOffUntil>` ("Bot A's holder break ended at {HH:MM} EAT. Confirm his permission before starting →", `?reverify=1`), no holder notice (`ALERT_KEY.rgEnded`, constants.ts:676). Ruling 18 puts L2 duties in the lifecycle ticker (open point 15).
1.14 **C9 erasure-request sweep** (04:864, :881): ACTIVE bot → `voidHouseConsent({cause:"HOLDER_ERASURE_REQUEST"})` — but ruling 18 / R6 (04:1256) place it in the L2 lifecycle sweep (step 7), not planner.ts. `listDsarRequests` is an in-memory queue (privacy.ts:214-218; open point 16).
1.15 **AlertOnce purge (P3)** — ✅ built in `retention.ts` (not the planner).
1.16 **A16 WALLET_MISSING** (04:463; HB-LC-10 01:822-825): holder wallet missing with OPEN house positions (REMOVED bots too) → AUTO_PAUSED(WALLET_MISSING) for ACTIVE bots; danger AlertOnce `settle-blocked:<botId>:<day>` ("Settlement is blocked on 6 markets for every player: Bot A's wallet is missing. Restore the wallet, then settlement resumes", market links, `/admin/settlement`), persisting until OPEN house positions settle. Uses `houseBookStore.openExposure(null)` (dal:1577), `db.wallet.findByUserId`, `stopBot`. Missing: open marked market ids per bot (twins).
1.17 **Duties C4 §3.8 missed:** (1) A16 PENDING sweeps → SKIPPED(MARKET_NOT_LIVE / CHAIN_NOT_RUNNING / MARKET_GONE) (04:456, :459, :464; HB-LC-08/-16) — set-based DAL members missing; (2) exit rules widened: runtime `exitConfigHash` (dal:179) → AlertOnce `exit-config:<hash>` when an ACTIVE bot's counter becomes impossible (04:462; 01:2527-2532); (3) `chain-in-scope:<botId>:<chainId>` (04:461; maybe superseded by C14, open point 20); (4) A12/F1 once-only alerts `ud-orphan-market:<marketId>`, `ud-born-unlocked:<roundId>`, `product-denied:<value>:<EAT day>` (04:381, :1624; ruling 40); (5) 02 X11: CANCELLED_BY_ADMIN is final — check `findByAnchor(kind, marketId)` before inserting (the unique index excludes CANCELLED); (6) `pruneInstanceRows()` has no caller (HOUSE-BOTS.md:64 "lands in build commit 4"; open point 18).

## 2. FILL and OPENER planning
- Rules: FILL at `cutoff − lead − jitter` stakes the thin side up to the target share (P:90); OPENER at `bettableFrom + delay` while both pools are 0, on the side drawn once (P:91, :804); A11 scope (04:340-341); A15 `dueAt ≤ deadlineAt`, FILL sized on `lockedForHouse` (04:438, :2559-2562); no plan on null cutoff, demo, UD without round, reopened ("never re-planned" 04:457), stopped chain; idle while OFF (P:500). Pure `planFill`/`planOpener` built (decide.ts:429-521; ruling 39).
- **2.1 Market scan: NOT DEFINED anywhere** (gap). `marketStore.listBoard` returns `StoredMarket` (A13 forbids) ordered by `resolutionAt`; `listMarkets` drops demo and is full-row; `marketView` reads one id. Proposal: `houseSeamStore.plannableMarkets({productLine, cutoffFromIso, cutoffToIso, emptyPoolsOnly, after?: {cutoff, id}, limit})` → `{id, cutoff}[]` in both twins: LIVE, raw product line, `reopenedAt IS NULL`, not `Demo · `, polls `selectionClosedAt IS NOT NULL` (cutoff = it), UD joined to round/chain/asset with `chain.state='RUNNING' AND asset.enabled` (cutoff = `least(opensAt + D, coalesce(selectionClosedAt, resolutionAt))`), OPENER pools 0, `NOT EXISTS` a non-CANCELLED FILL/OPENER intent with that anchorKey; keyset `(cutoff, id)`, page 200; FILL window `(dbNow, dbNow + max(lead + jitter) + PLANNER_INTERVAL_MS]`; OPENER `cutoff > dbNow` and bettableFrom ≥ scope start. EXPLAIN pin at 20k markets (A24 04:633; rulings 25–26).
- **2.2 Bots and `DecideBot`:** ACTIVE non-removed bots with parseable rules (FROM_FUTURE skipped); `botCovers(bot, view, mode)`; fields from `StoredHouseBot`, `openExposure` from one grouped `houseBookStore.openExposure(null)`, `lastPlacedAt` from `botUsage`, `scopeFrom` from runtime `bot:<id>`, `marketHeld` (open point 25), `capPrecheck` (open point 26). `PlanInput`: `globalScopeFrom` runtime `global` (null semantics open point 24), `pools` via `lockedForHouse(view.id, lockedPoolInputsOfView(view))`, `price` via `udPriceForDecision(chainKey asset id)`, `bounds` via `stakeBoundsForMarket`, `passNow` DB clock once, `randomInt` = `crypto.randomInt`. Bot choice `orderBots`, first bot returning a row inserts (open point 28).
- **2.3 Opener draw** (04:2582; P:804): `openerSide(marketId, {houseBotId, actorId:null, drawnFor:"OPENER_PLAN"})` outside any lock; once per market; blackout-before-draw is stated for Enter now (04:2619) but ruling 41 says automated OPENER ignores the blackout (open point 27). Tests 04:3701-3706; RED N1-2 (04:3797).
- **2.4 Insert:** anchorKey = marketId (P:162); `hbi_fill_opener_anchor_uq ("kind","anchorKey") WHERE kind IN ('FILL','OPENER') AND status<>'CANCELLED'` (04:2272) — one FILL and one OPENER per market across bots; a cancelled one can be re-planned, skipped/expired not (P:165, :517); CANCELLED_BY_ADMIN final. `insertIgnoringConflict(row)` (dal:1451) → null = no-op; it swallows every unique violation vs 04:2343 (open point 29). Early FILL planning can never be re-planned after a SKIP (open point 23).

## 3. DAL and module map
**Existing:** lease/beat (`acquireLeadership`; `beat(RUNTIME_KEY.plannerBeat)`); expiry/STALE/POISON/`get`; press `interruptStale`/`doneTerminalQueued`/`listAuditRepair`/`claimAuditLease`/`setAuditId`/`listForPress`; A8 `listAlertRepair`/`markAlerted`; `endTargets` reads (above); loss stops (`houseDayBooks`, `stopBot`, `switchOff`, `cancelLive`, `readControl`); F5/rules (`rulesStartProblems`, `minGapFloorSec`, runtime get/upsert); C8 `readBotAndHolder`; WALLET_MISSING (`openExposure`, `db.wallet`); FILL/OPENER (`listNonRemoved`, runtime, `botUsage`, `marketUsage`, `listLiveOnMarket`, `activeForMarket`, `marketHeld`, `lockedForHouse`, `udPriceForDecision`, `stakeBoundsForMarket`, `openerSide`, `infoBlackout`, `orderBots`/`planFill`/`planOpener`, `findByAnchor`, `insertIgnoringConflict`); alerts (`claim`, `claimWithEatSuffix`, `release`, `houseBotAlertRecipients`, `playerHandle`).
**Missing** (twins for DAL members): 2 `plannableMarkets` (+ optional `marketViews(ids)`); 3 a previous-hour EAT key unit; 4 PLACED counts/TZS per bot per EAT hour split staff-chosen/automatic; 5 staff-chosen PLACED rows since a time; 6 an audit reader by target/actions/since; ~~7 a per-officer settled book + baseline;~~ ⛔ **Superseded by D20 (Ali, 2026-09-17):** struck with the staff-edge duty and R1's scorecard (C5-SPEC rulings 199–213, 218–223); 8 set-based PENDING A16 sweeps; 9 open marked market ids per bot; 10 live bounds / `bet.place` / exit-config loaders and hashes; 11 a generalised switch-off for GLOBAL_LOSS_STOP; 12 `EngineAlerts` channels + emitters (step 9); 13 an exported `alertOnce`; 14 a shared press-audit builder; 15 `oversight.ts`; 17 cadence state for once-a-minute/hourly (F7).

## 4. Leadership, beats, master OFF
- Lease taken in engine.ts; planner.ts never re-takes it; conditional effects make a failover double-leader give one event and one alert (04:617, :629). Beat `beat:planner` (ruling 10); planner-stale copy "Loss stops, fills and summaries are delayed (last run {t} EAT). Bets still stop at every cap." (C11 04:974; `ENGINE_STALE_MS`); when to beat is open point 30. `HOUSE_BOT_ENGINE=false` → no planner (04:613, :1256).
- With master OFF / no ACTIVE bot: expiry, press, audit, A8, oversight, summaries, WALLET_MISSING, A16 sweeps → run (proposal); F5 `revalidateLive` → runs (explicit, 04:1701); `endTargets` (open point 5); rules-parse pauses (open point 32); global loss stop conditional no-op when OFF, bot DAILY_LOSS_STOP (open point 13); FILL/OPENER → do not run (P:500); C8/C9/A2/A5 → lifecycle ticker (ruling 18).

## 5. Tests demanded
- `test:house-bot-engine`: 04:627 (300 stale + 20 fresh → 0 stale placed); ENG-09, ENG-16 poison (01:1180); MON-10 (04:3713); MON-11 audit lease (04:3720); 04:3699 refused-press audits; TGT-15(e)/-18/-31; A8 45 s (04:261); two planners loss stop (04:629; ENG-14); CA-11; ENG-29; `endTargets` N2 04:4370-4375 (each cause within one pass; young claim alone keeps ACTIVE; reopen → MARKET_REOPENED; decided before CUTOFF_PASSED still fires; pause keeps ACTIVE; remove → BOT_REMOVED; sunset → SUNSET), INT-09, TGT-14/-23/-24(a)/-34; oversight 04:3723 (TGT-38); FILL/OPENER 04:3701-3706, 04:348, HB-LC-06/-07/-04/-22/-12/-15, P:517; A16 04:467-473, HB-LC-08/-16; F4 04:1688/1690; F5 04:1706-1709; A4 04:193; TGT-27; ENG-26.
- `test:house-bot-comms`: C13 straddling 14:00 (04:1076; ENG-25); F6 (04:1725); N1 alerts (04:3738-3739). `test:house-bot-caps`: MON-03 (04:3679); P:515; HB-LC-02. `test:house-bot-designation`: C8 bell (04:841), C9 (04:881). `test:retention`: P3 ✅. ~~`test:house-bot-reports` (commit 5): TGT-39.~~ ⛔ **Superseded by D20 (Ali, 2026-09-17):** TGT-39 (the staff-edge scorecard alert) is struck (C5-SPEC rulings 218–223). `test:house-bot-money`: HB-LC-10.
- `red:house-bot-engine`: N1-2 (04:3797), N1-7 (04:3802), N1-8 (04:3803), N2-E4 (04:4406).

## 6. Open points needing a build ruling
1. Placement of extra duties in the pass order — recommended: steps 1–7, then A16 sweeps → rules-parse pauses → F5/A7 → loss stops → WALLET_MISSING → FILL/OPENER (master ON only), then 8–9.
2. Deadline pass: PENDING only (code) vs also expired CLAIMED rows (ENG-16/-14).
3. POISON audit: one `house_bot.poison` row per intent vs alert only (A19 "no row per intent").
4. Press audit repair actor (`press.actorId` vs `system_house_bot`) and location (shared module vs planner.ts).
5. `endTargets` with master OFF; with unparseable rules (causes 1–5 only vs skip); `lastReactableStakeAt = null`.
6. TARGET_ENDED payload key: N2 `{targetId, endCause}` vs `voidHouseConsent`'s `{targetId, cause}` (designation.ts:560).
7. Once-a-minute/hourly cadence without module state: globalThis timestamps vs runtime row vs every pass + AlertOnce.
8. ~~Staff edge in commit 4 (minimal settled read now) vs skeleton now and live in commit 5.~~ ⛔ **Superseded by D20 (Ali, 2026-09-17):** moot — C4-SPEC ruling 78 moved it to Commit 5, and D20 strikes the staff-edge alert everywhere (C5-SPEC rulings 218–223).
9. Void time for "voided at {HH:MM}": the emergency-void audit row vs detection time.
10. Summary AlertOnce suffix: add a `previousHour` unit (like `previousMonth`) vs an explicit hour (forbidden by constants.ts:626-631).
11. Summary href: P:382 `range=today` vs C7 04:1929 custom hour range (later/specific wins).
12. Loss-stop records: `house_bot.loss_stop` vs `house_bot.auto_paused`/`switch_off{cause}`; global stop alert = switch bell + email vs `alerts.security`.
13. Bot DAILY_LOSS_STOP while master OFF: pause anyway vs only while ON.
14. F5: validate every pass (hash only in keys) vs only on hash change; where `boundsHash` lives (`global` vs `bot:<id>`); stakeMin < live min as pause-worthy; paused bots alert-only or nothing.
15. C8 bell and C9 erasure: planner (04:836, :864; P:575) vs L2 lifecycle sweep (ruling 18; R6) — proposal L2.
16. DSAR erasure detection reads an in-memory queue (privacy.ts:214-218) — confirm hydration on every container.
17. P3 purge location — resolved: `retention.ts` (one batch per daily run; A20 "batches of 5,000" — decide whether to loop).
18. Who calls `pruneInstanceRows()` (planner hourly vs lifecycle retention).
19. A16 PENDING sweeps are planner duties, PENDING-only; PENDING FILL/OPENER on a reopened market?
20. `chain-in-scope` (likely superseded by C14) and `exit-config` (planner duty, no reader).
21. A12/F1 once-only alerts from the scan; `ud-born-unlocked` has no detection rule.
22. The market scan (§2.1) — filters, windows, page size, ordering.
23. FILL horizon: plan only when `cutoff − lead − jitterMax ≤ passNow + PLANNER_INTERVAL_MS` vs at first sight.
24. NULL `scopeFrom`: out of scope (DAL:173-175, migration) vs `passNow` (decide.ts:439, :493). Same as trigger open point 1 — one ruling.
25. `marketHeld` for automated kinds: reuse enter-now's (rulings 58–59) vs another bot + own live intent only.
26. `capPrecheck` definition — proposal: first failing money cap in H2→H3→H4 order at `max(stakeMinTzs, bounds.min)`, NULL = refuse; staff-chosen and rate caps excluded. Same as trigger open point 10.
27. Blackout before the OPENER draw (Enter now rule) vs none for automated OPENER (ruling 41); which bot the draw names.
28. FILL/OPENER bot choice: first eligible per market in `orderBots` order vs per-bot pass.
29. `insertIgnoringConflict` swallows every unique violation vs rethrow unknown (04:2343). Same as trigger open point 12.
30. When the planner beats: start / after passes 1–6 / end.
31. Fix `HouseViewRound.chainKey`'s type comment ("asset symbol" → asset id), dal:1631.
32. RULES_FROM_FUTURE while OFF: send the 10-minute alert? Does `limitsSchemaVersion` share `global.rulesFutureSince`?
