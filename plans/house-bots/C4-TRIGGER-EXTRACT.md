# Commit 4 · `trigger.ts` build extraction (working aid, not an authority)

> Extracted read-only on 2026-09-15 (fourth session, OMEGA-COMPILE01) at `d69ac076`, for §5 step 6. The documents it cites win; re-derive every `file:line` before use. Open points in §10 become C4-SPEC rulings before any code is written.

## 1. The post-commit hook

### 1.1 Where it goes in `src/lib/server/market-service.ts` (lines at `d69ac076`)
- `BetContext` 902; `buyPosition` → `withAdmission(() => withTransientRetry(() => buyPositionInner(…, {kind:"player"})))` 948, 954; `placeHouseBet` 981; `buyPositionInner` 1004.
- `type CommittedBet` 1239 (positionId, placedAt, yesPool, noPool, payoutIfWin, bonusPart, walletId, bonusAllocations, usedTx); `let committed` 1240.
- Outer `withLock(wallet:)` 1243–1655; `try/catch BetAbort` 1656–1669; `// ── COMMITTED ──` 1671; **guard `if (result.ok && committed)` 1675**.
- In the committed block: `recordSnapshot` 1677 · `emit("market:odds")` 1678 · `void emitWalletBalances([userId])` 1685 · `bonus.spent` audit 1689–1691 · `readIdentityStanding` 1698 · `audit(market.position.opened)` 1699–1711 (`// SEAM:audit` 1708) · `// SEAM:receipts` 1720 · `if (ctx.kind === "house") {}` 1721 · `else if (!perEventNotificationsSuppressed(market))` 1723 (`notifyBetPlaced` 1724, email 1740) · **`else {` 1751–1779, the player Up & Down arm** (`pushOnly` 1764) · block closes 1780.
- `wageringFulfilled` 1783–1791; `if (result.ok)` → `// SEAM:recruit` 1809 → `onRecruitBet` 1810 (guarded by `result.ok` only, so it runs on replays too — do not copy that guard); `return result` 1815.
- `perEventNotificationsSuppressed(m)` = `m.productLine === "UPDOWN"` (823–825), so the 1751 arm is exactly "not a house bet, folded product line UPDOWN". The folded line is trustworthy for UPDOWN (`market-dal` folds everything else to MARKET, 04:359); raw non-house lines land in MARKET, which the hook skips and the sweep's raw check handles.
- Facts in memory at the call site: `userId`, `opts.marketId/side/stake`, `c.positionId`, `c.placedAt` (container JS time stamped before the market lock, A11 04:331), `market` (a `StoredMarket`, which the engine may not hold, A13 04:403). ENG-19's suggested signature (01:1203): `{positionId, userId, role, marketId, productLine, side, stake, placedAt, cutoff, houseBotId}` — pass only what exists without a new read.

### 1.2 Anchor files quoting nearby lines (never split or duplicate)
- `scripts/anchors/kyc-gate.anchors.mjs:141` (→142–144): the 4-line block `const standing = await readIdentityStanding(userId);` … `action: "market.position.opened",` (ms 1698–1701). `kyc-gate.anchors.mjs` ~127: `payoutIfWin: c.payoutIfWin, kycStatus: standing.kycStatus, everApproved: standing.everApproved,` (1706).
- `layout-staleness.anchors.mjs:92`: `void emitWalletBalances([userId]);\n    return { ok: true as const, data: { value: paid, …` — the cash-out function, not 1685; do not write that two-line shape.
- `house-bot-money.anchors.mjs:168` `            replayed: true as const,` and `house-bot-seam.anchors.mjs:43,52` (`// SEAM:H2`, `if (await isMaintenanceMode()) {`) — outside the block; must stay unique.
- Nothing else quotes 1712–1816. Safe slots: inside the UD `else` arm after `pushOnly(...)` (before 1779), or a separate statement after 1685/1691 or just before 1780. **Never between 1698 and 1701.**

### 1.3 Gates, in order
1. Call site, synchronous: under `committed` (1675), player only (C4-SPEC §2; ruling 12). `HOUSE_BOT_ENGINE=false` → return **before** the dynamic import (A24 04:613; R6 04:1256; engine.ts:13; ENG-22 01:1227-1228). `houseBotEngineEnabled` lives in `engine.ts` (imports DAL, leader, prisma), so the check is inline: `process.env[HOUSE_BOT_ENGINE_ENV] !== "false"` (`market-service.ts:45` already imports from `@/lib/house-bot/constants`).
2. Up & Down only: a poll position returns before any read (N2 §4 step 1 04:3940-3944; PLAN §18 P:813; ruling 12). TGT-19 wants a source pin that the hook returns for `productLine='MARKET'` (01:3214); red N2-E5 (04:4407).
3. Leave the lock context (ruling 31): `runOutsideLock(fn)` = `lockStore.exit` (locks.ts:62-76); pattern `runOutsideLock(() => { import("./house-bot/trigger").then((m) => m.onPlayerBetCommitted(facts)).catch(() => {}); })`. `void import(` appears nowhere today. ⚠️ The call runs inside `withAdmission` (954), whose AsyncLocalStorage (`admission.ts:126`, `inAdmission()` 132–134) `runOutsideLock` does not exit (open point 3).
4. Inside `trigger.ts`: `hookSuspendedBySkew(engineState())` (engine.ts:127-129; unknown or > 5 s → the sweep decides UD too, 04:3942); `HOOK_SEMAPHORE = 4` (constants:506; overflow dropped and counted, the sweep recovers, 04:616, ENG-05 01:1091(3)); `HOOK_SOFT_CACHE_MS = 5000` (constants:503) of "switch on and any bot ACTIVE" (P:229) — firing always re-reads (fire.ts:111), a stale cache's intents are cancelled at fire. Cache, semaphore and counter live on `globalThis.__50PICK_HOUSE_BOT_ENGINE` (ENG-23 01:1235); `EngineState` (engine.ts:44-66) has no such fields yet; engine case 11.22 forbids module-scope mutable state. Marked bets return at once (04:622).
5. Then the same per-trigger decision as the sweep (§3–§5), `passNow` from `houseBotRuntimeStore.dbClock()`.

### 1.4 The seam guard (C4-SPEC §7.1)
`scripts/house-bot-seam.test.mts` §4 (219–262): any `ctx.kind`, `house(Intent|Pool|Position)`, `houseBotId` comparison or `"house"` must sit within 3 code lines under a `// SEAM:<name>` marker; 4.2 markers = `SEAM_SITES` (`house-bot-seam.anchors.mjs:17-25`); 4.2b window touches house state; 4.3 only `fire.ts` imports `placeHouseBet`. Option A: call inside the 1751 arm with no `ctx.kind` token (no marker change). Option B: `if (ctx.kind === "player" && market.productLine === "UPDOWN")` under a new `// SEAM:trigger` with `"trigger"` in `SEAM_SITES`.

## 2. The sweep

### 2.1 Rules
- Keyset `(placedAt, id)`, pages of `SWEEP_PAGE_SIZE = 200` until caught up (P:230; constants:530); index `Position_placedAt_id_idx` (migration `20260916150100:53`; schema.prisma:1844).
- Start `max(watermark − SWEEP_LOOKBACK_MS 90 000, dbNow − SWEEP_MAX_LOOKBACK_MS 600 000)` (A11 04:342; ruling 3; constants:527-529). Only positions older than `passNow − SWEEP_MIN_AGE_MS 5000` (04:622; constants:490).
- SQL filter: unmarked, market LIVE, product line in scope, older than 5 s, `NOT EXISTS` a COUNTER intent with anchorKey = position id (04:622; 01:1203). `passNow` read once; ACTIVE targets with `createdAt ≤ passNow` in the same snapshot (N2 step 2, 04:3946).
- OFF / no ACTIVE bot → the watermark advances, no writes (04:343; 01:1131). Admission saturated → skip the pass while `queueDepth > 0` (04:616; 01:1091(4)). Inserts `ON CONFLICT DO NOTHING` (P:230). Naive-UTC `placedAt`; bounds as ISO `::timestamp` (ENG-12 01:1146).
- Watermark: `HouseBotRuntime` key `global` `sweepPlacedAt`/`sweepPositionId` (DAL:171-172), seeded to `now()` by migration `20260916150000:209` (memory seed DAL:1820); `advanceSweep(placedAt, positionId)` forward-only tuple compare (memory DAL:2126-2135; prisma 3179-3187).
- Watermark value unsettled: PLAN `min(max seen, now − 60 s)` (P:230) vs ENG-10 `dbNow − lookback` while OFF (01:1131) — open point 5. Which process runs the sweep and how often is specified nowhere (P:223-224; 04:2881-2892; `EngineTicks` engine.ts:70-75) — open point 4.

### 2.2 Missing DAL reads (both twins; `test:dal-parity` covers `HouseSeamStore`/`HouseBotRuntimeStore` members automatically, `scripts/dal-parity.test.mts:347-417`)
1. Keyset page of trigger candidates: `PositionStore` has no keyset (market-dal.ts:368-…; its comment forbids widening `listOpen`). Proposed `houseSeamStore.triggerPage({afterPlacedAt, afterId, fromIso, beforeIso, limit})` returning only `{id, userId, marketId, side, stake, placedAt}` of unmarked rows (I2 HOUSE-BOTS.md:37); plain SELECT.
2. Trigger-account facts per user `{role === PLAYER, liveHolder, penaltyToday, holderRecruit}`: exists only inline in `lockedPool` (SQL DAL:3857-3861 — `live` CTE non-REMOVED; `'penalty:' || u."id" || ':' || EAT_SQL.dayKey`; `recruitedBy`; memory DAL:2784-2788). N1 wants one shared SQL fragment (04:2425). No AlertOnce existence read exists.
3. BOTH_SIDES support ("did the house counter this account on this market"): no read; `listLiveOnMarket` is PENDING/CLAIMED only; index `HouseBotIntent_triggerUserId_createdAt_idx` exists.
- Present already: `positionStore.listForUserAndMarket`, `runtime.get`/`dbClock`, `targetStore.listActive({createdAtOrBefore})`/`activeForMarket`, `houseSeamStore.marketView`/`lockedPool`/`blackoutRow`/`botUsage`/`marketUsage`/`counterpartyToday`, `houseBotStore.findLiveByUserId`/`listNonRemoved`, `db.user.findByIds`. ENG-19 EXPLAIN pins `Position(placedAt,id)` and the anchorKey unique index (01:1204). Ruling 60: one read at a time.

## 3. Trigger filter and negative decisions (P:231-232; N2 step 4.1 04:3953; ruling 36)
| Fact | Result | Read |
|---|---|---|
| Marked | no row; hook returns | position row / committed bet |
| A live (non-REMOVED) bot's account | no row (I3) — the A21 check runs here | `findLiveByUserId` or the shared fragment |
| `role !== "PLAYER"` | no row | `db.user.findById(s)` |
| Penalty-boxed today | `filtered: "PENALTY_BOX"` | AlertOnce `penalty:<userId>:<EAT day from DB now()>` exists (constants:653; clock.ts:51) — new read |
| `recruitedBy` is a non-REMOVED bot's holder (A21 04:556) | `filtered: "HOLDER_RECRUIT"` | `user.recruitedBy` ∈ `listNonRemoved` holders |
- `decideCounter` (decide.ts:182-278) applies `scopeCode`, LIVE, global `scopeFrom` and "some bot covers or a target is eligible" first (→ `{row:null}`), then writes ONE SKIPPED COUNTER row for `filtered` (`reasonCode`, the target's bot or first `orderBots(inBotScope)`, `targetId` when eligible, `decision {entry:"AUTO", code, snapshot}`); TGT-25 expects `targetId` on both filtered codes (01:3316).
- No eligible candidate → one SKIPPED row with the target's code and `targetId`, else the first scope bot's code (decide.ts:393-403). `{row:null, code}` for PRODUCT_NOT_SUPPORTED (incl. demo), UD_NO_ROUND, NO_CUTOFF, MARKET_NOT_LIVE → no row; the code feeds once-only alerts (ruling 40): `ud-orphan-market:<marketId>` (constants:666), `product-denied:<value>` per day (constants:683, copy 466; not for demo), `ud-born-unlocked:<roundId>` (04:381, no detector in `decide`).

## 4. Scope start (A11)
`placedAt ≥ max(global scopeFrom, bot scopeFrom, target.effectiveFrom)` (04:3947; 04:339).
- Global: `HouseBotRuntime["global"].scopeFrom` — **no writer today** (`switchOn` DAL:1921-1928/2953-2964 writes control columns only; A11 wants it in the SWITCH_ON transaction, 04:335; the ON action is commit 7; migration :207-208 says NULL = never switched on = nothing in scope).
- Bot: `HouseBotRuntime["bot:<id>"].scopeFrom`, written by `designation.ts:502` at Start.
- Target: `effectiveFrom = createdAt + TARGET_ARMING_SEC (12)` from DB `now()` in the insert (target add action, commit 7).
- `decide.ts` applies them at 212 (`globalScopeFrom ?? placedAt`), 233/237 (`b.scopeFrom ?? placedAt`), 236 (`effectiveFrom <= placedAt`): **a null means "no bound" there**, contradicting DAL:173-175 and the migration (open point 1). Cases 7.15/7.16 test non-null only.

## 5. Targeted COUNTERs (N2 §4 steps 1–6)
1. Polls are sweep-only (04:3940).
2. Arming: `passNow` once; `targetStore.listActive({createdAtOrBefore: passNow})` (one ACTIVE per poll); a target counts only when `effectiveFrom ≤ placedAt`, its bot is ACTIVE with parseable rules, `targeting.enabled`, polls product on, category listed (decide.ts:189, 234-241; N2 04:3957). 12 s = 5 s skew + 2 s + 5 s age (constants:492-500; 04:3873).
3. Blackout: one `infoBlackout(marketId)` per poll with an ACTIVE target, passed as `{blocked}`, gating the target candidate only (ruling 41; decide.ts:303). `trigger.ts` is a pinned importer of `blackout.ts` (04:2598); `decide.ts` never imports it (04:3951).
4. Target candidate order (decide.ts:297-311, `marketCode` first): A16 MARKET_REOPENED/CHAIN_NOT_RUNNING → OUTSIDE_SCHEDULE (at `placedAt`) → TRIGGER_STAKE_RANGE → NO_REACT_ZONE → EXIT_WINDOW_TOO_LATE or CUTOFF → MARKET_HELD → INFO_BLACKOUT → `capPrecheck` → cut `lockedAtDue − raw(botSide)` and clamp → STAKE_BELOW_MIN. React probability, pool band, skip-closing not applied (W8 04:3966). Row: `targetId`, `staleAt = dueAt + 60 s`; `passNow ≥ staleAt` → EXPIRED(STALE); `decision {entry:"TARGET", targetId, delaySec, timingFrom, reactTo, requestedDueAt, heldToExit, lockMarginMs, exitCloseAt, snapshot}` (312-331). Fallback `targetSkipped {targetId, code}` on a scope bot's row (333, 386), else one SKIPPED row with `targetId` (394-401). Scope bots in `orderBots` order (156-160; P:236), excluding the target's bot (337).
5. Timing, loader side (ruling 38): `delaySec = drawTargetDelay(target, randomInt)` (decide.ts:406-409); `exit = exitWindowFacts({placedAtMs, closesAtMs: selectionClosedAt ?? resolutionAt, graceMin, paidMin})`; `requested = (STAKE ? placedAt : exitCloseAt) + delay`; `dueAt = max(requested, exitCloseAt + LOCK_MARGIN_MS)` (same terms as decide.ts:294-295; 04:3974-3981); then `lockedForHouse(marketId, {...lockedPoolInputsOfView(view), asOf: dueAt})` and `target: {...target, drawnDelaySec, lockedAtDue: pool[triggerSide].locked}` into `decideCounter`. The untargeted cut stays `nonHouse(trigger side) − raw(bot side)` (decide.ts:363); fire re-cuts on `lockedA15`.
6. Insert: targeted `insertTargetedIfActive(targetId, row)` → `{inserted, targetActive, row}` (Prisma `FOR SHARE` + `ON CONFLICT DO NOTHING` in one transaction, DAL:3323-3336; memory `memAtomic`, DAL:2264-2276). `targetActive:false` → re-run `decideCounter` with `target:null` in the same pass and insert the untargeted row (04:3971); `inserted:false, targetActive:true` → already decided. Untargeted `insertIgnoringConflict(row)` (DAL:1451). `NewHouseBotIntent` = `Omit<StoredHouseBotIntent,"idempotencyKey"|"createdAt">` (DAL:306): the loader adds `id` (`hbi_`), `anchorKey = triggerPositionId` (CHECK `HouseBotIntent_counter_anchor_check`, DAL:783-784), zero attempts, null claim/position/alert/request/entry fields, `finishedAt` as CHECKs require. No `DecidedRow → NewHouseBotIntent` mapper exists. Red N2-E1 (`effectiveFrom` dropped), N2-E6 (FOR SHARE removed) (04:4403, :4408).

## 6. A21 — holder against his own bot; recruits
- Detection (04:553): an unmarked position whose user holds a live bot, on a market where that bot holds the OPPOSITE side (same side: nothing). Reads: `findLiveByUserId(userId)`, then `positionStore.listForUserAndMarket(bot.userId, marketId)` filtered to that bot's OPEN opposite-side house positions (`y` = their stake sum).
- Where: the hook (Up & Down only — N2 04:3941 over A21 04:552's "hook check") and the sweep for every position, polls included (04:555; test "hook disabled: the sweep alerts once", 04:561).
- Key `ALERT_KEY.holderAgainst(botId, marketId)` = `holder-against:<botId>:<marketId>`, no day suffix (constants:672; ruling 15); claim, release on a failed send (pattern outcomes.ts:97-110, private). Copy (C13 04:1051; ruling 15): "Holder of Bot A staked TZS {x} {SIDE} against Bot A's TZS {y} {SIDE} on {market}." → `/admin/markets/<marketId>`; C13's `holder-conflict:` key overruled by ruling 15; players/holders only as `playerHandle` (R6). Event `HOLDER_AGAINST_BOT` (constants:271), payload unspecified. Never refuse the bet or pause the bot (04:555). Recruits → HOLDER_RECRUIT (§3).

## 7. Penalty box (R5, ruling 27)
- CASHED_OUT_COUNTERED: built — fire → H3 `house_trigger_gone` → mapper `{SKIPPED TRIGGER_EXITED, penalty:true}` (outcome-map.ts:84) → `penaltyBox(intent)` (outcomes.ts:235-244, 292).
- BOTH_SIDES: to build in the sweep — the trigger account holds OPEN positions on both sides of a market the house countered (ruling 27; P:231).
- Key `ALERT_KEY.penalty(userId)` = `{prefix:"penalty:<userId>", unit:"day"}` via `claimWithEatSuffix` (EAT day from DB `now()` inside the INSERT; constants:626-653; DAL:1421). The row IS the box (`lockedPool` excludes it, DAL:3860/2787; the trigger filter reads it); purged after 30 days.
- Event PENALTY_BOXED (R5 04:1229 lists `{userId, day, cause, intentId}`, 7 years); the built write has `houseBotId`, `userId`, `marketId` columns and payload `{cause, intentId}` — no `day` (open point 9). DSAR `liquidityDecisions.excludedDays[{day, cause}]` is commit 5 (04:1228).

## 8. Up & Down specifics
- Round: `houseSeamStore.marketView(position.marketId)` → `projectMarketView`; `HouseViewRound {roundId, chainId, chainKey "<assetId>:<durationMinutes>", roundNumber, opensAt, durationMinutes, openPrice, upTarget, downTarget, chainRunning, assetEnabled}`; no round → UD_NO_ROUND; cutoff = `min(opensAt + D, selectionClosedAt ?? resolutionAt)` (A12 04:371).
- Price: `udPriceForDecision(assetId, {nowMs})` (ruling 57) with `assetId = chainKey.slice(0, chainKey.lastIndexOf(":"))` and DB `nowMs`, as fire does.
- Closeness `udCloseness` (decide.ts:123-132): untargeted UD candidates (354), re-checked at fire; decision records `price`, `priceSource`, `priceAgeSec` (385).
- A16 `marketCode` (decide.ts:281-285). UD `staleAt = dueAt + 30 s` (constants:514); untargeted hold `max(requested, exitCloseAt)` with no margin (decide.ts:344; 04:4015-4019).

## 9. Tests and guards
- `test:house-bot-engine` (demanded): PLAN P:517 (bot-vs-bot, staff and AGENT triggers ignored; bait-and-dilute; TRIGGER_EXITED + penalty box; UD_CLOSENESS at 0.3·margin with 25%; exit-window hold; sweep catches a late commit); A11 04:348-350 (40 positions while OFF → 0; 3-day watermark with 10k positions → 0 and watermark ≥ now − 90 s; `placedAt` now − 45 s with no hook → exactly 1); A12 04:384-387; A15 04:445-447; A16 04:468-473; A21 04:559-562; A24 04:626-634 (saturation, skew, hook overflow counter ENG-05 01:1092); N2 04:4359-4377 (`effectiveFrom` ±1 s → 0/1; 8 s skew; poll position to the hook → 0 rows, the sweep decides; removal races; target + scope bot → target first; `targetSkipped`; timings +307/+310/+307/+7 s; UD 3-min 5/5 → +5 s; cash-out during the hold; late sweep → EXPIRED(STALE); `decide.ts` imports neither `blackout.ts` nor the store). Scenarios ENG-10, -13, -19 (load + EXPLAIN), -22, -23, -24, -31, -33; TGT-11(c), -19, -21, -25, -26.
- `red:house-bot-engine`: N2-E1, N2-E5, N2-E6 (04:4403-4408); TGT-19's mutation. `test:dal-parity`: the share-locked insert twin (04:4346) and every new member. `test:house-bot-comms`: holder conflict (04:1078). `test:dsar-secrets`: R5 `excludedDays` (04:1235; commit 5). Not yet in `package.json` (do not cite in code): `test:house-bot-info-edge`, `test:house-bot-comms`, `test:house-bot-holder-lifecycle`, `red:house-bot-engine`.
- Guards that go red (C4-SPEC §7): `test:house-bot-seam` §4 (markers + `SEAM_SITES`), 2.0 (`buyPositionInner` body > 20 000 chars), §3 parity; `test:red-anchors` (every `from` once; `UNDECLARED_CEILING = 65` exact — `red:house-bot-engine` must declare its anchors file); `test:failure-reasons` 9b (`reasonCode`, never `code:`) and 8c; `test:layout-staleness` 5.7 and anchor :92; `test:house-bot-rules` 11.8 / 11.22 (EVENT_KINDS stays 30) / §0; engine 11.22; the info-edge walker (P:220; `blackout.ts`, `ud-price.ts` exempt by name; `trigger.ts` never imports `oversight.ts`, 04:2612); `test:decomment`; `test:multi-container` only if `lifecycle.ts` is touched.

## 10. Open points needing a build ruling
1. **Null `scopeFrom`.** `decide.ts:212/233/237` treat null as no bound (`planFill`/`planOpener` use `passNow`, 439/493), but DAL:173-175 and the migration say null = out of scope, and nothing writes the global `scopeFrom` (A11 04:335). Options: (a) the trigger returns no row when either is null, before `decide`; (b) fix `decide.ts` + cases; (c) also have `switchOn` write it in its transaction now.
2. **Hook call-site shape:** (A) inside the UD player `else` arm (no seam change, depends on the notification branch) or (B) a `// SEAM:trigger` block under 1675 + `SEAM_SITES` entry (matches C4-SPEC §2's wording).
3. **Admission context leaks into the hook** (`withAdmission` 954; `runOutsideLock` exits only `lockStore`): (a) an admission-exit helper wrapping both, or (b) accept and pin that `trigger.ts` never calls `withAdmission`/`fire`.
4. **Where and how often the sweep runs:** (a) inside `plannerTick` under the 45 s lease every 15 s; (b) its own leader-leased timer at 2–5 s (UD `staleAt` = due + 30 s); (c) the poller on every instance (duplicates harmless via `ON CONFLICT` + forward-only `advanceSweep`, but load multiplied).
5. **Watermark value:** (a) last `(placedAt, id)` read; (b) PLAN `min(max seen, now − 60 s)`; (c) while OFF/idle `dbNow − 90 s` with id `""` (ENG-10). `advanceSweep` needs an id.
6. **A21 gating:** hook polls are sweep-only (N2 over A21); with master OFF or only PAUSED bots the sweep "writes nothing" (04:343), so no A21 alert then — rule whether A21 is exempt from the OFF no-write rule.
7. **A21 content:** HOLDER_AGAINST_BOT payload unspecified (proposal `{botId, marketId, side, stakeTzs}`, no position ids, D6); the UD `{market}` text needs the asset symbol, but `HouseViewRound.chainKey` holds the asset id (DAL:2842) while the `marketView` doc says symbol (DAL:1709-1711).
8. **BOTH_SIDES predicate** ("a market the house countered"): (a) any PLACED COUNTER on the market; (b) a COUNTER intent (any status) with this `triggerUserId`; (c) a PLACED house position on the market — plus which `intentId` the event carries and the new read.
9. **PENALTY_BOXED payload and alert:** R5 lists `{userId, day, cause, intentId}`; `outcomes.ts:242` writes `{cause, intentId}` with no `day`; PLAN promises one admin alert (P:231), the code sends none.
10. **`capPrecheck` and staff-chosen clamps:** no producer of `DecideBot.capPrecheck`; `decide.ts:306-311` does not clamp targeted stakes to the staff-chosen TZS left today (N2 step 6, 04:3986); TGT-25 expects CAP_COUNTERPARTY_COUNT (01:3316). Shared with the planner builder — one ruling for both.
11. **Due-time formula duplicated** (ruling 38 loader vs decide.ts:294-295): export a pure `targetDueAt()` from `decide.ts` for both (red N2-E2/E3).
12. **Unique-violation swallowing:** `insertIgnoringConflict` swallows every unique violation, while TGT-26(e) wants an unknown index rethrown (01:3334) — accept (only `hbi_counter_anchor_uq` can hit a COUNTER) or scope the conflict target.
13. **Hook always off until the engine starts:** `hookSuspendedBySkew` is true while `skewMs == null`, measured only by a started engine (wiring waits for step 9, ruling 46) — accept sweep-only on refused/unwired containers or measure skew independently.
14. **Hook cache scope and read cost:** PLAN caches only "switch on and any bot ACTIVE" (P:229); ENG-19 also caches bots, rules and penalty box. A cached penalty box could counter a boxed account (fire has no penalty re-check; the untargeted cut uses unfiltered `lockedA15`); `marketHeld` costs 5 reads per bot and ruling 60 wants sequential reads — rule per-pass caching and refreshing the in-pass held state after each insert on the same market.
15. **Overflow counter:** where the hook overflow count is stored and reported (EngineState field / health) is unspecified (04:616; ENG-05).
