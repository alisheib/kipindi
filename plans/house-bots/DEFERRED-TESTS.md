# House bots — the skipped-run register (ruling 275)

> **Authority.** `C5-D20-REPLAN.md` ruling 275 (Ali, 2026-09-17): the **initial** check of a change runs exactly as it
> always has; **repetition** is dropped. Every run a checkpoint skips is written here before that checkpoint closes, with
> the exact command, the store, what the run would prove, and why the skip is safe.
>
> **Commit 5 may not be marked ✅ until C5-8 has run this file to empty or Ali has released a row.** A row here is NOT a
> passed test — the PROGRESS entry that skipped it says NOT MEASURED.

## How to use this file

- **Adding a row** — the checkpoint that skips the run adds it, in its own section, and commits this file by name.
- **Clearing a row** — C5-8 (or the later test session) runs the command, records the printed result in the Result
  column, and moves the row to §3. A row whose run then fails is a finding, not a clearance.
- **Releasing a row** — only Ali releases a run without executing it, and the release is quoted in the row.

## 1. Open — owed before Commit 5 closes

| # | Checkpoint | Command | Store | What it would prove | Why the skip is safe |
|---|---|---|---|---|---|
| 1 | C5-5b | `npm run test:house-bot-caps` | both | That the per-bot and global cap refusals still fire on both stores | Changed files it DOES load: `house-bot-dal.ts` (C5-5b only REMOVED unused members — `test:dal-parity` 1348/0 and `test:house-bot-migrations` 655/0 on a real cluster), `constants.ts` (only `ALERT_KEY.staffEdge` / `STAFF_EDGE_MIN_SETTLED`) and `house-bot/clock.ts` (only `eatMonthWindow`); `book.ts` is byte-identical to its last green state for `houseDayBook` / `houseOpenExposure`, the two members this suite reads. MEASURED (grep over `scripts/`): no case in this suite names any removed member, column, alert key or module. No cap rule, limit or seam line changed. (Corrected by C5-5b's fixer: the earlier reason claimed no changed file reaches it at all — review conformance-04.) |
| 2 | C5-5b | `npm run test:house-bot-comms` | both | That every house bell and letter the engine sends still reads as it did | The only notifier C5-5b touched is `notifyAdminMarketCancelled` — a PLATFORM notice, not a house one — and its bytes are `main`'s again (exit gate A: no diff line in `notification-service.ts` touches it). `test:cert-c1` 1215/0 and `test:cert-c3` 1521/0 cover the templates and bells that did change. |
| 3 | C5-5b | `npm run test:house-bot-holder-lifecycle` | both | That designate → start → pause → remove and the holder's own doors still behave | It loads the edited `house-bot-dal.ts` and `constants.ts`, but MEASURED (grep over `scripts/`): no case in it names any removed reader, column, alert key or display module, and `designation.ts` was not edited by C5-5b (`test:house-bot-designation` 169/161 ALL PASS covers the status word it shares). (Corrected by C5-5b's fixer — review conformance-04.) |
| 4 | C5-5b | `npm run test:all` here and in `C:/kipindi-old-build`, compared red by red | — | That C5-5b introduced no red that clean `origin/main` does not also have | Ruling 275 assigns `test:all` red-by-red to C5-8 alone, once for the whole commit (§2 row A). C5-5b ran every suite its changed files reach, and compared the two inherited reds it does touch (`test:red-anchors` 4 · rg-doors ×2 + 4.1/4.2 66 vs 65; `test:read-tiers` 7.1) line by line against the known-red list. |
| 5 | C5-5b | The mutation batch of the FOUR NAMED registers — `plans/house-bots/tools/c5-s4-mutations.json` (85), `c5-s5-mutations.json` (140), `c5-5b-mutations.json` (26, written by the un-build's fixer) and `c7-s1b-mutations.json` (68 declarations + 2 header lines) | both | That every surviving assertion of Commit 5, and the un-build's own guards, can actually fail | Ruling 275 gives the batch to C5-8, once for the whole commit (§2 row E). C5-5b planned its own guard mutations and ran none; every struck ruling's mutations are listed as REMOVED, never as run. ⛔ **The glob was `tools/*.json` until 2026-09-18 (replan ruling 510) and is now the four files by name.** A glob picks up whatever is in the folder, and it had already picked up one dead file: `c7-s1-mutations.json`, step 1's SUPERSEDED register, all 58 of whose ids are contained in `c7-s1b-mutations.json`'s 68 — and six of whose `from` texts (`313-loader`, `432j-action-reason`, `432o-nowrap-header`, `432o-exposure-name`, `432o-bets-axis`, `417-docblock`) RESOLVED TO ZERO against the tree when re-measured at `8c3b90d3`, so the batch would have reported them as mutations that "could not be applied" against code that was corrected weeks earlier. The file is DELETED in the same commit; a register that misstates what a step built is worse than no register. A new register joins this row BY NAME. |
| 6 | C5-5b | `npm run qa:house-bot-holder-view` | — | That the holder's own pages and RSC payloads still carry nothing about house bots (D19c) | C5-5b removed admin-only display and readers; it changed no player or holder surface (exit gate A: the 10 display files and both player surfaces are byte-identical to `origin/main`, and reports 0.198.1–3 pin the player population from disk). C5-8 runs it once for the commit (§2 row C). |
| 7 | C5-5b (fixer) | `npm run qa:house-bot-console-probe` (it needs a fresh `next build`, then `next start`) | Postgres + served | That the ADMIN positive control 4.3 still hits every route instance it names now that `/admin/kyc/[id]<holder>` is back in `MUST_CARRY`, and that no non-staff response carries a house word | The fixer RE-ANCHORED the control (review d19-hunt-03): C5-5b had dropped the KYC case with the six display pages, but that page is not one of them — `src/app/admin/kyc/[id]/page.tsx:126` still reads the holder's durable audit rows through the gate, and the probe's own first-time designation writes `house_bot.password_verified` against that holder, so an ADMIN must still receive house data there. Ruling 275 spends the one fresh build at C5-8, which runs this probe once for the whole commit (§2 row D). The same durable read, the same viewers, are MEASURED at unit level on BOTH stores by `test:house-bot-reports` 4.260.5. |
| 8 | C5-5b (fixer) | Extend `scripts/house-bot-console-probe.mts`'s `viewers` with SUPPORT, COMPLIANCE and MODERATOR cookies and assert, per route, that a staff role without that route's view grant (and a MODERATOR on an Owner-only path) receives no house hit | served | The STAFF half of ruling 259's audience on the SERVED layer, not only in the gate's own unit cases | Not attempted here: the probe cannot run without the one fresh build ruling 275 reserves for C5-8, and an unrun change to a served harness is worse than a tracked one. The audience matrix itself is now MEASURED on both stores by the restored `test:house-bot-reports` 4.259.2, 4.259.3, 4.260.2 and 4.260.6 (COMPLIANCE, MODERATOR, SUPPORT, Owner-only paths, a PLAYER holding a view grant). C5-7 owns the served layer (replan §3). |
| 9 | C5-5b | The 360 tile of the resolver-queue card's crowd/held row, the ceremony's pools line, the objections POOL HELD cell, the Up & Down rounds VOLUME cell, the markets Actions cell, the KYC "Money at stake" card and the notifications bell body | — | That no leftover gap, empty slot or stray separator survives the un-build at 360 width on those seven surfaces | The 1280 tile of each WAS opened and read; in the captured 360 tile the changed region is below the fold or outside the ScrollX viewport (review visual-01, visual-02, visual-05), so those seven 360 reads are NOT MEASURED. What stands instead is exit gate A: each of those files is byte-identical to `origin/main`, so no markup of the removed line can remain — a proof of absence, not a render. No server was up for the fixer and ruling 275 forbids a second build here; C5-7 re-photographs every page at 1280 and 360 with the changed element scrolled into view first. **⚠️ ATTEMPTED AND NOT CLEARED at C7 step 1 (2026-09-18), and this is a finding, not a clearance (ruling 462 asked for it).** A server WAS up on this step's build, but the render database is seeded by `seed-admin-local.mts` + `seed-staff-local.mts` + the desk's own fixture only: it holds NO market, NO position, NO objection, NO Up & Down round and NO KYC case, so every one of those seven regions renders as an EMPTY QUEUE and the changed region is not on screen at any width. A tile whose subject is absent measures nothing — the exact defect the visual lens caught last time, pointing the other way. Clearing this row needs the fixture `house-bot-console-probe.mts` builds (a resolved market with crowd and held stakes, an objection, a void, a round, a KYC case), driven against a SERVED build; that is C5-7/C7's own visual pass and is now named as its precondition. |
| 10 | C5-5b | The four phase-D surfaces at 1280 and 360: the emergency-void dialog, the objection-decision dialog, the Up & Down void-round dialog, and the bulk-resolve confirm with rows ticked | — | That the removed `{exposureSlot}` above each "Reason (required)" field and the removed bulk count sentence left no gap or empty slot | Never captured — every shot is of a closed page (review visual-04). All four client components AND their server callers are byte-identical to `origin/main`, so the dialogs are provably `main`'s markup. C5-7's client pass opens each one. **⚠️ ATTEMPTED AND NOT CLEARED at C7 step 1 (2026-09-18) — a finding, not a clearance.** Same cause as row 9: each of those four dialogs is opened by a CONTROL ON A ROW — an emergency void needs a live market, an objection decision needs an objection, the rounds void needs a round, the bulk-resolve confirm needs resolvable rows ticked. This step's render database holds none of them, so there is no control to click and nothing to photograph. The byte-identity proof stands unchanged. |
| 11 | C5-5b (fixer) | `npm run test:house-bot-money`, `test:house-bot-engine`, `test:house-bot-designation`, `test:house-bot-info-edge` | both | That the money, engine, designation and info-edge behaviour over `market-service.ts` is unchanged after the fixer edited that file | The fixer's only change to `src/lib/server/market-service.ts` is ONE BLANK LINE restored between two statements inside `emergencyVoidMarket`, so the function differs from `origin/main` only by Commit 3/4's sanctioned seam lines (review conformance-06). No token, no order and no behaviour changed; `test:house-bot-seam` 96/0 and `test:red-anchors` (the inherited 4 only), which read that file as text, were re-run. The implementer's two-store runs (money 113/131, engine 698/677, designation 169/161, info-edge 20/5) were measured on bytes identical in every other respect. |

## 1b. Open — added by Commit 7 step 1 (the desk's console page, 2026-09-18)

| # | Checkpoint | Command | Store | What it would prove | Why the skip is safe |
|---|---|---|---|---|---|
| 12 | C7-1 | `npm run test:house-bot-caps`, `test:house-bot-seam`, `test:house-bot-holder-lifecycle`, `test:house-bot-info-edge`, `test:house-bot-migrations` | both | That the caps, seam, holder-lifecycle, info-edge and DAL behaviour is unchanged after this step edited `rules.ts`, `designation.ts`, `eligibility.ts`, `alert-copy.ts` and `notification-service.ts` | Those five suites LOAD `rules.ts` (through `house-bot-world.mts`'s `labelKey`) but MEASURED (grep over `scripts/`): not one case in them names `LIMITS_TAB_HREF`, `DESIGNATE_COPY`, a refusal `href`, the roster-full sentence or the route segment — and `grep -rn "admin/desk" scripts/` now returns only `verify-house-bot-bundle.mjs`'s deliberate planted-path control. Every suite that DOES assert one of those values was RUN on both stores and is green: `test:house-bot-rules` 521/0, `test:house-bot-designation` 3/0, `test:house-bot-comms` 3/0, `test:house-bot-engine` 3/0, `test:house-bot-money` 3/0, `test:house-bot-console` 95+45/0, `test:house-bot-reports` 3/0. No cap rule, limit, seam line or DAL member changed. |
| 13 | C7-1 | The six-width sweep (360·640·768·1024·1280·1920) of every NON-DEFAULT state of §5 surface 1 — switch ON, the four off-causes, two limits unset, three money limits unset, the schema state, 0/1/5 accounts, roster full/not-full | — | That no state reflows differently from the default at 640, 768, 1024 or 1920 | ⚠️ **CORRECTED BY THE REVIEW PASS: these tiles were taken at `3f789384`, and the markup has MOVED since.** `git diff 3f789384..c812a03e -- src/app/admin/desk/page.tsx` shows the page LOST a whole column (the way-out `open →` column, `colSpan` 7→6), renamed "Exposure" → "Open exposure", gained `!whitespace-normal` on three headers and `min-w-[128px]` on Status, turned "Set N global limits first →" from a `<Link>` into an arrow-less `<span>`, gained the `switchReason` line beside the Toggle, dropped the roster-full sentence's own arrow, and reworded all three auto-off Callout bodies. The 2026-09-18 finish re-photographed SEVEN states at 360 and 1280 off the current build; the states this row still owes — **switch ON, the GLOBAL_LOSS_STOP / ENGINE_FAULT / ENGINE_ERRORS off-causes, two limits unset, three money limits unset, and 0 / 1 accounts** — have NEVER been seen as they now render, and the unset-limits state is the one whose own rendering 432(i) changed. Treat them as UNCAPTURED, not as read. Ruling 464 requires the full six widths for the DEFAULT state (done and READ) and 360 + 1280 for every other state (done and READ at `3f789384`, 22 tiles). MEASURED at 360 and 1280: every non-default state differs from the default by exactly ONE inserted or removed block (a Callout, a "Set N global limits first →" strip line, a tile's value, an empty state), inside the same `AdminBody > space-y-4` stack — no state changes a grid, a column count or a card's own layout, so 464's own escalation trigger ("a layout difference beyond simple reflow") never fired. The commit-close visual pass sweeps them at the remaining four widths. |
| 15 | C7-1 | `qa:admin-view-matrix`, `test:admin-section-gate`, `test:responsive`, `qa:chaos`, `qa:dg-shell`, `qa:dg-measure`, `qa:admin-load` | — | That the desk drives clean as a third Owner-only surface, and inside the design-gate instruments | Each needs a running local app on a port this session does not own: `admin-section-gate`, `revoked-deadend` and `needle-rest` hard-code port 3009 (a parallel session's), and the design-gate drives take the shared `.qa-design-gate` session. `/admin/desk` is now IN `scripts/design-gate/routes.mjs` and in `break-it-admin.mjs`, so all six design-gate consumers pick it up with no further edit. What IS measured instead, on the served build: `qa:house-bot-console-probe` 4.1 — 0 hits over 1158 non-staff responses on 126 route instances, with the ADMIN control firing on `/admin/desk` AND `/admin/desk?tab=roster` by name. |
| 16 | C7-1 | The SERVED half of ruling 301: the restricted panel on `/admin/desk/<a real hb_ id>` as a non-ADMIN staff viewer | served | That the panel's heading is the neutral section word and never the record id | MEASURED and NOT MEASURABLE YET: `/admin/desk/[id]` has no page until C7 step 4, so that path answers 404 for EVERY viewer — the section layout never paints, and there is no panel to read. The restricted panel WAS captured and read on `/admin/desk` itself at 360 and 1280 (heading "Desk", no house word, no id). The ID half is measured at unit level today by `test:house-bot-console` 1.301, which asserts on the real resolver that `crumbsFromPath("/admin/desk/hb_<24 hex>").at(-1)` IS the raw id — so the prop's necessity is proven even though its served effect is not yet reachable. |
| 17 | C7-1 | The `loading.tsx` mid-load capture at 360 | — | That the swap does not jump at phone width | Ruling 464 requires surface 9 at 1280, which was captured and READ (card for card: 6 ghosts against 6 cards, residuals −10 strip / −4 per tile / −30 table, no card missing and no card extra). At 360 the sidebar nav `<Link>` is not rendered at all (the mobile nav replaces it), and an injected `<a>` would be a HARD navigation, which never shows a Suspense fallback — so the fallback cannot be reached at 360 without driving the mobile nav's own sheet. The commit-close client pass owns it. |
| 18 | C7-1 | The mutation batch `scripts/anchors/house-bot-console.anchors.mjs` (59 declared mutations) through `npm run red:house-bot-console` | both | That every assertion this step added can actually fail | Ruling 275 gives the BATCH to the commit close, once, from a temporary worktree on its own scratch Postgres port. ⚠️ The count is 59, not the 34 this row first said — `ae09672b` and `37cf48c3` added 24 more and this finish added `0-throw-schema-rows` for its own new assertion. Every anchor is PROVEN RESOLVABLE: `test:red-anchors` §3 imports the declaration file and insists each `from` matches exactly once. **FOURTEEN have been run INDIVIDUALLY, each reverted after its run, each CAUGHT on the assertion its own declaration names — and the arithmetic is now read OFF THE LIST rather than typed beside it, because this row has twice been one short.** At the 2026-09-18 finish (ruling 468's finding (h)), **TEN**: `355-all` (`1.355 · …a SETTLING combinator`), `321-nav` (rbac `13 · …the desk is still absent from SUPPORT's nav`, 1 of 124 red), `347-second-read` (`1.347 · exactly ONE day read and ONE exposure read`), `432l-day-fold` and `432l-exposure-fold` (`1.347 · 432(l) · the band measures the GATE's population`), `308-manual`, `308-stale`, `432m-sunset`, `432j-action-reason` and `0-throw-schema-rows` (`0.throw`). At the REVIEW PASS that followed (`c812a03e`), **FOUR MORE**, run through `red:house-bot-console -- --only …`, printed `4 caught, 0 missed, 0 files left dirty`: `432i-arrow-strip` → `1.314 · 432(i) · the PLAIN form … WITHOUT its tail`; `404-round` → `1.404 · the delta is the FLOORED proportion …` (delta printed `100% of open exposure limit`); `432p-wrong-side` → `1.306 · 432(p) · … the no-break space is AFTER every '·'`; `318-expect-drift` → `1.318`'s own roll-call. ⚠️ **The declaration count is now 68, not 59** — the review pass added nine — and **THREE of the pre-existing 59 could not have reported at all**: `453-off`, `453-accounts` and `373-subject` named labels that had been reworded, so `red:house-bot-console` would have printed WRONG-ASSERTION and exited 1. They are fixed, and the CLASS is now guarded every day by `test:house-bot-console` 1.318. So **54 remain for the close** (68 − 14). |
| 19 | C7-1 (finish, corrected by the review pass) | `npm run verify:house-bot-bundle` and `npm run qa:house-bot-console-probe` off a build of HEAD | served | That the public JavaScript still carries no house word (D19), and that no console response reaches a non-staff viewer (rulings 259/260) | **NOT MEASURED FOR HEAD, and the reason this row first gave was FALSE.** It claimed `src/` was byte-identical to the build both instruments last ran against. MEASURED: `git diff --stat 3f789384..HEAD -- src/` → **4 files, +329 −107** (`desk/page.tsx`, `desk/loading.tsx`, `house-console-read.ts`, `console-routes.ts`) across `ae09672b`, `37cf48c3` and now `c812a03e` — the gated page, the module that decides what is SENT, and every JSX string ruling 453 added. The narrow claim that held was only `git diff 21556245..HEAD` at the time it was written, which is not the claim the row made. **What IS on the record:** the last actual execution of either instrument was at `3f789384` — bundle scan 0 hits over 167 static files / 9 prerendered documents / 35 public files with its planted-path control firing; probe 4.1 with 0 hits over 1158 non-staff responses on 126 route instances with the ADMIN control firing on `/admin/desk` by name. Neither number is current for HEAD and no reasoning here is offered in place of one. Both belong to §2 rows B and D, off the commit's own fresh build. ⚠️ The review pass could not run them either: the one build ruling 275 spends per step was spent at 10:00 on this step's FIRST pass, its `.next` is now stale for HEAD's `src/`, and scanning a stale build while reporting the number for HEAD is the exact defect this row is being corrected for. |
| 20 | C7-1 (finish) | The 640, 768, 1024 and 1920 tiles of the SEVEN states captured at this finish — the money-bearing roster (default), roster not full (5 of 8), SUNSET at the maximum, the schema state, the failed ROSTER read, the failed MONEY read and the unreadable CONTROL read | — | That none of those states reflows differently from the default at the four middle widths | Ruling 464: a non-default state owes 360 and 1280, and every one of these was captured and READ at both. MEASURED across the pair: each differs from the default by exactly one inserted or replaced block inside the same `AdminBody > space-y-4` stack — a Callout, an `AdminLoadError` in place of the strip or the table, a tile's value swapped for `n/a`, a cell's figure swapped for '—' — and none changes a grid, a column count or a card's own layout, so 464's escalation trigger (a difference beyond simple reflow) never fired. `qa:house-bots-visual` DID sweep the default state at all six widths in this same session, 66/0, and the review pass has now OPENED AND READ the four middle-width tiles it wrote (row 14) — the sweep's pass count is NOT offered here as the reading. ⚠️ **AND THESE SEVEN STATES ARE NOW THEMSELVES ONE COMMIT STALE**: `c812a03e` changed three of the strings they show (both disabled-control reasons, the roster-full sentence's arrow, all three auto-off Callout bodies) and added the loader's action ghost. See row 21. The commit-close visual pass sweeps the rest. |
| 21 | C7-1 (review pass, `c812a03e`) | A fresh `next build`, then `next start` on 3021, then re-capture and READ at 360 and 1280: the money-bearing default (the head's roster-full sentence, now arrow-less), the roster-not-full state (the two disabled-control reasons, now different sentences), the SUNSET state (the Withdrawn Callout, now without "Nothing will be staked"), the GLOBAL_LOSS_STOP and ENGINE_FAULT Callouts (also reworded), and the loader mid-load (its head now ghosts the action row) | served | That the five copy changes and the loader ghost read correctly on screen at both widths, and that the ~105px swap jump at 360 is gone | **NOT RUN, and the reason is the build budget, not confidence.** Ruling 275 spends ONE `next build` per step and step 1 spent it at 10:00; the served app and the scratch cluster were both stopped before this pass began, so re-rendering needs a SECOND build, which the pass was instructed not to take. What IS measured instead, on both stores: every one of these five changes is a STRING the gated reader or the page returns, and each is pinned by a case that was SEEN RED on the unfixed code first and by its own declared mutation — `432(j) · 432(n)` (the two reasons differ, and each names its control), `1.308 · 432(n)` (no Callout body repeats the strip's sentence, with a control that reads their lengths), `1.314 · 432(i)` and `1.312a · 432(i)` (the plain form and where the page paints it), `1.417` (the loader's ghost sequence and its head ghost, read from the PAGE's own `<Button size="md">`). The screens they replace ARE on the record and were re-read by this pass: `desk-notfull-1280.png` shows "Not ready on this build yet." twice, `desk-sunset-full-1280.png` shows the strip's sentence and the Callout's repetition ~87px apart, and `.qa-house-bots/admin-desk-{640,768,1024,1920}.png` show the live-looking "on Limits →". |
| 22 | C7-1 (review pass) | `KP_BASE=http://127.0.0.1:3021 npm run qa:house-bots-visual` after the two edits this pass made to `scripts/qa-house-bots-visual.mjs` | served | That §5.1's new CONTROL (`the money scan reached at least one figure`) fires, and that `clipBox`'s corrected `reachable` (now `auto|scroll` only, never `hidden`) does not newly flag a figure | **NOT RUN — it needs the served build row 21 also needs.** Row 14's clearance (66/0) stands for the file AS IT WAS RUN; these two edits are AFTER that run, so the row is not re-cleared by it and that is stated here rather than assumed. Both edits are review findings `test-strength-5` and `test-strength-10`: §5.1 was the one new check in the file with NO planted control while §5.3, §5.4 and §5.6 each gained one in the same commit (an empty selector reports zero clipped figures and reads as compliance), and `reachable` was computed from `scrollWidth > clientWidth` alone while the box-selection regex deliberately includes `hidden` — so a figure genuinely sliced by a hidden-overflow edge was exempted as "one scroll away". `node --check` passes. Risk measured by reading: the nearest overflow ancestor of every `.amount` on this page is the `ScrollX` region, which is `overflow-x: auto`, so the corrected predicate returns the same answer for the default state; and row 14's own §5.2 numbers prove the money population was non-empty, so the new control cannot newly fail. |
| 23 | C7-1 (review pass) | A scan of the SENT payload and of the ATTRIBUTES of the fourteen non-default states captured by the gitignored drivers under `.qa-house-bots/drv/` | served | That no `aria-label`, `title`, `placeholder` or `alt`, and no RSC/flight payload, of a failure state carries a house word, a table name, a raw Postgres error or a stack | **NOT MEASURED, and the finish's report over-claimed it (review finding `d19-hunt-02`).** `capture.mjs:83` reads `main.innerText` and `document.body.innerText` only: `innerText` excludes every attribute AND excludes `<script>` content, which is exactly where Next's flight payload lives (`self.__next_f.push(…)`). Verified against the artefacts: `desk-roster-failed-1280.json` has `mainText.len=425`, `bodyText.len=1282`, and no attribute string in any of the fifteen JSON files. A failed-read state is precisely where a table name or a stack could ride into the SENT payload without being painted. ⚠️ `scan.mjs` also has no population floor and no planted in-page control (`d19-hunt-03`): it globs whatever `*.json` is in the directory, scans `j.bodyText ?? ""` so a capture that wrote nothing passes, and its only control is a sample-string test of the regexes. **What IS measured for the attribute half, on the DEFAULT state at all six widths:** `qa:house-bots-visual` §5.6 collects `aria-label`/`title`/`placeholder`/`alt` (15 attributes) and runs the shared 453 lexicon over them with a control that fires on its own samples — 66/0, row 14. And at SOURCE level for every state, `test:house-bot-console` 4.453 walks every string literal and JSX text of the section and of the gated reader with four planted controls. The served SENT-payload half is `qa:house-bot-console-probe`'s job (§2 row D), which reads raw response bodies in document, `RSC: 1` and router-state modes. |
| 24 | C7-1 (review pass) | `npm run test:responsive`, `test:admin-section-gate`, `qa:admin-view-matrix`, `qa:chaos`, `qa:dg-shell`, `qa:dg-measure`, `qa:admin-load` after this pass's `src/` changes | served | That the desk still drives clean inside the design-gate instruments after the five copy changes and the loader's new action ghost | Same cause as row 15, re-measured here rather than inherited: `npm run test:responsive` was ATTEMPTED at this pass and threw at `responsive-audit.mjs:431` — it needs a running app, and this session owns no port with a served build on it (row 21). `test:admin-section-gate`, `revoked-deadend` and `needle-rest` hard-code port 3009, a parallel session's. What IS measured over the same files, statically and green at `c812a03e`: `test:measure` (605 tsx files), `test:ui-consistency`, `test:chip-contract` 19/0, `test:section-rail`, `test:tap-target` 29/0, `test:labels` (1060 files, 3 locales), `test:admin-nav` 24/0, `test:rbac` 124/0. |
| 25 | C7-1 (review pass) | `npm run test:guards-exist` | — | That every suite key a ruling cites exists in `package.json` | NOT RUN, and nothing in this pass can move it: the pass adds NO suite key and cites no new suite. It changes `scripts/lib/house-bot-console-cases.mts`, `scripts/anchors/house-bot-console.anchors.mjs`, `scripts/qa-house-bots-visual.mjs` and three `src/` files, all behind keys that already exist. MEASURED independently by the review lens over `C7-SPEC.md`: all 51 cited suite keys resolve in `package.json`. PROGRESS records `test:guards-exist` 9/0 at `37cf48c3`. |

## 1c. Open — added by checkpoint C5-6 (R8 + R6, 2026-09-18)

| # | Checkpoint | Command | Store | What it would prove | Why the skip is safe |
|---|---|---|---|---|---|
| 26 | C5-6 | `npm run test:house-bot-designation` | both | That §8 — the two-store R6 proof `scripts/erasure.test.mts`'s header now CITES by name (PENDING through the DSAR queue, the refusal copy on the admin surface, the owner alert, the pseudonymised label/note/reasons, the admin-inbox redaction, the re-run reporting zero) — is still green at this build | NOT RUN. No file this checkpoint changed can reach it: `src/lib/server/erasure.ts`, `house-bot-dal.ts`, `designation.ts` and `scripts/lib/house-bot-designation-cases.mts` are all byte-identical to the checkpoint's base `5005c811` (`git diff --stat 5005c811..HEAD` names only `scripts/erasure.test.mts`, `scripts/anchors/erasure.anchors.mjs`, `scripts/lib/house-bot-engine-cases.mts`, the two reports files and the four R8 source files). What C5-6 added is a CITATION in a comment, not a call into that suite. ⚠️ A citation is not a measurement: the words were checked by reading `house-bot-designation-cases.mts:754-791`, never by running it here. |
| 27 | C5-6 | The mutation batch for C5-6's NEW R8 assertions — the ten mutations listed in this checkpoint's report (`report-pack.ts` ×5, `catalogue.ts` ×3, `pack-actions.ts` ×1, `report-pack-card.tsx` ×1), each against `npm run test:house-bot-reports` | both | That every new §0.214, §0.215 and §8 assertion can actually FAIL, and that each names the defect it claims | NOT RUN here by ruling 275: C5-8 runs ONE mutation batch for the whole commit, from a temporary worktree on its own scratch port. The three R6 mutations this checkpoint added are NOT in this row — they are declared anchors in `scripts/anchors/erasure.anchors.mjs` and were each RUN and SEEN RED in this checkpoint (8.b `notificationsAdmin`; 8.b `houseBots`/`houseBotEvents`/`houseBotPresses`/lowercased; §2b.1/2b.3/2b.4/2b.5). |
| 28 | C5-6 | `npm run test:all` here and in `F:/kipindi-old-build` at the merged `origin/main`, compared red by red | — | That C5-6 introduced no red clean `origin/main` does not already have | NOT RUN by ruling 275 (`test:all` red-by-red belongs to C5-8 alone). Every guard a changed file can reach WAS run here and matched the recorded inherited reds line for line: read-tiers 7.1, type-scale 909 vs 908, decomment 2.1, red-anchors 4 (rg-doors ×2, 4.1/4.2 66 vs 65), orphans 6 undeclared, failure-reasons 10.1/10.2. |

## 2. Owed once for the whole commit (C5-8's own gates, not skips)

These were never per-checkpoint runs; they are listed so nothing is lost between here and C5-8.

| # | Command | Store | What it proves |
|---|---|---|---|
| A | `npm run test:all` here, and in `C:/kipindi-old-build` at `origin/main`, compared **red by red** | — | No red on this branch that clean `origin/main` does not also have |
| B | A fresh `next build`, then `npm run verify:house-bot-bundle` | — | No house word, identifier, prop name or action name in the real public JavaScript (D19) |
| C | `npm run qa:house-bot-holder-view` | — | The holder's pages and RSC payloads carry nothing about house bots (D19c) |
| D | `npm run qa:house-bot-console-probe` | — | No console page, route handler or server action sends a house audit row to a non-staff session (rulings 259/260) |
| E | The ONE mutation batch, from a temporary worktree on its own scratch Postgres port | both | Every surviving assertion of Commit 5 can actually fail; every struck mutation listed as removed, never counted as run |
| F | Every `test:house-bot-*` suite on both stores | both | The whole commit green together, not suite by suite across sessions |

## 3. Cleared

| # | Checkpoint | Command | Result | When |
|---|---|---|---|---|
| 14 | C7-1 | `KP_BASE=http://127.0.0.1:3021 npm run qa:house-bots-visual` | **66 passed, 0 failed, 0 NOT MEASURED** — its FIRST EXECUTION EVER, off this run's fresh build, over `/admin/desk` at 360·640·768·1024·1280·1920 with a five-account roster carrying REAL money (the earlier by-hand pass read a roster of zeros, which cannot clip). All three controls fired at every width: §5.3 read four real deltas, §5.4 reached 6 controls at 360 and 49 at 1024+ against the `--tap-min` token read from the page (40px), §5.6 collected 15 attributes and its 453 scanner fires on its own samples. §5.1 no money figure clipped and none broken across two lines; §5.2 at 360 the subject column is 21→171 and the first money answer 171→338 inside a 21→339 strip, with the second answer at 338→505 one scroll away and `scrollable: true`. ⚠️ **A PASSING ASSERTION SET IS NOT A READ TILE**, and this row first offered the 66 passes as the reading — the substitution ruling 464's last sentence forbids. The review pass therefore OPENED AND READ all four middle-width tiles with the Read tool: `.qa-house-bots/admin-desk-640.png`, `-768.png`, `-1024.png`, `-1920.png`. Measured by eye on each: money whole and unclipped, the AUTO-PAUSED chip ONE line at 768/1024/1920, "Not set" on the null limit at 1920, `Up & Down · Polls` unbroken, the Products column sliced by the card edge at 1024 (inside the `ScrollX`, one scroll away, which §5.1 exempts by design), and — the finding that came OUT of the reading — the head's inert sentence carrying a live-looking "on Limits →" at every one of the four widths, which is review finding `visual-01` and is fixed at `c812a03e`. | 2026-09-18 |
