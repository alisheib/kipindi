[
 "READ ME FIRST · C5-7's mutation register (the absence sweep, written by the checkpoint that built it, 2026-09-20). Ruling 275 gives the BATCH to C5-8; NOTHING here was run as a batch by this checkpoint. TWELVE entries, NINE of them DECLARED in `scripts/anchors/house-bot-console.anchors.mjs` under the `reports-mem` suite key and are therefore audited every day by `test:red-anchors` §3 (each `from` must resolve exactly once against the working tree — measured 2026-09-20: 2431 passed, the 5 unchanged inherited lines) and by the `0.505b` expect-drift roll-call inside `test:house-bot-reports` itself (which caught TWO of this checkpoint's own label edits the same day; see the note at the end). The remaining THREE (S7-X01, S7-X02, S7-X03 — the third added by C5-7's REVIEW) have no red harness at all — `test:dsar-secrets` declares no anchors (C5-SPEC ruling 243: 'DSAR MISSED (no red harness)') — so they are written out longhand here as scratchpad mutations, with their exact from-text and the assertion each must redden. ⛔ THE REGISTER IS THE SECOND INSTRUMENT, NEVER THE FIRST: every absence assertion this checkpoint added carries a PLANTED CONTROL inside its own suite that runs on every run, and no clean verdict in this checkpoint was read before its control was seen to report. Conventions: the repo is CRLF and `resolveAnchor` normalises EOL, so a multi-line `from` written with \\n resolves — but a `.replace()` inside a SUITE does not, which is how two controls in this checkpoint planted nothing and had to be repaired (0.232.c4b and 11.247.c1c; both now assert the source CHANGED before reading a verdict). ⛔ **CORRECTED BY C5-7's REVIEW, 2026-09-20, and each correction was MEASURED by applying the mutation, never reasoned:** (a) **S7-M08 could not have reddened its declared assertion.** §11.247 serialised every reader's answer with `JSON.stringify`, and `traderSeedsByMarket` returns a `Map` — `JSON.stringify(new Map(...))` is the two characters `{}`. Three of the sweep's readers answer with a Map, so each contributed ZERO BYTES to the needle search for every viewer. Applied at `2026-09-20`: 11.247.2 stayed **GREEN**. With the sweep's own `sweepJson` in place it goes **RED** (`public/traderSeedsByMarket → id:hb_…, id:hbi_…, id:hb:hbi_…`), and `11.247.c5` now plants that same answer shape in-suite so the register is never the first instrument. (b) **S7-X01's declared red named one line too many** — see its own entry. (c) The `expect` of `247-seeds-raw` in `house-bot-console.anchors.mjs` was re-anchored when 11.247.2's label grew the target and press ids; `0.505b` reported the drift, which is the third time that roll-call has caught this checkpoint.",

 "S7-M01 · DECLARED as `L52-rule-word` · src/lib/server/rate-limit.ts · from `  \"desk.verify\":   { capacity: 3, refillPerMin: 0.2 },` → `  \"liquidity.verify\":   { capacity: 3, refillPerMin: 0.2 },` · test:house-bot-reports (memory child) · RED: 0.L52.1 — a rate-limit ACTION NAME carries a vocabulary word again, and `/admin/system` paints every live bucket's action half verbatim to the whole ops VIEW audience. The exact historical spelling is NOT used here: 0.L52.c4 plants it inside the suite, rebuilt from the shared sample by dropping its separator, because a word in an anchors file is still a word in the tree.",

 "S7-M02 · DECLARED as `L52-stale-caller` · src/lib/server/house-bot/designation.ts · from `  const rl = await rateCheckAsync(`${officerId}:${userId}`, \"desk.verify\");` → the same line with `\"desk.verify.v2\"` · test:house-bot-reports (memory child) · RED: 0.L52.3 — the caller names a rule `RATE_RULES` does not declare. ⛔ THIS IS THE FAIL-OPEN `tsc` CANNOT SEE: `RATE_RULES` is typed `Record<string, RateRule>`, so `keyof typeof RATE_RULES` is `string`, and `rateCheck` returns `{ allowed: true }` on an unknown action. `test:house-bot-designation` 1.8 would STILL PASS under this mutation, because an unlimited bucket never refuses — which is exactly why 0.L52.3 reads the call sites and not only the table.",

 "S7-M03 · DECLARED as `232-marker-wrong-object` · src/lib/server/market-service.ts · from the three-line window `      ...(p.houseBotId ? { houseBotId: p.houseBotId } : {}),` + `      amlReason: null,` + `      createdAt: p.settledAt, updatedAt: p.settledAt, completedAt: p.settledAt,` (the orphan-refund write at :2792; unique) → the same window with the marker reading `w` (the WALLET) instead of `p` · test:house-bot-reports (memory child) · RED: 0.232.1 — the marker is present, well-formed and read from the wrong object. A presence-only scan passes this; only the pairing of the marker's source with the row's own `positionId` reports it.",

 "S7-M04 · DECLARED as `232-dal-bypass` · src/lib/server/market-service.ts · from `    if (realRefund > 0) await db.txn.create({` → `    if (realRefund > 0) await prisma().transaction.create({ data: { id: \"x\" } });` ⏎ the same line · test:house-bot-reports (memory child) · RED: 0.232.3 — a service writes a Transaction row around the DAL, and therefore around every marker rule the DAL applies.",

 "S7-M05 · DECLARED as `235-houseonly-dropped` · src/lib/server/updown-digest.ts · from `      if (!channelAllowed(\"ROUND_RESULT\", { houseOnly: line.totals.ownRounds === 0 }).email) continue;` → `      if (!channelAllowed(\"ROUND_RESULT\", {}).email) continue;` · test:house-bot-reports (BOTH stores) · RED: 9.235.1 — the call still goes through F6's helper and still gates the email, without the one argument that makes it F6, so a holder whose Up & Down day was entirely house-marked is emailed a letter about stakes they did not choose. (Ruling 235 names this mutation by hand: 'the `houseOnly` argument dropped → red'.)",

 "S7-M06 · DECLARED as `235-no-caller` · src/lib/server/updown-digest.ts · from that same line PLUS its trailing newline → `` (the gate deleted) · test:house-bot-reports (memory child) · RED: 0.235.1 — `channelAllowed` goes back to having ZERO production callers, which is the state this checkpoint found the platform in: F6's rule written down, and never once run. Mutually exclusive with S7-M05: overlapping from-text.",

 "S7-M07 · DECLARED as `149-docstring-promise` · src/lib/server/comms-registry.ts · from ` * house-marked it never goes to a phone and never becomes a letter.` → the same line + ` — the holder's hourly summary is the one account of those stakes, and it is a bell.` · test:house-bot-reports (memory child) · RED: 0.235.3 — the docstring promises an hourly holder summary again: a surface owner ruling D19 does not allow and this programme never built. A docstring that describes a compensating surface is how the next session learns the wrong law.",

 "S7-M08 · DECLARED as `247-seeds-raw` · src/lib/server/market-service.ts · from `    if (arr.length < n && !arr.includes(p.userId)) arr.push(p.userId);` → `    if (arr.length < n && !arr.includes(p.userId)) arr.push(p as unknown as string);` · test:house-bot-reports (BOTH stores) · RED: 11.247.2 — the trader-seed reader answers with position ROWS instead of user ids, so the marker and the `hb:` bet key reach every viewer of a market. Nothing about the change looks wrong: it still answers 'who is on this market', it is still one aggregate, and the extra field is invisible until somebody serialises the answer to a signed-out visitor.",

 "S7-M09 · DECLARED as `247-round-item-marker` · src/lib/server/updown-board.ts · from the two-line window `      id: p.id,` + `      side: (p.side === \"YES\" ? \"UP\" : \"DOWN\") as \"UP\" | \"DOWN\",` (inside `myStakesByMarket`'s projection) → the same window with `      houseBotId: p.houseBotId,` inserted between them · test:house-bot-reports (memory child) · RED: 11.247.c1c — a holder's own round panel carries the marker. ⛔ THIS ONE MATTERS MOST OF THE NINE: no Up & Down fixture runs in `test:house-bot-reports`, so `getRoundDetail` is NOT swept (DEFERRED-TESTS row 76) and 11.247.c1c's source pin is the ONLY thing standing between that projection and a marker on a player's screen. A reader a sweep cannot reach is not a reason to leave the reader unguarded.",

 "S7-X01 · NO RED HARNESS (scratchpad mutation; `test:dsar-secrets` declares no anchors — C5-SPEC ruling 243) · src/lib/server/user-service.ts · from `    auditEntries: withoutHouseAuditKeys(await getAuditForActorDurable(userId, { limit: 1000, excludeActions: OWN_AUDIT_EXCLUDED_ACTIONS })),` → the same line without the `withoutHouseAuditKeys(` wrapper (and its closing paren) · npm run test:dsar-secrets (memory; it is in `predeploy`) · RED (⚠️ CORRECTED BY C5-7's REVIEW, and MEASURED on 2026-09-20 by applying it: 41 passed / 2 failed) · '⛔ D19 · …and that row carries NEITHER house key, at any depth' and the **player export** line of '⛔ D19 · HB-ACC-12 / CRA-04 · D20 · the holder's <door> carries no marker…' — **NOT the officer-bundle line**, which this register claimed and which cannot go red: `buildDsarBundle` returns no audit section at all (generatedAt, schemaVersion, user, wallet, transactions, kyc, responsibleGambling, notificationsCount, rights) and §6's holder has no transactions, so that door's JSON can never hold this needle. The limb stays as a FLOOR and now carries `6.CONTROL.bundle`, which attaches the real durable audit rows to the bundle document and REQUIRES the needle reported — so the absence is a measurement. The bundle's own stripper is S7-X03. What this mutation DOES prove, and it is the whole of §6's subject: the holder's own bet audit reaches their right-of-access export with `houseBotId` and `intentId` on it. §6 exists as a section separate from §9 precisely because the money rows and the audit rows are stripped by DIFFERENT code (`dsarTxnView`'s allowlist against `withoutHouseAuditKeys`), and one assertion over the whole document would pass while either of them was doing nothing.",

 "S7-X02 · NO RED HARNESS (scratchpad mutation) · src/lib/server/privacy.ts · from `    notificationsCount: notifications.length,` → `    notificationsCount: notifications.length,` ⏎ `    liquidityDecisions: { counteredPositionsCount: (await import(\"./house-bot-dal\")).houseBotIntentStore ? 1 : 0 },` · npm run test:dsar-secrets (memory) · RED: both '⛔ D19 · ruling 239 · CRA-10 · D20 · the trigger player's buildDsarBundle has the SAME key set at every depth as the twin's' and '…and the whole buildDsarBundle document is IDENTICAL once ids, times and contact details are normalised' — the struck trigger section returns to a releasable door. ⚠️ This mutation would be caught by the KEY-SET half even with a count of 0 on both accounts, which is the point of comparing paths as well as values: ruling 239's claim is that the two documents are indistinguishable, and an empty section is still a distinction. The in-suite control for the same shape is '7.CONTROL: the comparison FINDS the struck trigger section…' and its subtler twin, a bare COUNT with no section and no word.",

 "S7-X03 · NO RED HARNESS (scratchpad mutation; added by C5-7's REVIEW, 2026-09-20) · src/lib/server/privacy.ts · from `    transactions: txns.map(dsarTxnView),` → `    transactions: txns,` · npm run test:dsar-secrets (memory) · RED (MEASURED 2026-09-20 by applying it: 40 passed / 3 failed) · '⛔ the officer bundle carries the row but not the future column's value or name', '⛔ D19 · the officer bundle carries no house marker, key or vocabulary word at any depth' (both §9) and 'the player export and the officer bundle expose an IDENTICAL transaction field set' — the officer bundle's money rows reach a releasable door WHOLE, carrying `houseBotId` on Postgres and every future column. ⛔ WHY THIS ENTRY EXISTS: §6's two-door loop cannot be reddened on the bundle side by S7-X01 (the bundle has no audit section), so the bundle's stripping is proved by ITS own code path — `dsarTxnView`'s allowlist — and that is §9's subject, not §6's. Two doors, two strippers, two mutations; one mutation over one document would have left either of them doing nothing.",

 "⚠️ TWO LABEL EDITS OF THIS CHECKPOINT WERE CAUGHT BY THE ROLL-CALL, the same day, and the register records it because it is the only evidence that the roll-call is live: naming scenario ids into assertion labels (ruling 250) rotted `GATE-1`'s `expect` in `house-bot-money.anchors.mjs` (reported by `test:house-bot-seam` 8.505 as `stale`) and `247-seeds-raw`'s in `house-bot-console.anchors.mjs` (reported by `test:house-bot-reports` 0.505b). Both `expect` strings were corrected in the same pass. A declaration whose `expect` matches no label the suite prints can only ever report WRONG-ASSERTION — red for the wrong reason — which is the class ruling 505 exists to end."
]
