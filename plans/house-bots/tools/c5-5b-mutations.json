[
 "READ ME FIRST · C5-5b's mutation list (the D20 un-build, written by the checkpoint's fixer at 2026-09-18). Ruling 275 gives the BATCH to C5-8; nothing here was run as a batch. Conventions: every from-text is ONE LINE and occurs exactly ONCE at this checkpoint's head (verified by script); ⏎ in a replacement means a line break; the repo is CRLF, so a runner that inserts a line must write CRLF. Entries marked MEASURED were applied in the main worktree by the fixer, seen red, and the file restored with `git checkout --` plus `git diff --quiet` before anything else. Every mutation of a STRUCK ruling's code is gone from this list: the un-build's own guards are what is mutated here (C5-D20-REPLAN §3, C5-5b's exit).",
 "M1 · src/lib/server/market-service.ts · from `      payload: { reason, refundedCount, refundedTzs, grossPoolBefore: grossPool, title: m.titleEn },` → `      payload: { reason, refundedCount, refundedTzs, grossPoolBefore: grossPool, title: m.titleEn, houseStake: null },` · test:house-bot-reports (memory child; §0 runs memory-only) · RED: 0.187.1 [memory] — a new R9 audit site appears where D20 allows none.",
 "M2 · src/app/admin/resolver-queue/bulk-resolve-action.ts · from `        note: \"Bulk resolve from /admin/resolver-queue. Every market was sealed through resolveMarket — the same path, locks, ceremony, objection window and settle timer as the per-market control. No money moved in this action.\",` → the same line ⏎ `        houseStakes: {},` · test:house-bot-reports · RED: 0.187.1 [memory] — the bulk Batch row carries a house key again.",
 "M3 · src/lib/server/objections-service.ts · from `  payload: { objectionId, objectorId: o.userId, reason: o.reason, note: reviewNote, effect: \"verdict stands; settlement released\" },` (two-space indent — the rejectObjection audit; the four-space twin does not exist) → the same line with `, houseStake: null` added before the closing brace · test:house-bot-reports · RED: 0.187.1 [memory] — a SECOND struck R9 site, in another of the 21 decision files. (Replaces the implementer's M3, which planted a whole new file under src/app/admin/: review test-strength-11 showed that plant also reddens the CONTROL 0.187.c1, whose own plant list is derived from the real files on disk — an unexplained second red in a batch.)",
 "M4 · src/lib/server/objections-service.ts · from `  notifyObjectionDecided(o.userId, { upheld: false, marketId: o.marketId, note: reviewNote }).catch(() => {});` → `  const snap = await houseStakeForAudit(o.marketId);` ⏎ `  if (snap?.staffChosen.requestedBy.includes(officerId)) return { ok: false as const, error: \"You chose a stake on this market.\", code: \"CONFLICT\" as const };` ⏎ the same line · test:house-bot-reports · RED: 0.191.1 [memory] (ruling 270's I10 — a refusal branch in a decision service reads a requester) and 0.191.0 [memory] (a struck reader's name reappears in one of the 21 decision files).",
 "M5 · src/app/admin/resolver-queue/page.tsx · from `export default async function ResolverQueuePage({` → `export function Gate({ view, session, canResolve }: { view: { staffChosen: { requestedBy: string[] } }; session: { userId: string }; canResolve: boolean }) {` ⏎ `  const chosenByMe = view.staffChosen.requestedBy.includes(session.userId);` ⏎ `  return canResolve && !chosenByMe ? \"resolve\" : null;` ⏎ `}` ⏎ `export default async function ResolverQueuePage({` · test:house-bot-reports · RED: 0.191.2 [memory] (a decision page names requestedBy) and 0.191.4 [memory] (a page condition on house data decides a control).",
 "M6 · src/app/admin/audit/page.tsx · from `  const allEntries = await houseAuditForConsole(session?.userId ?? null, \"/admin/audit\", getAuditPage({ limit: 100_000 }));` → `  const allEntries = await houseStakeForConsole(session?.userId ?? null, \"/admin/audit\", getAuditPage({ limit: 100_000 }));` · test:house-bot-reports · RED: 0.260.1 [memory] twice over — the struck display reader's call count is no longer 0, and the audit read no longer goes through houseAuditForConsole. (Single-file replacement for the implementer's two-file M6: the pin reads source text, so the struck name need not exist for the count to move.)",
 "M7 · src/lib/server/notification-service.ts · from '    bodySw: `Soko limefutwa kwa dharura. ${formatTzs(opts.refundedTzs)} imerejeshwa.`,' → '    bodySw: `Soko limefutwa kwa dharura. ${formatTzs(opts.refundedTzs)} imerejeshwa, ikiwemo dau la nyumba.`,' · test:house-bot-reports · RED: 0.198.2 [memory] — the ADMIN twin beside the player's notice carries ruling 195's house clause again.",
 "M8 · src/lib/house-bot/rules.ts · from `} satisfies Record<string, FieldSpec>;` → `  gStaffEdgeWinRatePts: num(\"limits\", \"Oversight\", \"Staff edge: win rate\", \"pts\", 1, 100, { nullable: true, recommended: 60, hint: \"Planted by a mutation.\" }),` ⏎ `} satisfies Record<string, FieldSpec>;` · test:house-bot-rules · RED: 2.d20 (a FIELD_META row names a staff edge; the limits tab becomes 15, and the tab no longer equals LIMIT_FIELDS) — single store. Also expected red, and not a defect: 2.0's numeric-field count. 2.d20c stays GREEN (its arms are synthetic on purpose).",
 "M9 · src/lib/house-bot/rules.ts · from `export const NULLABLE_LIMIT_FIELDS = [` → the same line ⏎ `  \"gStaffEdgeNetTzs\",` · test:house-bot-rules · RED: 2.d20 (a nullable writable limit names a staff edge, and NULLABLE_LIMIT_FIELDS plus the counts no longer equal LIMIT_FIELDS) — single store. MEASURED by the fixer with the equivalent insertion at the list's tail: 514 passed / 2 failed, where the guard C5-5b shipped stayed GREEN (it read only FIELD_META and CLEAR_EXEMPT — review test-strength-02). 2.d20c stays GREEN.",
 "M10 · src/lib/server/house-bot-dal.ts · from `  gStaffChosenMaxCounterpartyShare: { col: \"gStaffChosenMaxCounterpartyShare\", kind: \"int\" },` → the same line ⏎ `  gStaffEdgeNetTzs: { col: \"gStaffEdgeNetTzs\", kind: \"bigint\" },` · test:house-bot-migrations · RED: the dal-cases `c11.d` on BOTH twins (the column map names a struck column again) plus a1.cols / a1.types / a2.cols / a2.types (map-versus-database parity) — Postgres and memory. MEASURED by the fixer: 650 passed / 5 failed. The version C5-5b shipped would have stayed GREEN on c11.d, because saveLimits refuses on the LIMIT_FIELDS whitelist, not on the column map (review test-strength-03).",
 "M11 · prisma/schema.prisma ONLY · from `  gStaffChosenMaxCounterpartyShare Int?` → the same line ⏎ `  gStaffEdgeNetTzs                 BigInt?` · test:dal-parity · RED: `6.schema · model HouseBotControl has no column StoredHouseBotControl lacks` — single store.",
 "M12 · prisma/migrations/20260916150000_house_bot_tables/migration.sql ONLY · from `    \"gStaffChosenMaxCounterpartyShare\" INTEGER,` → the same line ⏎ `    \"gStaffEdgeNetTzs\" BIGINT,` · test:house-bot-migrations · RED: the `.seed` case (the control row carries a staff-edge column again, measured on a real cluster) and a1.cols / a2.cols — Postgres.",
 "M13 · src/lib/server/house-bot-dal.ts · from `  saveLimits(baseVersion: number, patch: HouseBotLimitsPatch, tx?: HouseTx): Promise<CasResult<StoredHouseBotControl>>;` → the same line ⏎ `  recordDisclosure(sections: readonly string[], tx?: HouseTx): Promise<StoredHouseBotControl>;` · test:dal-parity · RED: 16.d20 (a struck step-3 member is declared again) — single store.",
 "M14 · src/lib/server/txn-filters.ts · from `  if (f.attentionOnly && attentionOf(t, nowMs)?.level !== \"warn\") return false;` → the same line ⏎ `  if (f.house === \"only\" && t.houseBotId == null) return false;` · test:dal-parity · RED: 16.d20.house (ruling 210's filter survives in the search grammar) — single store. Its control 16.d20.house.c1 stays green (added by the fixer — review test-strength-05).",
 "M15 · src/lib/server/txn-filters.ts · from `  if (f.attentionOnly && attentionOf(t, nowMs)?.level !== \"warn\") return false;` → the same line ⏎ `  if (t.houseBotId != null) return false;` · test:txn-search · RED: the D20 case `a house-marked row is an ordinary row…` — single store. MEASURED by the fixer: 50 passed / 1 failed, with the deciding anchors the review asked for (review test-strength-07); the equality-only case C5-5b shipped would also have caught THIS mutation but not a filter that refuses both rows.",
 "M16 · src/lib/server/market-dal.ts · from `      if (p.houseBotId == null) e.ownRounds += 1;` → `      e.ownRounds += 1;` · test:dal-parity · RED: 16.ownRounds (the memory twin) — single store; 16.ownRounds.c1 stays green.",
 "M17 · src/lib/server/market-dal.ts · from `              (count(*) filter (where p.\"houseBotId\" is null))::int                            as \"ownRounds\",` → `              count(*)::int                                                                   as \"ownRounds\",` · test:dal-parity · RED: 16.ownRounds (the Prisma twin) — single store.",
 "M18 · src/app/admin/audit/page.tsx · from `  const allEntries = await houseAuditForConsole(session?.userId ?? null, \"/admin/audit\", getAuditPage({ limit: 100_000 }));` → `  const allEntries = getAuditPage({ limit: 100_000 });` · test:house-bot-reports · RED: 0.260.1 [memory] (an audit reader no longer goes through the gate). The served half — qa:house-bot-console-probe 4.1 going red as the measured leak returns — belongs to C5-8's one built server (DEFERRED-TESTS §1 row 7). Mutually exclusive with M6: same from-text.",
 "M19 · src/lib/server/house-console-read.ts · from `  if (rows == null || (await houseConsoleAudience(viewerUserId, route))) return rows;` → `  if (rows == null) return rows;` ⏎ `  return rows;` · test:house-bot-reports · RED: 4.260.1, 4.260.2, 4.260.3, 4.260.4, 4.260.5 and 4.260.6 on BOTH stores. MEASURED by the fixer: memory 79 passed / 6 failed, Postgres 10 passed / 6 failed. ⛔ At the checkpoint's own head this mutation applied CLEANLY and the suite stayed GREEN — C5-5b had deleted every case that calls the gate (review conformance-01 / d19-hunt-01 / test-strength-01); the fixer restored §4, which is what makes this entry a proof instead of a hole.",
 "M20 · scripts/house-bot-reports.test.mts (the suite's own floor; run it last) · from `minPass: { memory: 85, postgres: 16 }` → `minPass: { memory: 86, postgres: 16 }` · test:house-bot-reports · RED: 0.mem — the floor is a real floor, so a section that stops running cannot pass unnoticed. (The floors rose from the measured 77/8 to the measured 85/16 when §4 came back.)",
 "M21 · src/lib/server/house-console-read.ts · from `    if (isOwnerOnlyPath(route)) return isAdmin(viewer.role);` → `    if (isOwnerOnlyPath(route)) return isStaffRole(viewer.role);` · test:house-bot-reports · RED: 4.259.2 [memory and postgres] (an Owner-only console path stops being the Owner's alone) and 4.260.2. MEASURED by the fixer on the memory child: 83 passed / 2 failed. NEW with the fixer's restoration of §4 — nothing at the checkpoint's head could see this.",
 "M22 · src/lib/server/store.ts · from `        const isStake = t.type === \"BET_PLACED\";` → `        if (t.houseBotId != null) continue;` ⏎ `        const isStake = t.type === \"BET_PLACED\";` · test:dal-parity · RED: 16.d20.aggregates (the memory twin of the concentration list excludes a house-marked row again — D20a) — single store; 16.d20.aggregates.c1 stays green. MEASURED by the fixer: 1347 passed / 1 failed. NEW with the fixer's guard (review test-strength-04): at the checkpoint's head no suite named these two aggregates.",
 "M23 · src/lib/server/market-dal.ts · from `      const e = acc.get(p.userId) ?? { resolved: 0, staked: 0, paidOut: 0 };` (the accumulator inside memoryPositions.leaderboard; unique) → `      if (opts?.excludeHouse && p.houseBotId != null) continue;` ⏎ the same line · test:dal-parity · RED: 16.d20.aggregates (the leaderboard excludes a house-marked row again, D20a) — single store; 16.d20.aggregates.c1 stays green.",
 "M24 · scripts/house-bot-disclosure.test.mts (a harness mutation: it proves the CONTROL can fail) · from `  if (spec.startsWith(\"@/\")) base = P.join(srcRoot, spec.slice(2));` → `  if (spec.startsWith(\"@/\")) return null;` · test:house-bot-disclosure · RED: 4.c7 (the planted client file no longer reaches the real house copy module over the real tree) and §2's alias control — single store. NEW with the fixer's re-anchor (review d19-hunt-04): C5-5b had deleted 4.c7, leaving every control on a virtual tree.",
 "M25 · scripts/house-bot-migrations.test.mts (a harness mutation) · from `    const ctl = (await c.query(`SELECT * FROM \"HouseBotControl\"`)).rows;` → `    const ctl = (await c.query(`SELECT \"id\" FROM \"HouseBotControl\"`)).rows;` · test:house-bot-migrations · RED: the `.seed` D20 case — Postgres. It proves the fixer's positive anchor: before it, an empty or projected row made the absence of the struck columns trivially true (review test-strength-06)."
]
