# (no brief is open)

Batch 6 — the phone filter sheet and the chart rail at 44px — **shipped 2026-08-15**. Its record
is `PLAN-OF-RECORD.md` §8.7i, and the batch log row is §6.

⛔ **This file is deliberately empty.** A spent brief that still says *"paste this whole file as
your opening prompt"* sends the next session to redo finished work — which is the reason the
batch-6 definition of done ends with "then EMPTY this file".

**When Ali commissions the next batch**, write it here, from `PLAN-OF-RECORD.md` §8.8 (the
deferred register, each item with its reason) and §8.8's "ALI'S RULINGS" table.

⛔ Six things are ruled OUT, not merely deferred — do not propose them again: the compact list /
density toggle · search typeahead · admin filter rails · `/wallet`'s section tabs · the `/markets`
`aria-pressed` wording · the `rounded-pill` → `rounded-chip` sweep.
