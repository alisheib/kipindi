# SESSION PROMPT — THE RED FLEET · ⚪ FOLDED IN, 2026-08-15

> ⛔ **DO NOT PASTE THIS.** Its work was folded into
> [`SESSION-PROMPT-FINISH-LINE.md`](SESSION-PROMPT-FINISH-LINE.md) as **units A–E**, so that one
> session closes every open item at once (Ali's call: *"meet all the remaining issues, all at
> once, in one session"*). Two live prompts for one body of work is how a session does it twice.

**What it commissioned, and where it now lives:** the RED guard fleet — `red:all` is a
41-segment `&&` chain that exits 1 early, so roughly thirty harnesses after the break run
**never**; `red:updown-readiness` has five stale anchors; and a static sweep's wider count is a
lead, not a measurement. All of it is `FINISH-LINE` §3 units **A–E**.

**Why it mattered, kept here because the causal chain is the argument:** a RED harness printed
*"tree restored"* while leaving two mutations on disk (hard-coded restore list). One — a bare
`if (true)` — was swept into `76efe614` and deployed, so for two hours every hedging player read
a false statement about a bonus they did not hold. Fixed at `690aa237`; recorded at
[`FAILURE-INVENTORY.md`](FAILURE-INVENTORY.md) §3.8 and §3.9.
