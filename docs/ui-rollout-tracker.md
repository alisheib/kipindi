# UI Rollout Tracker — Final UI Enhancement Kit

Single source of rollout truth for porting Claude Design's Final UI enhancement Kit
(`Final UI enhancement Kit/50pick-design-final/`) into the live app. Work strictly
against this, one item at a time. Update the row **after every item**. If context
resets, this file says exactly where to resume.

> **Mindset (Ali, every item):** hold FIVE lenses at once — **integration engineer**
> (end-to-end connection, i18n/money-path, reuse primitives), **UI/UX engineer**
> (pixel polish, 360→1920 responsiveness, a11y, motion + reduced-motion; READ the
> shots), **software architect** (single source of truth, no divergence, delete the
> superseded), **manager** (scope/risk, ship-vs-defer, flag to Ali), and **player**
> (clear, trustworthy, fast, RG-safe, correct in EN/SW/ZH). Resolve or record every
> concern a lens raises — never ship past it silently. Full note in `docs/ui-rollout-next-session.md`.

Status legend: `[ ]` not started · `~` in flight · `[x]` done · `⊘` blocked/sourced-externally

Regression gate: a batch is not "done" until the full suite (tsc + money/i18n tests +
smoke scripts + `scripts/ui-regression.mjs` at 360/768/1280/1920) passes and is logged.

---

## Foundations (step 1)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| F0 · Merge `glyphs-additions.tsx` into `glyphs.tsx` (spread `...Iplus`; percent/activity redraws win; remove interim controlled-poll block) | [x] | 279d0a4 | tsc PASS; screens render | +G64 (2.2) wrapper; Ibase+Iplus spread; removed 12 interim controlled-poll glyphs. `bank` added (payment tile); existing `landmark` kept |
| F1 · Wire `state-tokens.css` + `micro-patterns.css` after `globals.css` | [x] | 279d0a4 | render OK all 4 widths | copied to `src/app/`, imported in `layout.tsx`; classes available, applied by later A/B items |
| F2 · `scripts/ui-regression.mjs` (Playwright 360/768/1280/1920; no h-overflow, zero console errors, key interactions, screenshot+read) | [x] | 279d0a4 | 158/158 pass, 52 shots | domcontentloaded (board polls → never networkidle); ONLY/WIDTHS env filters |
| F3 · Foundations regression green (tsc + money/i18n + smoke + ui-regression) | [x] | 279d0a4 | see batch log | all green; switcher-test needs fresh store (documented state-leak) |

---

## PART A — Cross-cutting (do first, in order)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| A1 · MarketCard v2 — spark + trader crests, `@` labels, demote MoveChip | [x] | 8cfa885 | /live full grid + /markets EN 1280 + SW 360 (NDIO/HAPANA fit); ui-reg 158/158 | spark = real YES% history, smooth cubic, aqua, draws in (pathLength=1); crest = 3 av-stack + count (falls back to meta count when no traders); YES/NO localized (NDIO/是); move → mono micro-text above bar. All 4 pages (/ /markets /live /results) already plumbed the data. Added `seed-markets` dev endpoint |
| A2 · `AuthShell` + brand side-rail (6 `/auth/*` routes) | [x] | 91831f7 | a2-login lg (rail) + 390 (rail hidden) + SW (tagline+NDIO/HAPANA); ui-reg 158/158 | new `auth-shell.tsx`; adopted by login/register/otp/forgot/reset/verify (admin auth untouched). Rail: royal `--bg-overlay`, BrandTopo 0.09, localized tagline (`railTagline` key ×3), YES 64% TippingBar, trust strip. No gold in rail. ⚠ pre-existing gold `SubmitButton` CTA on auth → flag for B4 gold-discipline |
| A3 · Shared `RouteError` (root + 6 boundaries) | [x] | b679181 | a3-routeerror EN 1280 + SW 390 (triggered real boundary); ui-reg 158/158 | new `route-error.tsx`; all 8 boundaries (root/admin/auth/markets/positions/profile/proposals/wallet) now thin wrappers. FiftyMark 64 + BrandTopo 0.06 frame, rose alert tint only (no claret), digest ref, Try again + back link(s). global-error.tsx left as-is (self-contained). Follow-up: not-found pegged-bar frame still TODO |
| A4 · `PageHero` on 5 bare routes | [x] | c6f94ad | a4-proposals/fairness/proposals-new 1280; ui-reg 158/158 | Added masthead to the 3 genuinely-bare routes: /proposals (gold+trophy), /proposals/new (gold+trophy), /fairness (info+shieldcheck). **Deferred (documented):** /proposals/[id] — detail page, a "Propose & earn" masthead is contextually wrong; reward framing comes via A5 burst. /profile/invite — already has a gold EarningsRing hero (adding PageHero = redundant/over-gold); reworked in A9 |
| A5 · Shared reward-burst (`reward-burst.tsx`) | [x] | 1a8561e | a5 test-route 1280+390 (4 configs: trophy+amount, shieldcheck, static end-frame, size-64 crest — rays centred, corner brackets, gold-mono amount); ui-reg 158/158 | new `components/brand/reward-burst.tsx` (presentational, server+client). 12 gilt rays via kit `reward-burst__rays`/`__medal` classes; **rays centre with negative margins, NOT translate** (the scale() entrance clobbers a centering translate — was the one bug found + fixed in review). Wired: /proposals/[id] approval-bonus (trophy+amount end-frame), /profile/kyc APPROVED (shieldcheck "ID verified" — split the combined submitted card so PENDING keeps its clock), OperationResultModal `celebrate`+`celebrateGlyph` opts (earned-money success only → ready for B7/payout). No new i18n keys (reused common.approved/earnedAPrize/creditedToBonusWallet, profile.idVerified/kycApprovedBody) → parity 1203³. **Deferred:** win-celebration.tsx pairing waits on `win-seal.png` (⊘ bitmap). Gold legit on all (earned peak) |
| A6 · MNO/payment tile system | [x] | 1c2803f | a6 deposit 1280+390 (M-Pesa selected: royal ring + check pip; Card glyph), withdraw (Bank glyph), Methods tab, disabled state (HaloPesa 40% + "Temporarily unavailable"); ui-reg 158/158 | new shared `components/wallet/payment-logo.tsx` (48 slot; Card/Bank = in-house cardPay/bank glyphs; 4 MNOs = hued-initials placeholder, `MNO_LOGOS` map = 1-line drop-in when Ali delivers ⊘ official SVGs). `ProviderRadioGrid` → tile system: selected = **royal `--brand-500`** ring + pip (NOT gold — clears a gold-discipline leak on deposit), disabled = 40% + `temporarilyUnavailable` (new i18n ×3 → 1204³), 120ms scale-in / reduced-motion instant, pure CSS `group-has-[:checked]` (stays server component → deposit+withdraw both upgrade). Methods tab adopts same PaymentLogo. Deposit gains a **labeled** regulator-seal trust-strip slot (never fabricated — seal is ⊘ licensed). **Still ⊘ (Ali):** the 4 official MNO marks + regulator seal |
| A7 · Category-art layer (chips/watermark/home row) | [x] | ccd1db6 | a7 home topic-row 1280+390 (8 tiles, royal glyphs), markets topic chips (glyph per chip, All=grid selected royal), /markets/[id] crypto watermark 7%; ui-reg 158/158 | reused shared `categoryGlyph()`. (1) 14px glyph in TOPIC chips on /markets + /results (All=`layoutGrid`; active→brand-300; px-3.5→px-3+gap so SW-long fits); WHEN filters untouched. (2) 96px 7% watermark behind /markets/[id] question (header `relative isolate`, `-z-10`). (3) home "Browse by topic" row = 8 tiles into filtered board, royal-tint glyphs (not gold). New i18n `browseByTopic` ×3 → 1205³. Bumped opacity 6→7% for visibility on navy. **Still ⊘:** `public/category/*.webp` section-topper bitmaps (not needed for chip/watermark tier) |
| A8 · Admin primitives (AdminKpi spark, AdminBarList, AdminMeter, AdminFunnelChart) | ~ | 4753241 | a8 cohorts 1280 (meters brand fill, bar lists proportional stacked, no gold) + overview (live pulse→aqua, deltas→brand); ui-reg 158/158 fresh store, admin-smoke 125/125 | **Primitives DONE** in `admin-charts.tsx`: `AdminMeter` (value-vs-cap, brand→no-500 past threshold, tick), `AdminBarList` (stacked label+value over full-width bar — the single-line label/bar/value starved the track to ~1–17px in narrow 3-up admin cards; stacked fixes it), `AdminSpark` (renders only ≥2 pts, never fabricated). `AdminFunnelChart` already existed. `AdminKpi` gained `series` → renders spark; **shared gold-discipline fixes** (all screens): live pulse gold→aqua, delta-up gold→brand, funnel conversion gold→brand. New CSS `admin-bar-grow` (240ms, RM-static). **Adopted:** players/cohorts (3 distributions→AdminBarList, health meters, spark) + fixed a dev-only crash (`db.user.list().catch` on sync array → `Promise.resolve`). **REMAINING (follow-up):** adopt on the other ~12 screens (ai-usage/players/moderation/ai-polls/candidates/config/privacy/invites/audit/players[id]/live/finance) + purge remaining admin gold (GGR/NGR KPI `gold`/`tone="gold"` values on overview/finance, admin-shell `AdminFunnel` conversion) |
| A9 · Invite share-card + QR + wallet balance spark | [x] | c592bc7 | a9-invite (royal share-card: FiftyMark, "You've been invited", code DEMOAEMG in GiltCorner, QR) + a9-wallet-spark (aqua "AVAILABLE · 30 days" line + terminal pip above activity, driven via real deposits); ui-reg 158/158 fresh | **Invite:** server-generated QR (`qrcode` lib, royal modules on white) of the referral link + share-card block showing the CODE (never a balance); gold principled (referrer earns). Existing WhatsApp/SMS/copy share kept. **Wallet:** `balance30d()` reconstructs 30-day end-of-day balance from signed txns vs current balance → `BalanceSpark` (aqua 1.5px + terminal pip, hidden <2 pts so empty wallet stays clean); aqua not gold (balance=state). New i18n `youveBeenInvited`+`days30` ×3 → 1207³. **Deferred:** the 1080² Satori PNG for `navigator.share` files + `public/banners/invite.webp` texture (⊘ bitmap) — on-page share-card + link share cover the feature |
| A10 · Leaderboard podium | [x] | 15f600e | a10-leaderboard 1280+390 (podium: #1 asha gilt ring+crown+glow+rank badge raised, #2/#3 muted 48px, hot-flame streak chips; table streak cells → flame chips); ui-reg 158/158 | Top-3 `Podium` above the table (order [#2,#1,#3]); #1 = `--gold-400` ring + `crown` + rank badge, #2/#3 = `--text-muted` rings. `kp-rise` staggered 60ms + `podium-crown` 6° overshoot keyframe (both RM-static). Gold on #1 principled (earned standing). `HotChip` (`I.hot` flame + win count) in podium + table streak cell. **Canonical `TierBadge` was already in use** (KitTierBadge from avatar; sovereign=★ already fixes the S-collision) — no local re-impl to kill. Real players from row 1 already (synthetic only when empty). No new i18n |

---

## PART B — Refine-existing (keep/upgrade)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| B1 · ConvictionDial — thumb grab-pip + focus ring + coach hint; widen NDIO/HAPANA box ~24%; RG detent @10× + deliberate 2nd gesture past 50× | [x] | c2f305d | b1 en-yes 360/768/1280 (coach pill + grip dots + tachymeter), en-focus 1280 (brand-400 knob focus ring), sw-no 360 (YES ndio / NO hapana fit, "BURUTA KUWEKA DAU"), reduced-motion 1280; ui-reg 158/158 fresh | Grab-pip = 3 grip dots on knob + `.dial-focus-ring` (2px `--brand-400`, `:focus-visible` only, replaces the rect ring). Coach = one-time localStorage `50pick-dial-coached`, dismissed on drag/key/focus (nudge RM-static). RG: module-scope detents at 1×/2×/5×/10× (4px window, `haptics.select()` edge-triggered) + 50× gate (`gestureBeyond50Ref`; single drag clamps to 25,000 @ base500, 2nd drag from beyond is exempt) — **verified numerically: fresh single drag→25,000, 2nd→94,100, detent→exactly 5,000**. Keyboard rebuilt: ←→ = one detent on `POS_LADDER` [1,2,5,10,20,50,100,200], PgUp/PgDn ±10×, Home/End = extremes. aria-valuetext → "TZS {stake} on {side/NDIO/是}" (new `onSide` key). Reduced-motion: `useRollingNumber` jumps (motionReduced). Poles localized+widened (free-mode header; dial runs **locked-only** live via SidePicker so poles verified via locked pills). New i18n `dragToSetStake`+`onSide` → **1209³** |
| B2 · MarketCard MoveChip → mono micro-text (folds into A1) | [x] | 8cfa885 | /live grid shows mono move-line | done with A1: removed the chip + old `.mcard-move*` CSS; now right-aligned mono `↗ +Npt` above the bar |
| B3 · Empty states — redraw `emptyMarkets` (scales+YES/NO pips), widen box 360px, drop 52px ring; add 5 new | [x] | 0f7daf2 | b3 preview (all 12 kinds render), proposals empty 360/1280 (ballot+quill), admin/invites empty (clipboard+lens); ui-reg 158/158 fresh | Box/ring were **already** 360px + no-ring-badge (earlier pass) — this was the ART. Redrew `markets` scales to weigh a YES/NO pip pair (filled YES / hollow NO, colour-free). Added 5 kinds in the **existing 56-grid one-gold-accent idiom** (NOT the plain 64-grid glyph-kit `emptyProposals/kycRail/fairnessChain/rgSelfCare/adminGeneric` — those are **unused** kit specimens; component art is the single source): proposals (ballot+quill), kyc (ID+progress rail), fairness (commit→reveal→attest chain, gilt attest node), rg (calm sunrise, no gambling imagery), admin (clipboard+lens). Wired /proposals both empties markets→proposals; admin default empties (affiliate/ai-usage/bonuses/invites/invites[id])→admin. kyc/fairness/rg kinds now **available for PART C** (C1b/C1d/C2h). No new i18n (1209³) |
| B4 · Buttons/chips — systematize states (Part D recipe); per-size padding 12/16/20/24, `--r-md`/`--r-lg`; 0.7s spinner; drop leading glyph on SW gold CTA | [x] | 227d56d | b4 login EN+SW 360 (royal "Ingia", fits no-wrap), deposit (computed btn-gold), kyc (btn-primary); ui-reg 158/158 fresh | **Recipe was already in place** (globals.css `.btn`: per-size padding 12/16/20/24, `--r-md`/xl `--r-lg`, `white-space:nowrap`, hover/focus-ring/active-scale/disabled-45%, 0.7s spinner in Button+Spinner; Chip has full status palette + brand-400 selected ring). Net-new = **gold-discipline** (§1 gold = money-in/earned only): flipped `SubmitButton` default gold→**primary** (root cause — auth×6/kyc×2/rg/sof/withdraw all relied on the gold default → now royal), deposit opts back into `variant="gold"` (only money-in submit). SW rule satisfied by `nowrap` + no 360 overflow (no gold CTA has a leading glyph that overflows in SW). **Flag to Ali:** global header "Sign up / Jisajili" is still gold (site-wide acquisition CTA — product call). Admin gold left to A8-breadth. No i18n change (1209³) |
| B5 · PageHero — add `glow=aqua` for `/live`; BrandTopo 0.09 | [x] | 83e5108 | b5 live 360/1280 (topo depth, cards crisp); ui-reg 158/158 fresh | Added 5th PageHero glow `aqua` (hue 195 = LIVE accent). /live has **no PageHero** (deliberate slim header — full aqua hero is C1e), so applied the two safe parts: aqua variant is ready for C1e, and /live `BrandTopo 0.04→0.09` (verified subtle behind the dense card wall). No i18n change (1209³) |
| B6 · Wallet cards / profile hero / EarningsRing — KEEP; add balance spark (A9) + real Methods/Limits data | [x] | 2de92d9 | b6 wallet Limits tab 1280 (caps from validators); ui-reg 158/158 fresh | Balance spark done in A9. **Limits were hardcoded string literals** that matched the server but could drift → made money caps a **single source of truth**: `validators.ts` exports `DEPOSIT_MIN/MAX_TZS`+`WITHDRAW_MIN/MAX_TZS` (used in the zod schemas), threaded via `wallet/page.tsx` `limits` prop → Limits tab derives "TZS 500–2,000,000"/"TZS 1,000–5,000,000"; deposit/withdraw input bounds read the same consts. Methods tab = real supported MNOs (fixed list, not mock; per-MNO kill-switch availability = ADM4). test:wallet(8)+numeric(26) green. No i18n change (1209³) |
| B7 · Modals — OperationResultModal success → reward-burst end-frame; bet-confirm pool-share sentence (EN/SW/ZH) | [x] | 34687b4 | b7 betconfirm EN+SW 420 (side-aware "NDIO ikishinda, unagawana mfuko"), win crest 420 (RewardBurst rays+medallion); ui-reg 158/158 fresh | **reward-burst end-frame:** the earned WIN moment (`win-celebration.tsx`) now uses the shared A5 `RewardBurst` (12 gilt rays + trophy medallion) — unifies with proposal-approved/KYC-verified; completes the A5-deferred win pairing (RewardBurst is pure SVG → the ⊘ win-seal.png was never needed); removed dead wc-glow/wc-pop. Celebrate discipline held: WIN only (deposit/bet-placed/cash-out never celebrate, §12) — no non-earned OperationResultModal was turned into a celebration. **pool-share invariant (§10.2):** bet-confirm payout box now shows the side-aware `poolShareIfWins` prominently ("If {NDIO/HAPANA/是/否} wins, you share the pool"). New key ×3 → **1210³** |
| B8 · Admin KPI/tables/charts — upgrade via A8 | [ ] | | | |
| B9 · Glyphs — redraw 3 (percent/activity/landmark), add 12 controlled-poll (done in F0) | ~ | 279d0a4 | via F0 | percent+activity redraws + 12 controlled-poll DONE; `landmark` optical redraw still pending (Iplus adds `bank`; existing `landmark` unchanged) |
| B10 · Hero image — REPLACE F1 shot; delete 20 orphan slides + 2 orphan hero components | [ ] | ⊘ bitmap | | webp sourced externally; component deletion here |
| B11 · BrandTopo — 0.05 → 0.09 everywhere | [ ] | | | |
| B12 · PulseRing / SignalPip / GiltCorner — KEEP as-is | [x] | | | no-op by design |

---

## PART C — Per-page

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| C1a · `/markets/[id]` hero — category watermark (A7) + gilt hairline; states open/closing/waiting(hourglassOff)/resolved | [ ] | | | |
| C1b · `/profile/kyc` — progress-rail line-art, ID silhouettes, APPROVED reward-burst, canonical `<Input>` | [ ] | | | KYC gates withdraw — clarity first |
| C1c · `/profile/sessions` — device cards (glyph, geo, mono time, this-device aqua rule), claret revoke, IP reveal-on-tap | [ ] | | | replace debug dump |
| C1d · `/fairness` — PageHero glow=info + 5-step provably-fair SVG chain (gilt on attest node) | [ ] | | | step labels in HTML not SVG |
| C1e · `/live` — PageHero glow=aqua; hero = most-contested (`abs(yesPct-50)`); dense TippingBar-wall card | [ ] | | | fixes "soonest-closing" label lie |
| C2a · `/` home — category row (A7) + animated stats band (real aggregates, count-up-flash) | [ ] | | | |
| C2b · `/results` — aggregate YES/NO donut + one featured result card; keep header lean | [ ] | | | |
| C2c · `/positions` — mini countdown-ring + YES/NO exposure bar | [ ] | | | |
| C2d · `/positions/performance` — streak pip-chain + best-win gilt crest | [ ] | | | gold right here |
| C2e · `/wallet/withdraw` — KYC-lock line-art, merge 3 notices, route amount/balance through kit controls | [ ] | | | money-path audit flags |
| C2f · `/proposals*` — PageHero (A4), proposals EmptyState, propose banner, category tint | [ ] | | | |
| C2g · `/profile`, `/account`, `/source-of-funds` — GiltCorner only if reward framing; per-source glyphs; signature line-art; warning-topo on close-account | [ ] | | | SoF = royal not gold |
| C2h · `/profile/responsible-gambling` — RG self-care line-art, yes-toned helpline callout | [ ] | | | no gambling imagery |
| C2i · `/help` — per-FAQ topic glyphs, tone-coded quick-link chips | [ ] | | | |
| C2j · `/offline` route — FiftyMark + retry + precache | [ ] | | | |
| C2k · OG — extend dynamic OG to leaderboard/results/proposals/profile; gradient align; render png masters | [ ] | ⊘ font | | rasterize where Sora/Inter/JBM installed |
| C2l · `manifest.json` — shortcuts (Markets/Wallet/Deposit) + narrow screenshot label | [ ] | | | |
| C2m · `legal/*` — LegalHeader GiltCorner + per-doc glyph | [ ] | | | |

---

## Admin reporting surface (Batch 3 spec)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| ADM1 · Reporting console — GGR/NGR defs, daily P&L w/ totals, category breakdown, regulator pack maker-checker chain | [ ] | | | per `50pick-admin-reporting-spec.md` |
| ADM2 · Two-officer Resolution Ceremony | [ ] | | | |
| ADM3 · KYC/AML workstation | [ ] | | | |
| ADM4 · Payments ops — MNO kill-switches, reconciliation, retry queue | [ ] | | | |

---

## Assets (PART E) — produce or source

| Asset | Path | Status | Notes |
|---|---|---|---|
| DROP-IN svg/glyphs ×59 | `public`/in-code | [ ] | currentColor; wired per usage |
| DROP-IN empty-states ×5 | in-code | [ ] | via F0 |
| DROP-IN badges ×12 + gilt | `svg/badges*` | [ ] | map tiers I–V to TierBadge; locked = 30% ink + lock |
| DROP-IN pay/card.svg + bank.svg | `public/pay/` | [ ] | delivered |
| DROP-IN comms (email+SMS/WA) | `comms/` | [ ] | server-side tokens |
| Hero bg webp | `public/hero/hero-bg.webp` | ⊘ | bitmap pipeline (Ali) |
| Propose/Bonus/Invite banners | `public/banners/*.webp` | ⊘ | bitmap pipeline |
| Category art ×7 | `public/category/*.webp` | ⊘ | bitmap pipeline |
| Section texture | `public/texture/navy-weave.png` | ⊘ | bitmap pipeline |
| Win seal png | `public/celebrate/win-seal.png` | ⊘ | bitmap pipeline |
| MNO logos ×4 | `public/pay/{mpesa,airtel,halopesa,mixx}.svg` | ⊘ | source official (Ali) |
| OG masters png | `public/og/*.png` | ⊘ | rasterize where brand fonts installed |
| Regulator seal | deposit trust strip | ⊘ | never fabricate; labeled slot until supplied |

---

## Open items to flag to Ali
- ~~Header "Sign up / Jisajili" CTA is gold~~ — **RESOLVED (`3556845`)**: Ali confirmed "no one is above the law" → sign-up CTAs (header + market-detail guest) are now royal. Gold remains money-in/earned only.
- MNO brand logos (M-Pesa, Airtel Money, HaloPesa, Mixx by Yas) — trademarked, source official.
- Bitmap tier (hero, banners, category art, texture, win-seal) — needs image pipeline.
- Regulator seal — licensed asset.
- SMS sender ID (`"KIPINDI"`) and real deploy domain — env-driven.

---

## Batch log
_(append one entry per batch: date · items · regression result)_

- **2026-07-09 · B7 modals — win reward-burst + pool-share invariant (`34687b4`)** — win-celebration now uses the shared A5 `RewardBurst` crest (12 gilt rays + trophy medallion), unifying the earned-win moment with proposal-approved/KYC-verified and completing the A5-deferred win pairing (pure SVG, no bitmap needed). Bet-confirm sheet now shows the side-aware §10.2 pool-share invariant ("If YES wins, you share the pool / NDIO ikishinda... / 若"是"获胜...") prominently. Celebrate discipline held — WIN only. New key `poolShareIfWins` → parity **1210³**. **Regression:** tsc PASS · test:i18n (1210³) · **ui-regression 158/158 (fresh)** · drove bet-confirm EN+SW + win crest, zero console errors. **→ next unchecked = B8 (admin KPI/tables/charts upgrade via A8) — note this overlaps the A8-breadth carry-over.**
- **2026-07-09 · B6 wallet real Methods/Limits (`2de92d9`)** — money caps are now a single source of truth: `validators.ts` consts (`DEPOSIT/WITHDRAW_MIN/MAX_TZS`) drive both the zod enforcement and the wallet Limits-tab display (threaded via a `limits` prop) + the deposit/withdraw input bounds, so the shown caps can't drift from what the server enforces (the "mock = launch blocker" risk). Methods = real supported MNOs; A9 spark already covered the balance-spark half. **Regression:** tsc PASS · test:wallet(8) · test:numeric(26) · **ui-regression 158/158 (fresh)** · drove /wallet Limits tab, zero console errors. No i18n change (**1209³**). **→ next unchecked = B7 (OperationResultModal success → A5 celebrate on payout/earned only + bet-confirm pool-share sentence).**
- **2026-07-09 · B5 PageHero aqua glow + /live topo (`83e5108`)** — added a 5th PageHero `glow=aqua` (hue 195, LIVE accent) ready for C1e; bumped /live `BrandTopo` 0.04→0.09 (subtle depth, cards stay crisp). /live keeps its slim header (full aqua PageHero = C1e). **Regression:** tsc PASS · **ui-regression 158/158 (fresh)** · /live 360+1280 read, zero console errors. No i18n change (**1209³**). **→ next unchecked = B6 (wallet real Methods/Limits data — mock = launch blocker).**
- **2026-07-09 · B4 buttons/chips gold-discipline (`227d56d`)** — the button/chip state recipe (per-size padding 12/16/20/24, `--r-md`/`--r-lg`, 0.7s spinner, hover/focus-ring/active/disabled/loading, nowrap; Chip status palette + brand-400 selected ring) was already implemented, so B4 = the flagged gold leak: `SubmitButton` default gold→primary (fixes auth/kyc/rg/sof/withdraw at the root), deposit re-opts into gold (sole money-in submit). SW no-wrap satisfied. **Flag to Ali:** header "Sign up/Jisajili" still gold (site-wide growth CTA). **Regression:** tsc PASS · **ui-regression 158/158 (fresh)** · live-drove login EN+SW 360 (royal, fits), deposit (btn-gold), kyc (btn-primary), zero console errors. No i18n change (**1209³**). **→ next unchecked = B5 (PageHero glow=aqua for /live + BrandTopo 0.09).**
- **2026-07-09 · B3 empty states (`0f7daf2`)** — redrew the `markets` empty illustration (scales weighing a colour-free YES/NO pip pair) and added 5 kinds to the `EmptyState` system in its existing 56-grid one-gold-accent idiom: proposals (ballot+quill), kyc (ID+progress rail), fairness (attest chain), rg (sunrise), admin (clipboard+lens). Wired /proposals→proposals and admin default empties→admin; kyc/fairness/rg reserved for PART C. Box/ring already kit-correct (360px, no badge) from an earlier pass. The 64-grid glyph-kit empty-state entries are unused specimens — component art stays the single source. No new i18n (**1209³**). **Regression:** tsc PASS · test:i18n (1209³) · **ui-regression 158/158 (fresh store)** · live-drove a temp preview of all 12 kinds + real /proposals (360/1280) + /admin/invites empty — render + contextually correct, zero console errors. **→ next unchecked = B4 (button/chip state recipe, incl. the flagged gold SubmitButton CTA leaking onto auth).**
- **2026-07-09 · B1 ConvictionDial refinements (`c2f305d`)** — grab-pip (grip dots + 2px `--brand-400` `:focus-visible` ring on the knob), one-time coach hint (`dragToSetStake`, localStorage-dismissed on first drag/key/focus), RG mitigations for the 200× range (soft magnetic detents 1×/2×/5×/10× with 4px window + edge-triggered `haptics.select()`; single-gesture 50× gate needing a deliberate 2nd drag), keyboard rebuilt to detent-ladder stepping (←→ one detent, PgUp/PgDn ±10×, Home/End extremes) with spec aria-valuetext "TZS X on YES/NDIO/是", localized+widened poles, and reduced-motion needle-jump. New keys `dragToSetStake`+`onSide` → parity **1209³**. Dial runs **locked-only** live (SidePicker gates side first) so all visuals verified in locked mode. **Regression:** tsc PASS · test:i18n (1209³) · test:trilingual (36) · **ui-regression 158/158 (fresh store)** · live-drove /markets/[id] EN+SW 360/768/1280 — **detent snaps to exactly 5,000 (10×), fresh single drag gates to 25,000 (50×) then 2nd drag reaches 94,100**, focus ring + coach + NDIO/HAPANA fit confirmed, zero console errors. **→ PART B underway (B1 done); next unchecked = B3 empty states.**
- **2026-07-09 · A10 leaderboard podium (`15f600e`)** — top-3 podium (gilt ring + crown on #1, muted rings #2/#3, rank badges, ROI, hot-flame streak chips) above the table; table streak cells → flame chips. `kp-rise` + `podium-crown` keyframes (RM-static). Canonical TierBadge already in use. No new i18n. **Regression:** tsc PASS · **ui-regression 158/158** · live-verified 1280 + 390. **→ PART A cross-cutting is now complete (A1–A10)**, except A8-breadth (primitives built, ~12 admin screens + gold purge still to adopt).
- **2026-07-09 · A9 invite share-card + wallet spark (`c592bc7`)** — invite share-card (royal field, FiftyMark, "You've been invited", code in GiltCorner, server-gen QR) + wallet 30-day aqua balance spark above activity (hidden when empty). New keys `youveBeenInvited`+`days30` → parity **1207³**. **Regression:** tsc PASS · test:i18n (1207³) · **ui-regression 158/158 (fresh store)** · live-verified share-card (code+QR) + spark (real deposits).
- **2026-07-08 · A8 admin primitives (`4753241`, in-flight)** — built AdminMeter + AdminBarList + AdminSpark in admin-charts.tsx; wired AdminSpark into AdminKpi's `series` slot; shared admin gold-discipline fixes (live pulse→aqua, delta/funnel→brand); adopted on players/cohorts (distributions→AdminBarList, health meters, spark) + fixed a dev-only `db.user.list().catch` crash. **Regression:** tsc PASS · **ui-regression 158/158 (fresh store — 36 transient fails on a seeded store were `navigator.vibrate` haptics warnings from notifications, not code)** · admin-grids-smoke 125/125. Remaining ~12 screens + admin gold purge = follow-up.
- **2026-07-08 · A7 category-art layer (`ccd1db6`)** — promoted the shared `categoryGlyph()` sigils onto 3 surfaces: 14px glyph in TOPIC filter chips (/markets + /results), 96px 7% watermark behind the /markets/[id] question, and a new home "Browse by topic" row (8 tiles). Royal-tinted, never gold. New key `browseByTopic` → parity **1205³**. **Regression:** tsc PASS · test:i18n (1205³) · **ui-regression 158/158** · live-verified all three surfaces (desktop + mobile).
- **2026-07-08 · A6 payment-tile system (`1c2803f`)** — new shared `payment-logo.tsx` (48 slot; Card/Bank in-house glyphs, MNO hued-initials placeholders pending Ali's ⊘ official SVGs); `ProviderRadioGrid` reworked into the tile system with royal selection ring + check pip + disabled state; Methods tab + deposit trust-strip slot aligned. New key `temporarilyUnavailable` → parity **1204³**. **Regression:** tsc PASS · test:i18n (1204³) · **ui-regression 158/158** · live-verified deposit (desktop+mobile), withdraw, Methods, disabled state.
- **2026-07-08 · A5 reward-burst (`1a8561e`)** — new shared `reward-burst.tsx` crest ported from the kit A5 living standard (12 gilt rays + corner-bracketed medallion + gold-mono amount + Sora caption); wired into /proposals/[id] approval-bonus, /profile/kyc APPROVED, and an `OperationResultModal` celebrate opt (for B7/payout). Fixed a ray-centering bug (negative-margin centering vs. animation-transform clobber) caught in screenshot review. **Card-consistency spot-check (Ali flagged):** /markets board seeded (30 live), audited 360/768/1280/1920 — uniform 356px card heights, columns reflow 1/2/3/3, zero h-overflow, zero console errors. **Regression:** tsc PASS · test:i18n (1203³) · test:trilingual (36) · **ui-regression 158/158**. Deferred: win-celebration pairing (⊘ win-seal.png bitmap).
- **2026-07-08 · Foundations (F0–F3)** — merged glyph kit (49 glyphs + 5 empty-states, G64 wrapper, removed interim controlled-poll block, percent/activity redraws now win); wired `state-tokens.css` + `micro-patterns.css`; added reusable `scripts/ui-regression.mjs`.
  **Regression:** `tsc` PASS · test:i18n (en=sw=zh=1202) · test:trilingual (36) · test:markets (18) · test:ledger (69) · test:audit (15) · test:emergency (30) · test:bonus (59) · test:proposals (44) · test:admin-roles (33) · admin-grids-smoke (125) · markets-retest (18) · switcher-test (5/5, fresh store) · **ui-regression 158/158** across 360/768/1280/1920, 52 screenshots read (markets, wallet, admin verified — no breaks). ALL GREEN.
