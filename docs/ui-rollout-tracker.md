# UI Rollout Tracker — Final UI Enhancement Kit

Single source of rollout truth for porting Claude Design's Final UI enhancement Kit
(`Final UI enhancement Kit/50pick-design-final/`) into the live app. Work strictly
against this, one item at a time. Update the row **after every item**. If context
resets, this file says exactly where to resume.

> **Mindset (Ali, every item):** hold FIVE lenses at once — **integration engineer**
> (end-to-end connection, i18n/money-path, reuse primitives), **UI/UX engineer**
> (pixel polish, 360→1920 responsiveness, a11y, motion + reduced-motion; READ the
> shots), **software architect** (single source of truth, no divergence, delete the
> superseded), **manager** (scope/risk, ship-vs-defer, flag to Ali), and **player**
> (clear, trustworthy, fast, RG-safe, correct in EN/SW/ZH). Resolve or record every
> concern a lens raises — never ship past it silently. Full note in `docs/ui-rollout-next-session.md`.

Status legend: `[ ]` not started · `~` in flight · `[x]` done · `⊘` blocked/sourced-externally

Regression gate: a batch is not "done" until the full suite (tsc + money/i18n tests +
smoke scripts + `scripts/ui-regression.mjs` at 360/768/1280/1920) passes and is logged.

---

## Foundations (step 1)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| F0 · Merge `glyphs-additions.tsx` into `glyphs.tsx` (spread `...Iplus`; percent/activity redraws win; remove interim controlled-poll block) | [x] | 279d0a4 | tsc PASS; screens render | +G64 (2.2) wrapper; Ibase+Iplus spread; removed 12 interim controlled-poll glyphs. `bank` added (payment tile); existing `landmark` kept |
| F1 · Wire `state-tokens.css` + `micro-patterns.css` after `globals.css` | [x] | 279d0a4 | render OK all 4 widths | copied to `src/app/`, imported in `layout.tsx`; classes available, applied by later A/B items |
| F2 · `scripts/ui-regression.mjs` (Playwright 360/768/1280/1920; no h-overflow, zero console errors, key interactions, screenshot+read) | [x] | 279d0a4 | 158/158 pass, 52 shots | domcontentloaded (board polls → never networkidle); ONLY/WIDTHS env filters |
| F3 · Foundations regression green (tsc + money/i18n + smoke + ui-regression) | [x] | 279d0a4 | see batch log | all green; switcher-test needs fresh store (documented state-leak) |

---

## PART A — Cross-cutting (do first, in order)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| A1 · MarketCard v2 — spark + trader crests, `@` labels, demote MoveChip | [x] | 8cfa885 | /live full grid + /markets EN 1280 + SW 360 (NDIO/HAPANA fit); ui-reg 158/158 | spark = real YES% history, smooth cubic, aqua, draws in (pathLength=1); crest = 3 av-stack + count (falls back to meta count when no traders); YES/NO localized (NDIO/是); move → mono micro-text above bar. All 4 pages (/ /markets /live /results) already plumbed the data. Added `seed-markets` dev endpoint |
| A2 · `AuthShell` + brand side-rail (6 `/auth/*` routes) | [x] | 91831f7 | a2-login lg (rail) + 390 (rail hidden) + SW (tagline+NDIO/HAPANA); ui-reg 158/158 | new `auth-shell.tsx`; adopted by login/register/otp/forgot/reset/verify (admin auth untouched). Rail: royal `--bg-overlay`, BrandTopo 0.09, localized tagline (`railTagline` key ×3), YES 64% TippingBar, trust strip. No gold in rail. ⚠ pre-existing gold `SubmitButton` CTA on auth → flag for B4 gold-discipline |
| A3 · Shared `RouteError` (root + 6 boundaries) | [x] | b679181 · **404 frame be4cc89** | a3-routeerror EN 1280 + SW 390 (triggered real boundary); 404 @360+1280 (BrandTopo+FiftyMark64+royal search-badge, de-golded); ui-reg 158/158 | new `route-error.tsx`; all 8 boundaries now thin wrappers. FiftyMark 64 + BrandTopo frame, rose alert tint only (no claret), digest ref, Try again + back link(s). global-error.tsx left as-is. **Follow-up DONE (be4cc89):** `/not-found` now uses the same kit frame (BrandTopo 0.09 + FiftyMark 64 + royal/info `search` badge — 404≠error so royal not rose) + de-golded (eyebrow/hovers/link gold→neutral/brand) |
| A4 · `PageHero` on 5 bare routes | [x] | c6f94ad | a4-proposals/fairness/proposals-new 1280; ui-reg 158/158 | Added masthead to the 3 genuinely-bare routes: /proposals (gold+trophy), /proposals/new (gold+trophy), /fairness (info+shieldcheck). **Deferred (documented):** /proposals/[id] — detail page, a "Propose & earn" masthead is contextually wrong; reward framing comes via A5 burst. /profile/invite — already has a gold EarningsRing hero (adding PageHero = redundant/over-gold); reworked in A9 |
| A5 · Shared reward-burst (`reward-burst.tsx`) | [x] | 1a8561e | a5 test-route 1280+390 (4 configs: trophy+amount, shieldcheck, static end-frame, size-64 crest — rays centred, corner brackets, gold-mono amount); ui-reg 158/158 | new `components/brand/reward-burst.tsx` (presentational, server+client). 12 gilt rays via kit `reward-burst__rays`/`__medal` classes; **rays centre with negative margins, NOT translate** (the scale() entrance clobbers a centering translate — was the one bug found + fixed in review). Wired: /proposals/[id] approval-bonus (trophy+amount end-frame), /profile/kyc APPROVED (shieldcheck "ID verified" — split the combined submitted card so PENDING keeps its clock), OperationResultModal `celebrate`+`celebrateGlyph` opts (earned-money success only → ready for B7/payout). No new i18n keys (reused common.approved/earnedAPrize/creditedToBonusWallet, profile.idVerified/kycApprovedBody) → parity 1203³. **Deferred:** win-celebration.tsx pairing waits on `win-seal.png` (⊘ bitmap). Gold legit on all (earned peak) |
| A6 · MNO/payment tile system | [x] | 1c2803f | a6 deposit 1280+390 (M-Pesa selected: royal ring + check pip; Card glyph), withdraw (Bank glyph), Methods tab, disabled state (HaloPesa 40% + "Temporarily unavailable"); ui-reg 158/158 | new shared `components/wallet/payment-logo.tsx` (48 slot; Card/Bank = in-house cardPay/bank glyphs; 4 MNOs = hued-initials placeholder, `MNO_LOGOS` map = 1-line drop-in when Ali delivers ⊘ official SVGs). `ProviderRadioGrid` → tile system: selected = **royal `--brand-500`** ring + pip (NOT gold — clears a gold-discipline leak on deposit), disabled = 40% + `temporarilyUnavailable` (new i18n ×3 → 1204³), 120ms scale-in / reduced-motion instant, pure CSS `group-has-[:checked]` (stays server component → deposit+withdraw both upgrade). Methods tab adopts same PaymentLogo. Deposit gains a **labeled** regulator-seal trust-strip slot (never fabricated — seal is ⊘ licensed). **Still ⊘ (Ali):** the 4 official MNO marks + regulator seal |
| A7 · Category-art layer (chips/watermark/home row) | [x] | ccd1db6 | a7 home topic-row 1280+390 (8 tiles, royal glyphs), markets topic chips (glyph per chip, All=grid selected royal), /markets/[id] crypto watermark 7%; ui-reg 158/158 | reused shared `categoryGlyph()`. (1) 14px glyph in TOPIC chips on /markets + /results (All=`layoutGrid`; active→brand-300; px-3.5→px-3+gap so SW-long fits); WHEN filters untouched. (2) 96px 7% watermark behind /markets/[id] question (header `relative isolate`, `-z-10`). (3) home "Browse by topic" row = 8 tiles into filtered board, royal-tint glyphs (not gold). New i18n `browseByTopic` ×3 → 1205³. Bumped opacity 6→7% for visibility on navy. **Still ⊘:** `public/category/*.webp` section-topper bitmaps (not needed for chip/watermark tier) |
| A8 · Admin primitives (AdminKpi spark, AdminBarList, AdminMeter, AdminFunnelChart) | [x] (primitives + core adoption) · ~ (long-tail gold) | 4753241 · **A8-breadth `<this>`** | a8 cohorts 1280 (meters brand fill, bar lists proportional stacked, no gold) + overview (live pulse→aqua, deltas→brand); **A8-breadth: finance concentration→royal AdminBarList, ai-usage credit→AdminMeter (spend/cap, 80% danger tick), live/overview/admin-shell chrome de-golded** — verified real data 1280/360, 0 overflow/console errs; admin-smoke 125/125, ui-reg 158/158 fresh | **Primitives DONE** in `admin-charts.tsx`: `AdminMeter`/`AdminBarList`/`AdminSpark` + `AdminFunnelChart`; `AdminKpi` `series`→spark; B8 gold half done. **A8-breadth THIS session:** (1) finance Top-10 concentration hand-rolled `bg-gold/70` bar → **royal `AdminBarList`** (kills a gold leak + adopts the primitive); (2) ai-usage Credit-budget → **`AdminMeter`** (spend-vs-cap, danger past the 80% alert threshold) — genuine value-vs-cap fit; (3) **chrome gold purge**: admin-shell ConfidentialBand border/dot gold→neutral+claret-restricted-dot, officer pill dot gold→aqua (every admin page), live polling chip + live-match minute gold→aqua, live pool/net + overview BET-feed + decorative "uthibitisho/asilimia" gold→neutral. **Did NOT force** primitives where a table reads fine (self-exclusions roster left as table). **⚑ REMAINING (dedicated sweep — flag for Ali):** the long-tail per-page admin gold (affiliate/bonuses/invites *earnings* amounts, proposal/ai-polls warning-amber, totp-verify, players/[id]) is a genuine product call — app-wide law says gold=earned-money but the admin-strict spec says no-gold-except-resolved-seal; needs Ali's ruling on whether admin earnings stay gold. |
| A9 · Invite share-card + QR + wallet balance spark | [x] | c592bc7 | a9-invite (royal share-card: FiftyMark, "You've been invited", code DEMOAEMG in GiltCorner, QR) + a9-wallet-spark (aqua "AVAILABLE · 30 days" line + terminal pip above activity, driven via real deposits); ui-reg 158/158 fresh | **Invite:** server-generated QR (`qrcode` lib, royal modules on white) of the referral link + share-card block showing the CODE (never a balance); gold principled (referrer earns). Existing WhatsApp/SMS/copy share kept. **Wallet:** `balance30d()` reconstructs 30-day end-of-day balance from signed txns vs current balance → `BalanceSpark` (aqua 1.5px + terminal pip, hidden <2 pts so empty wallet stays clean); aqua not gold (balance=state). New i18n `youveBeenInvited`+`days30` ×3 → 1207³. **Deferred:** the 1080² Satori PNG for `navigator.share` files + `public/banners/invite.webp` texture (⊘ bitmap) — on-page share-card + link share cover the feature |
| A10 · Leaderboard podium | [x] | 15f600e | a10-leaderboard 1280+390 (podium: #1 asha gilt ring+crown+glow+rank badge raised, #2/#3 muted 48px, hot-flame streak chips; table streak cells → flame chips); ui-reg 158/158 | Top-3 `Podium` above the table (order [#2,#1,#3]); #1 = `--gold-400` ring + `crown` + rank badge, #2/#3 = `--text-muted` rings. `kp-rise` staggered 60ms + `podium-crown` 6° overshoot keyframe (both RM-static). Gold on #1 principled (earned standing). `HotChip` (`I.hot` flame + win count) in podium + table streak cell. **Canonical `TierBadge` was already in use** (KitTierBadge from avatar; sovereign=★ already fixes the S-collision) — no local re-impl to kill. Real players from row 1 already (synthetic only when empty). No new i18n |

---

## PART B — Refine-existing (keep/upgrade)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| B1 · ConvictionDial — thumb grab-pip + focus ring + coach hint; widen NDIO/HAPANA box ~24%; RG detent @10× + deliberate 2nd gesture past 50× | [x] | c2f305d | b1 en-yes 360/768/1280 (coach pill + grip dots + tachymeter), en-focus 1280 (brand-400 knob focus ring), sw-no 360 (YES ndio / NO hapana fit, "BURUTA KUWEKA DAU"), reduced-motion 1280; ui-reg 158/158 fresh | Grab-pip = 3 grip dots on knob + `.dial-focus-ring` (2px `--brand-400`, `:focus-visible` only, replaces the rect ring). Coach = one-time localStorage `50pick-dial-coached`, dismissed on drag/key/focus (nudge RM-static). RG: module-scope detents at 1×/2×/5×/10× (4px window, `haptics.select()` edge-triggered) + 50× gate (`gestureBeyond50Ref`; single drag clamps to 25,000 @ base500, 2nd drag from beyond is exempt) — **verified numerically: fresh single drag→25,000, 2nd→94,100, detent→exactly 5,000**. Keyboard rebuilt: ←→ = one detent on `POS_LADDER` [1,2,5,10,20,50,100,200], PgUp/PgDn ±10×, Home/End = extremes. aria-valuetext → "TZS {stake} on {side/NDIO/是}" (new `onSide` key). Reduced-motion: `useRollingNumber` jumps (motionReduced). Poles localized+widened (free-mode header; dial runs **locked-only** live via SidePicker so poles verified via locked pills). New i18n `dragToSetStake`+`onSide` → **1209³** |
| B2 · MarketCard MoveChip → mono micro-text (folds into A1) | [x] | 8cfa885 | /live grid shows mono move-line | done with A1: removed the chip + old `.mcard-move*` CSS; now right-aligned mono `↗ +Npt` above the bar |
| B3 · Empty states — redraw `emptyMarkets` (scales+YES/NO pips), widen box 360px, drop 52px ring; add 5 new | [x] | 0f7daf2 | b3 preview (all 12 kinds render), proposals empty 360/1280 (ballot+quill), admin/invites empty (clipboard+lens); ui-reg 158/158 fresh | Box/ring were **already** 360px + no-ring-badge (earlier pass) — this was the ART. Redrew `markets` scales to weigh a YES/NO pip pair (filled YES / hollow NO, colour-free). Added 5 kinds in the **existing 56-grid one-gold-accent idiom** (NOT the plain 64-grid glyph-kit `emptyProposals/kycRail/fairnessChain/rgSelfCare/adminGeneric` — those are **unused** kit specimens; component art is the single source): proposals (ballot+quill), kyc (ID+progress rail), fairness (commit→reveal→attest chain, gilt attest node), rg (calm sunrise, no gambling imagery), admin (clipboard+lens). Wired /proposals both empties markets→proposals; admin default empties (affiliate/ai-usage/bonuses/invites/invites[id])→admin. kyc/fairness/rg kinds now **available for PART C** (C1b/C1d/C2h). No new i18n (1209³) |
| B4 · Buttons/chips — systematize states (Part D recipe); per-size padding 12/16/20/24, `--r-md`/`--r-lg`; 0.7s spinner; drop leading glyph on SW gold CTA | [x] | 227d56d | b4 login EN+SW 360 (royal "Ingia", fits no-wrap), deposit (computed btn-gold), kyc (btn-primary); ui-reg 158/158 fresh | **Recipe was already in place** (globals.css `.btn`: per-size padding 12/16/20/24, `--r-md`/xl `--r-lg`, `white-space:nowrap`, hover/focus-ring/active-scale/disabled-45%, 0.7s spinner in Button+Spinner; Chip has full status palette + brand-400 selected ring). Net-new = **gold-discipline** (§1 gold = money-in/earned only): flipped `SubmitButton` default gold→**primary** (root cause — auth×6/kyc×2/rg/sof/withdraw all relied on the gold default → now royal), deposit opts back into `variant="gold"` (only money-in submit). SW rule satisfied by `nowrap` + no 360 overflow (no gold CTA has a leading glyph that overflows in SW). **Flag to Ali:** global header "Sign up / Jisajili" is still gold (site-wide acquisition CTA — product call). Admin gold left to A8-breadth. No i18n change (1209³) |
| B5 · PageHero — add `glow=aqua` for `/live`; BrandTopo 0.09 | [x] | 83e5108 | b5 live 360/1280 (topo depth, cards crisp); ui-reg 158/158 fresh | Added 5th PageHero glow `aqua` (hue 195 = LIVE accent). /live has **no PageHero** (deliberate slim header — full aqua hero is C1e), so applied the two safe parts: aqua variant is ready for C1e, and /live `BrandTopo 0.04→0.09` (verified subtle behind the dense card wall). No i18n change (1209³) |
| B6 · Wallet cards / profile hero / EarningsRing — KEEP; add balance spark (A9) + real Methods/Limits data | [x] | 2de92d9 | b6 wallet Limits tab 1280 (caps from validators); ui-reg 158/158 fresh | Balance spark done in A9. **Limits were hardcoded string literals** that matched the server but could drift → made money caps a **single source of truth**: `validators.ts` exports `DEPOSIT_MIN/MAX_TZS`+`WITHDRAW_MIN/MAX_TZS` (used in the zod schemas), threaded via `wallet/page.tsx` `limits` prop → Limits tab derives "TZS 500–2,000,000"/"TZS 1,000–5,000,000"; deposit/withdraw input bounds read the same consts. Methods tab = real supported MNOs (fixed list, not mock; per-MNO kill-switch availability = ADM4). test:wallet(8)+numeric(26) green. No i18n change (1209³) |
| B7 · Modals — OperationResultModal success → reward-burst end-frame; bet-confirm pool-share sentence (EN/SW/ZH) | [x] | 34687b4 | b7 betconfirm EN+SW 420 (side-aware "NDIO ikishinda, unagawana mfuko"), win crest 420 (RewardBurst rays+medallion); ui-reg 158/158 fresh | **reward-burst end-frame:** the earned WIN moment (`win-celebration.tsx`) now uses the shared A5 `RewardBurst` (12 gilt rays + trophy medallion) — unifies with proposal-approved/KYC-verified; completes the A5-deferred win pairing (RewardBurst is pure SVG → the ⊘ win-seal.png was never needed); removed dead wc-glow/wc-pop. Celebrate discipline held: WIN only (deposit/bet-placed/cash-out never celebrate, §12) — no non-earned OperationResultModal was turned into a celebration. **pool-share invariant (§10.2):** bet-confirm payout box now shows the side-aware `poolShareIfWins` prominently ("If {NDIO/HAPANA/是/否} wins, you share the pool"). New key ×3 → **1210³** |
| B8 · Admin KPI/tables/charts — upgrade via A8 | [x] | 3597111 | b8 admin overview + finance 1280 (KPIs neutral, net-flow chart royal); admin-grids-smoke 125/125; ui-reg 158/158 fresh | **Admin gold purge (the A8-breadth gold half):** removed `gold`/`tone="gold"` from ~21 AdminKpi tiles (GGR/NGR/deposits/margin/commission/reserve/registered/outstanding/etc.→ neutral) across overview/finance/live/config/affiliate/bonuses/invites/invites[id]/players[id]/markets[id]/proposals/aml/approvals; semantic danger/success + aqua live-pulse untouched. Net-flow `AdminAreaChart` (finance/live/overview) + component default gold→royal (matches operator-margin); categorical stacked-bar keeps gold as one data-viz hue. **Remaining A8-breadth = opportunistic AdminMeter/AdminBarList adoption on screens whose data fits** (players/cohorts already done). No i18n change (1210³) |
| B9 · Glyphs — redraw 3 (percent/activity/landmark), add 12 controlled-poll (done in F0) | [x] | 279d0a4 (percent/activity/+12) · 0049213 (landmark) | b9-landmark 14/24/48/96px (crisp closed pediment, no roofline blur; consistent w/ `bank`); ui-reg 158/158 | percent+activity redraws + 12 controlled-poll done in F0. **`landmark` now redrawn** (`0049213`) to the kit's canonical geometry (shared w/ `bank`): closed pediment + gap-before-columns fixes the pediment/entablature merge at the 12–14px chip/watermark size. `landmark` kept as its own key (categoryGlyph `infrastructure`); geometry documented as shared with `bank`. No i18n change (**1210³**) |
| B10 · Hero image — REPLACE F1 shot; delete 20 orphan slides + 2 orphan hero components | [x] | 5fc3784 (code) / ⊘ bitmap | tsc clean; zero refs to `components/landing` | **Component deletion already shipped** in `5fc3784` — that commit deleted BOTH orphan hero components (`hero-constellation.tsx` 655L + `hero-slideshow.tsx` 220L incl. its ~20-slide array) and its message says so verbatim. Verified this session: no lingering imports of `components/landing`/`hero-slideshow`/`hero-constellation`, tsc PASS, `components/landing` dir gone. Home hero is now a single inline `hero-bg.webp` in `page.tsx` (single source). **Remaining = ⊘ bitmap only**: current `public/hero/hero-bg.webp` is still the F1 champagne shot (audit flag: off-subject for TZ); replacement webp sourced externally by Ali (assets-brief 4.1) — no in-code work left |
| B11 · BrandTopo — 0.05 → 0.09 everywhere | [x] | 0b0ddc6 | b11 auth rail 360/1280 (topo depth on rail + form, YES 64% bar crisp, royal CTA, no contrast loss); ui-reg 158/158 fresh | Full BrandTopo inventory swept to 0.09: `live`(0.09 B5)·`auth-shell`×2(0.09 A2) already at target; **bumped `route-error` 0.06→0.09 + component default 0.07→0.09** (no caller relied on the default — all pass explicit — so bumping it just makes "everywhere" true + future-safe). PageHero is NOT a BrandTopo surface (FiftyMark watermark + radial glow) → correctly out of scope. No i18n change (**1210³**) |
| B12 · PulseRing / SignalPip / GiltCorner — KEEP as-is | [x] | | | no-op by design |

---

## PART C — Per-page

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| C1a · `/markets/[id]` hero — category watermark (A7) + gilt hairline; states open/closing/waiting(hourglassOff)/resolved | [x] | 8ed7b83 | c1a open/closing/waiting EN+SW 360–1920 (gilt hairline all states; CLOSING SOON·30 MIN pulse pill; CLOSED·AWAITING SETTLEMENT hourglassOff badge; SW pills wrap clean, 0 overflow); ui-reg 158/158 fresh | Gilt hairline = sanctioned exception (§C1a: seal-of-the-real, 1px gold-deep@40%, ~2% surface, frames market not CTA → doesn't breach gold discipline). `heroState` server-computed (force-dynamic + 15s poller keeps <1h window fresh). LIVE chip now shows open/closing only; waiting badge fills the selection-closed-while-LIVE gap (gold text) vs settling (warning text). Resolved unchanged. Removed dead `timeLeftStr`. New `market.closingSoon` ×3 → **1211³** |
| C1b · `/profile/kyc` — progress-rail line-art, ID silhouettes, APPROVED reward-burst, canonical `<Input>` | [x] | 5eda4a7 | c1b nida step (node-0 gilt, no fill), docs step (node-1 gilt + NIDA green + gilt fill + ID/person silhouettes), EN+SW 360/640, 0 overflow; ui-reg 158/158 fresh | Replaced the 3-card grid with a connected **4-node rail** (ID→Selfie→Review→Verified), gilt fill to current node, done=green/live=gilt-ring/future=muted (presentational, no motion). Doc slots now show `idCard`/`user` silhouettes vs generic camera. **Canonical `<Input>` + APPROVED reward-burst already in place** (local `Field`→KitField/Input kills admin-focus; burst from A5) — verified, not re-done. No i18n (reused nida/selfie/review/idVerified) → **1211³**. Money paths untouched |
| C1c · `/profile/sessions` — device cards (glyph, geo, mono time, this-device aqua rule), claret revoke, IP reveal-on-tap | [x] | 2a15281 | c1c desktop (Chrome on Windows) + Pixel-5/Android (Chrome on Android) EN+SW, IP reveal→::1, 0 overflow; ui-reg 158/158 fresh | Replaced the sessionId/IP/issued/expires/role/KYC **debug grid** with one device card: smartphone glyph + honest **browser-on-OS** (UA-parsed) + relative mono time + **2px aqua-400 left rule** + "This device" chip. Sign out = btn-ghost **claret** (no-300, inline colour beats btn-ghost). New **IpReveal** client comp (mask first octet → reveal-on-tap). **Single-active-session model** (session-registry) → only ONE real session, shown truthfully (no fake multi-device list); **no geo/city fabricated** (we don't geolocate the IP — never-fake discipline). New `profile.revealIp/hideIp` ×3 → **1213³** |
| C1d · `/fairness` — PageHero glow=info + 5-step provably-fair SVG chain (gilt on attest node) | [x] | f511647 | c1d EN 1280 (full chain, gilt only on Stage-2 attest) + SW/ZH 360 (labels scroll in-panel, page overflow 0); ui-reg 158/158 fresh | Added `FairnessChain` — horizontal 5-node glyph-idiom chain (flag/coins/user/shieldcheck/wallet) above the detail list; **gilt ONLY on the attest node**, royal on the rest; labels in HTML; static (RM-safe); `overflow-x-auto` so long trilingual labels scroll in-panel (no page h-overflow). PageHero glow=info already A4. **Aligned to the page's existing compliance-reviewed 5-step model** (Created/Stake/Stage1/Stage2/Settlement) not the spec's literal names — chain+list stay one story, no regulatory-copy rewrite. B3 fairness EmptyState (3-node commit→reveal→attest) is a different empty-state illustration, not reused. No new i18n → **1213³** |
| C1e · `/live` — PageHero glow=aqua; hero = most-contested (`abs(yesPct-50)`); dense TippingBar-wall card | [x] | bb6e164 | c1e aqua hero + dense wall EN 768/1280/1920 + SW 360 (NDIO/HAPANA @ prices, uniform cards, 0 overflow); ui-reg 158/158 fresh | Slim header → **PageHero glow=aqua** featuring the most-contested market (abs(yesPct−50)) + royal Open-market CTA; removed the duplicate bottom section. `PulseCard`: full MarketCard → **dense bar-card** (category+time · title · bar · @ prices; no spark/crest/KPI/buttons). Dropped dead per-market `getCardChart` fetch. Gold→royal on the 2 nav CTAs (Open market/Browse all). Aqua live-pulse (was red). No new i18n → **1213³** |
| C2a · `/` home — category row (A7) + animated stats band (real aggregates, count-up-flash) | [x] | 4f021cb | c2a band EN 1280 + SW 360 (142 MARKETS SETTLED / TZS 49M PAID OUT, count-up, SW labels fit, 0 overflow) via forced test values then reverted; ui-reg 158/158 fresh | A7 category row already on home. Added client **StatsBand** below trust strip: 2 mono counters bound to **real aggregates** (resolved count + sum of confirmed BET_PAYOUT/CASHOUT txns — same as finance reports), count-up on scroll-in (rAF easeOutCubic + IntersectionObserver) w/ gilt `value-flash`; reduced-motion → final values immediately. **Hidden until settledCount>0** (no hollow proof band on fresh deploy). Payout sum is a full-txn scan → flagged to materialise/cache before high-traffic launch. New `home.statsSettled/statsPaidOut` ×3 → **1215³** |
| C2b · `/results` — aggregate YES/NO donut + one featured result card; keep header lean | [x] | 4dbf8e6 | c2b donut+legend in lean header (YES2/NO2) + featured Bitcoin card (gilt RESOLVED·YES seal, 👑 NOTABLE RESULT, resolved bar), EN 1280 + SW 360 (NDIO/HAPANA, MATOKEO MASHUHURI), 0 overflow; ui-reg 158/158 fresh | Added `OutcomeDonut` (SVG, green YES·rose NO·neutral void) in header + "YES n/NO n" legend (count text hides on mobile → lean). `FeaturedResult` = highest-volume settled market above grid w/ resolved gilt-seal chip + crown label + resolved TippingBar, excluded from page-1 grid (no dup; page-1 no-search only). **Removed sidebar Outcome-summary bars** (donut = single source). New `results.notableResult` ×3 → **1216³**. Verified via temp synthetic resolved markets (dev store has none) then reverted |
| C2c · `/positions` — mini countdown-ring + YES/NO exposure bar | [x] | 0c42b35 | c2c verified vs REAL open positions — ring 5d/4d aqua arcs + exposure bar YES·18K/NO·12K (NDIO/HAPANA end-labels) at 360 SW / 768 / 1280, 0 overflow; ui-reg 158/158 fresh | New `CountdownRing` (client, depleting arc placed→deadline, aqua→gold <1h, compact 5d/4h/42m centre, serverNow-seeded so no hydration mismatch) beside the localized close line. YES/NO **exposure bar** above open grid: green/rose split of open stake w/ end-labels (a11y §6b, never colour-only). Reused `positions.atRisk`+`common.yes/no` → **1216³**. Real open positions seeded via a dev-only test endpoint (uncommitted, deleted at cleanup) |
| C2d · `/positions/performance` — streak pip-chain + best-win gilt crest | [x] | 371eb5a | c2d vs REAL data (80% WR, +TZS 40,890, best win TZS 21,385, streak 4): gilt trophy-medallion crest + 4 gold hot-flame pip-chain, 360 SW/768/1280, 0 overflow; ui-reg 158/158 fresh | Turned the existing bestMarket/currentStreak/longestStreak stats into visuals: **best-win gilt crest** (trophy medallion + glow + GiltCorner + gold amount + title) + **streak pip-chain** (`StreakChain`: filled gilt `I.hot` flames to current streak, muted to longest). avg/total stay plain KPIs. Gold legit (earned). No new i18n → **1216³** |
| C2e · `/wallet/withdraw` — KYC-lock line-art, merge 3 notices, route amount/balance through kit controls | [x] | 0f8a25e | c2e withdraw EN 1280 + SW 360 (KYC-lock gate ID+padlock, amount chips 5K-100K, merged notices panel) + deposit no-regression; test:wallet 8·numeric 26; ui-reg 158/158 fresh | Extracted shared **`AmountField`** (Input + quick chips); deposit + withdraw both use it (withdraw was a bare number input). Balance already via `<Cash>`. Merged the 2 notice strips → one iconized panel. Added **KycLock** line-art (padlock over ID silhouette) to the verify gate. `name="amount"` unchanged → money path intact. No new i18n → **1216³** |
| C2f · `/proposals*` — PageHero (A4), proposals EmptyState, propose banner, category tint | [x] | ea37020 | c2f /proposals EN 1280 + SW 360 (SPORTS=football glyph now, was trophy); ui-reg 158/158 fresh | PageHero glow=gold (A4)·EmptyState ballot/quill (B3)·ProposePromo banner (propose.webp bitmap ⊘) already shipped. **Real fix: `CategoryIcon` had a divergent glyph map (sports=trophy) → delegated to app-wide `categoryGlyph()`** so proposals/admin/propose-form/detail all match market/live glyphs. Category chip stays **neutral** (per-category colour tint would break the neutral-category standard). No new i18n → **1216³** |
| C2g · `/profile`, `/account`, `/source-of-funds` — GiltCorner only if reward framing; per-source glyphs; signature line-art; warning-topo on close-account | [x] | 3f469b9 | c2g SoF (per-source glyphs + ROYAL selection + signature declaration) + account (close-account warning-topo) + profile (verify/SoF icons + step-active + role badge gold→royal; Invite keeps gold), EN 1280 + SW 360; ui-reg 158/158 fresh | SoF glyphs = purpose-built `sof*` set (salary/business/savings/investment/gift + fileText other). SoF selection gold→brand (compliance ≠ earnings). `fileSignature` on declaration. `BrandTopo 0.08` over the claret close-account panel. Profile: only Invite&Earn (earnings) stays gold. No new i18n → **1216³** |
| C2h · `/profile/responsible-gambling` — RG self-care line-art, yes-toned helpline callout | [x] | dc09a44 | c2h RG EN 1280 + SW 360 (green sunrise self-care art + "Support is available/Msaada unapatikana" yes callout; FeedbackSettings toggles de-golded); ui-reg 158/158 fresh | Extracted B3 rg sunrise → shared **`RgSunriseArt`** (yes-toned core, no gold, no gambling imagery). Buried helpline → prominent **yes-toned support callout** after the hero. Bonus: de-golded FeedbackSettings (haptic/motion toggles + sparkle → brand). New `rg.supportAvailable` ×3 → **1217³** |
| C2i · `/help` — per-FAQ topic glyphs, tone-coded quick-link chips | [x] | bd43351 | c2i help EN 1280 + SW 360 (8 FAQ topic glyphs, tone-coded contact + quick-link cards, gold→info/aqua); ui-reg 158/158 | Per-FAQ glyph (percent/activity/withdraw/idCard/pause/cashout/alert/shield). De-golded: PageHero+eyebrow gold→info, chat contact gold→aqua, quick-links gold→tone-coded (RG=yes/wallet=info/positions=aqua), positions trophy→portfolio. No new i18n → **1217³** |
| C2j · `/offline` route — FiftyMark + retry + precache | [x] | 1fbfcb5 | c2j /offline EN 1280 + SW 360 (FiftyMark + wifiOff + "Hauko mtandaoni" + "Jaribu tena" retry, BrandTopo); ui-reg 158/158 | New `/offline` client route (localises from cookie even from cache). sw.js OFFLINE_URL "/"→"/offline" + precached + CACHE v1→v2. No new i18n (offline/offlineHint/error.tryAgain) → **1217³** |
| C2k · OG — extend dynamic OG to leaderboard/results/proposals/profile; gradient align; render png masters | [x] | 3a7a593 | New `/api/og/page?title=&sub=` generic card (navy #0A0E33→#060A50 + mark-a + wordmark + gilt hairline + "wisdom of YES & NO"); leaderboard/results/proposals `generateMetadata`→openGraph+twitter images; market OG gradient aligned green/red→navy. Rendered generic (Top predictors) + market (Bitcoin) OG → both 200 image/png, verified. ui-reg 158/158 · i18n 1217³ | Profile skipped — redirects to login, crawler never gets its OG. **⊘ font**: dynamic OG text uses Satori default sans (brand fonts not loaded into ImageResponse); mark/gradient/colours/tagline brand-exact. Static og-1200x630 + twitter masters already mark-a from logo work |
| C2l · `manifest.json` — shortcuts (Markets/Wallet/Deposit) + narrow screenshot label | [x] | b7c7f39 | manifest 200; 3 shortcut icons 96px (Markets/Wallet white, Deposit gold=money-in) traced 1:1 from glyphs.tsx on navy tile; narrow screenshot = real 390x844 portrait /markets capture (was a mislabeled 1200x600 landscape); OG stays wide. tsc·i18n 1217³·wallet 8·ui-reg 158/158 | Static EN manifest kept (PWA has no per-locale mechanism; name/desc already EN) — flagged for future dynamic manifest if trilingual install copy wanted. New assets: `public/icons/shortcut-{markets,wallet,deposit}.png` + `public/screenshots/markets-narrow.png` |
| C2m · `legal/*` — LegalHeader GiltCorner + per-doc glyph | [x] | 18d6a55 | c2m terms/privacy/RG/aml 360+1280 EN + terms SW + privacy ZH: framed "regulator letter" header w/ GiltCorner seal (TL+BR) + per-doc sigil chip (terms=scrollText/privacy=lock/RG=shield/aml=shieldcheck); aside de-golded; 0 overflow, 0 console errs, CJK/SW titles fit; ui-reg 158/158 · i18n 1217³ | GiltCorner = kit-sanctioned single gilt note ("frames regulator letters" per its doc; cf. C1a seal exception) — glyph+eyebrow NEUTRAL (gold reserved). Also fixed the consistency-sweep-flagged **legal gold-eyebrow leak** in `legal/layout.tsx` aside (gold-300 → text-subtle, dropped gilt radial). **PART C COMPLETE.** No new i18n |

---

## Consistency enhancements — Ali-requested (do after PART C per-page pass)

> The UI kit is now enhanced/consistent across the app (gold discipline, unified
> category glyph, kit primitives). These surfaces must be brought to the SAME
> standard so *everything* is consistent — Ali flagged them explicitly.

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| EMAIL1 · **Full transactional-email redesign** — every email sent (`src/lib/server/email.ts` + templates): header/logo, card layout, colours, buttons, footer — rebuilt to match the enhanced UI kit. Logo already = mark-a (`mark-color-512.png`, fe81145). Verify EVERY email type (welcome/OTP/deposit/withdraw/KYC/win/proposal/etc.) renders consistently; gold only on earned-money moments; trilingual. | [x] | 59686e5 | Rendered deposit/win/invite/bonus/kyc-approved (gold) + welcome/loss/withdrawal/betPlaced/login/kyc-rejected (royal) @640 + mobile 375 (rows stack, CTA full-width, 0 overflow). Gold-discipline: loss email now fully royal; gold CTA only on money actions (Claim bonus/Withdraw). tsc·i18n 1217³·email-stress 12·markets 18·kyc 12·referral 15·ui-reg 158/158 | **Applied the app-wide gold law to emails** (was gilt-uniform on all 43): `wrap({accent})` top-bar+footer-rule, `eyebrow(…,gold)`, `ctaButton(…,variant)` all default royal/neutral; **9 earned emails opt into gold** (deposit/win/invite/kyc-approved/referral×2/proposal-approved-bonus/bonus×2). Logo already mark-a. **Note:** emails are EN/SW bilingual (matches SMS); ZH-in-email = separate content task. Preview-route trick (deleted after) |
| AI1 · **AI agent / chat assistant consistency** — the in-app chat/AI help surface (`src/components/chat/*` — ChatPanel, HelpMark, message Bubbles, EmptyState, EscalateHandoff, RgRedirectCard): bring the panel, bubbles, glyphs, tones and gold-discipline in line with the enhanced kit. | [x] | 10b6ce1 | Verified FAB + open panel + real AI answer (pearl citations) @360+1280, 0 console errs. **De-golded:** HelpMark FAB gilt→pearl (was a persistent gold bubble on every screen); player-bubble edge + citation/source underlines gilt→pearl (`--pearl-edge`); gilt fully retired (escalate already claret). **Brand tone:** Online dot + FAB ring + typing/live signals emerald-152→aqua-195 (152 = YES betting cue the chat forbids). **RM:** added the missing status-dot pulse guard. Mark = mark-a throughout. tsc·i18n 1217³·ui-reg 158/158 | Chat token system already restrained gilt to 2 spots + had pearl as its emphasis axis; completed the pearl migration so the surface is fully gold-free |

---

## Admin reporting surface (Batch 3 spec)

| Item | Status | Commit | Live evidence | Notes |
|---|---|---|---|---|
| ADM1 · Reporting console — GGR/NGR defs, daily P&L w/ totals, category breakdown, regulator pack maker-checker chain | [x] (console) · ⊘ (maker-checker) | 76efba1 | /admin/reports console verified with REAL data (resolve-seed): GGR 64,526 = 282,000 stakes − 217,474 payouts, Hold 22.9%, 6-KPI strip + daily P&L totals + GGR-by-category bars; + empty-store (honest zeros + EmptyState). 360/1280, 0 overflow/console errs. tsc·i18n 1217³·wallet 8·ledger 69·ui-reg 158/158 | **DONE:** normative `report-money.ts` (Stakes/Payouts/GGR=S−P/Bonus/Fees/NGR/Hold%, daily P&L EAT, category breakdown via positionId→cat) = single source of truth; console w/ period(Today/7d/30d/MTD)+compare+freshness+printed defs; report library retained. **⊘ DEFERRED — regulator-pack two-officer maker-checker chain:** needs a report-pack signing backend + compliance definition of officer roles → **Ali product/compliance decision** (no fabricated signatures). **✅ GGR RECONCILED (see D1 row below).** |
| D1 · GGR reconciliation — unify finance/live/overview + gbt-monthly to report-money | [x] | _this session_ | REAL resolve-seed data (3 markets, both sides, YES resolved): stakes 120,000 − payouts 109,200 = **GGR 11K shown identically on /admin, /admin/finance, /admin/reports AND /admin/live** (was 120K turnover on the first three, 11K on reports — now ONE number). Finance tax-accrued now 5% of GGR (540), not 5% of turnover. tsc·i18n 1217³·markets 18·ledger 69·wallet 8·officer-conflict 17·cashout 15·ui-reg 158/158 | **⚑ CHANGES DISPLAYED MONEY NUMBERS (flagged for Ali).** Added `report-money.moneyForWindow(start,end)` = single money-summary primitive; `analytics.grossGamingRevenue()` now returns **Stakes − Payouts** (was Stakes/turnover, mislabelled GGR) and `netGamingRevenue()` returns **GGR − Bonus − Fees** (was Stakes − Payouts, i.e. the value that is actually GGR) — both delegate to `moneyForWindow`, so finance/live/overview + the gbt-monthly statutory report all read ONE definition. gbt-monthly note prose corrected to match. No test pinned the old values; money paths untouched. |
| ADM2 · Two-officer Resolution Ceremony | ~ (surface kit-clean) · ⊘ (dedicated ceremony page) | 11fc2a5 | resolver-queue reviewed: already kit-standard + gold-clean (semantic teal/claret/warning/yes/no) + safely implements the two-officer flow (confidence ring, source + AI-Sentinel evidence, stage-1/stage-2 attestation display, verdict controls). tsc·markets 18·officer-conflict 17·i18n 1217³·ui-reg 158/158 | **DONE:** the one gold leak fixed — settlement `OperationResultModal` `stripTone="gold"`→`"brand"` (ADM2 §2: gold burst is the PLAYER's moment, not the officer's). **⊘ DEFERRED — the dedicated `/admin/resolver/[id]` CEREMONY page** (evidence-left/verdict-right, attestation rail, seal & publish w/ **typed-SEAL** hard-confirm, **24h objection-window ring**, VOID-must-toast-player) needs new backend (objection window, void player-toast, evidence paste-storage) + is money-critical → **Ali product/compliance decision** (won't fabricate non-existent behaviors or risk the settlement path). The safe two-officer flow already lives in the resolver-queue |
| ADM3 · KYC/AML workstation (`/admin/kyc/[id]` — NEW route) | [ ] | | | ⊘ Needs a document-viewer + decision backend (tri-state checklist, risk meter, reason codes, maker-checker) — **new compliance backend + Ali decision** |
| ADM4 · Payments ops (`/admin/payments` — NEW route) — MNO kill-switches, reconciliation, retry queue | [ ] | | | ⊘ Needs MNO health telemetry + kill-switch backend + PSP reconciliation + retry-queue backends — **new payments/ops backends + Ali decision** (only-live-data; can't fabricate) |

---

## Assets (PART E) — produce or source

| Asset | Path | Status | Notes |
|---|---|---|---|
| DROP-IN svg/glyphs ×59 | `public`/in-code | [ ] | currentColor; wired per usage |
| DROP-IN empty-states ×5 | in-code | [ ] | via F0 |
| DROP-IN badges ×12 + gilt | `svg/badges*` | [ ] | map tiers I–V to TierBadge; locked = 30% ink + lock |
| DROP-IN pay/card.svg + bank.svg | `public/pay/` | [ ] | delivered |
| DROP-IN comms (email+SMS/WA) | `comms/` | [ ] | server-side tokens |
| Hero bg webp | `public/hero/hero-bg.webp` | ⊘ | bitmap pipeline (Ali) |
| Propose/Bonus/Invite banners | `public/banners/*.webp` | ⊘ | bitmap pipeline |
| Category art ×7 | `public/category/*.webp` | ⊘ | bitmap pipeline |
| Section texture | `public/texture/navy-weave.png` | ⊘ | bitmap pipeline |
| Win seal png | `public/celebrate/win-seal.png` | ⊘ | bitmap pipeline |
| MNO logos ×4 | `public/pay/{mpesa,airtel,halopesa,mixx}.svg` | ⊘ | source official (Ali) |
| OG masters png | `public/og/*.png` | ⊘ | rasterize where brand fonts installed |
| Regulator seal | deposit trust strip | ⊘ | never fabricate; labeled slot until supplied |

---

## Open items to flag to Ali
- ~~Header "Sign up / Jisajili" CTA is gold~~ — **RESOLVED (`3556845`)**: Ali confirmed "no one is above the law" → sign-up CTAs (header + market-detail guest) are now royal. Gold remains money-in/earned only.
- MNO brand logos (M-Pesa, Airtel Money, HaloPesa, Mixx by Yas) — trademarked, source official.
- Bitmap tier (hero, banners, category art, texture, win-seal) — needs image pipeline.
- Regulator seal — licensed asset.
- SMS sender ID (`"KIPINDI"`) and real deploy domain — env-driven.

---

## Batch log
_(append one entry per batch: date · items · regression result)_

- **2026-07-10 · A8-breadth admin-primitive adoption + chrome gold purge (`<this>`)** — (1) **finance** Top-10 player concentration: hand-rolled `bg-gold/70 prog-sweep` bars → **royal `AdminBarList`** (adopts the A8 distribution primitive + kills a gold leak). (2) **ai-usage** Credit budget: added **`AdminMeter`** (cycle spend vs limit, flips to danger past the 80% email-alert threshold) — a real value-vs-cap fit. (3) **Chrome gold purge** (admin = no gold except the resolved seal): admin-shell ConfidentialBand `border-gold`+gold-dot → neutral border + claret "restricted" dot, officer pill dot gold→aqua (every admin page); live-ops polling chip + live-match-minute gold→aqua, live pool/net + overview BET-feed variant + decorative gold text → neutral. **Held the discipline:** did NOT force a primitive onto the self-exclusions roster (a table reads fine). Verified real 2-officer-resolved data at 1280 + 360 (finance bar-list royal, ai-usage meter honest at $0, live aqua polling, 0 h-overflow, 0 console errors). tsc·i18n 1217³·admin-grids-smoke 125/125·**ui-reg 158/158 (fresh)**. **⚑ Flag for Ali:** the long-tail per-page admin gold (affiliate/bonus/invite *earnings*, proposal/ai-polls warning-amber) needs a product ruling (earned-money-gold vs admin-strict-neutral) before a full sweep. **→ next = ADM2 resolution ceremony page.**
- **2026-07-10 · D1 GGR reconciliation (`8c37cd0`)** — ⚑ **CHANGES DISPLAYED MONEY NUMBERS.** The admin overview, /admin/finance, /admin/live and the GBT-monthly statutory report labelled **turnover (stakes only) as "GGR"**, while the /admin/reports console + report prose used the correct **GGR = Stakes − Payouts**. Unified to ONE definition: added `report-money.moneyForWindow(start,end)` as the single money-summary primitive; rewrote `analytics.grossGamingRevenue()` to return **Stakes − Payouts** and `netGamingRevenue()` to return **GGR − Bonus − Fees** (both delegate to `moneyForWindow`). Finance's tax-accrued estimate is now 5% of GGR (correct) instead of 5% of turnover. gbt-monthly notes corrected. **Verified with REAL two-officer-resolved data** (recreated the uncommitted `resolve-seed-markets` endpoint: 3 markets, both sides bet, YES resolved → stakes 120,000 − payouts 109,200 = **GGR 11K shown identically on all four surfaces**, was 120K on three of them). No test pinned the old numbers; money paths untouched. tsc·i18n 1217³·markets 18·ledger 69·wallet 8·officer-conflict 17·cashout 15·**ui-reg 158/158 (fresh)**. **→ next = A8-breadth admin-primitive adoption + admin gold purge.**
- **2026-07-09 · C2j /offline route (`1fbfcb5`)** — new branded `/offline` route: FiftyMark (mark-a) + `wifiOff` badge + "You're offline" + hint + royal "Try again" retry (reload), over a BrandTopo backdrop. Client component (`useT`) so it localises from the `kp-locale` cookie even when served from the SW cache (verified SW: "Hauko mtandaoni / Jaribu tena"). Service worker (`public/sw.js`): `OFFLINE_URL` "/"→"/offline", added `/offline` to PRECACHE, bumped `CACHE_NAME` v1→v2 so clients re-precache. No new i18n → **1217³**. tsc·ui-reg 158/158; EN 1280 + SW 360. **→ next unchecked = C2k** (OG — extend dynamic OG to leaderboard/results/proposals/profile + gradient align; note C2k is flagged ⊘-font for the static PNG masters, but the dynamic OG route already runs with the app fonts, so the extension is doable).
- **2026-07-09 · C2i /help (`bd43351`) + queued Ali enhancements (EMAIL1, AI1)** — **C2i:** added a per-FAQ **topic glyph** to each of the 8 help FAQs (percent/activity/withdraw/idCard/pause/cashout/alert/shield, brand-toned, answer indented) and **de-golded** the page (help is support, not earned money): PageHero + eyebrow gold→info, Live-chat contact card gold→aqua (so the 3 contact cards are tone-coded call=yes/email=info/chat=aqua), the 3 quick-link cards gold→tone-coded (RG=yes/wallet=info/positions=aqua) with the positions icon trophy→portfolio. No new i18n → **1217³**; tsc·ui-reg 158/158; EN 1280 + SW 360. **NEW queued items (Ali-requested, see "Consistency enhancements" section):** **EMAIL1** = full transactional-email redesign for kit consistency (logo already mark-a; redo card/layout/colours/buttons across every email type), **AI1** = the in-app AI/chat assistant surface (`components/chat/*`) brought to the enhanced-kit standard (gold discipline, tones, mark). **→ next unchecked = C2j** (`/offline` route — FiftyMark + retry + precache).
- **2026-07-09 · C2g profile/SoF/account (`3f469b9`) + C2h RG (`dc09a44`)** — **C2g:** Source-of-funds got the purpose-built `sof*` per-source glyphs (salary/business/savings/investment/gift + fileText other) and its source + income-band selection went **gold→royal** (SoF is compliance, not earnings); added `fileSignature` signature line-art to the declaration. Close-account got a **warning-topo** backdrop (`BrandTopo 0.08` over the claret danger panel). Profile de-golded the verification/compliance chrome — Verify-ID + Source-of-funds row icons + the verification-step active state gold→brand, staff role badge gold→info; only **Invite & Earn** (referral earnings) keeps gold. **C2h `/profile/responsible-gambling`:** extracted the B3 rg calm-sunrise into a shared **`RgSunriseArt`** (yes-toned core — not gold; no gambling imagery) and turned the buried helpline into a prominent **yes-toned "Support is available" callout** after the hero; also de-golded FeedbackSettings (haptic/motion toggles + sparkle → brand). New `rg.supportAvailable` ×3 → **1217³**. Both: tsc·i18n·ui-reg 158/158; EN 1280 + SW 360. **→ next unchecked = C2i** (`/help` — per-FAQ topic glyphs, tone-coded quick-link chips; the audit flagged /help gold eyebrow + hover chrome to fix here too).
- **2026-07-09 · C2f proposals category-glyph unification (`ea37020`)** — `/proposals*`: PageHero glow=gold (A4), proposals EmptyState (B3), ProposePromo reward banner (the `propose.webp` 1600×360 bitmap is ⊘ asset-pipeline) were already shipped. **The real C2f fix = consistency:** `CategoryIcon` used its OWN divergent glyph map (sports=trophy, macro=trendingUp) so proposal cards showed a different glyph per category than market/live cards — **delegated it to the app-wide `categoryGlyph()`** so proposals + admin-proposals + propose-form + proposal-detail all render the SAME glyph per category (sports=football now, not trophy). Category chip kept **neutral** (`variant="neutral"`) — a per-category colour tint would contradict the neutral-category standard just established, so the "tint" stays neutral. No new i18n → **1216³**. tsc·ui-reg 158/158 (fresh); drove EN 1280 + SW 360. **→ next unchecked = C2g** (`/profile`, `/account`, `/source-of-funds` — GiltCorner only if reward framing; per-source glyphs; signature line-art; warning-topo on close-account; SoF = royal not gold).
- **2026-07-09 · C2e withdraw + consistency fixes (`0f8a25e`, `1341dcf`; earlier consistency sweep `f80efd0`/`0eaa9e8`)** — **C2e `/wallet/withdraw`:** extracted a shared **`AmountField`** (Input + quick-amount chips) and routed BOTH deposit and withdraw through it (withdraw was a bare `<input type=number>`; deposit `DepositAmount` is now a thin wrapper — behaviour identical). Balance already via `<Cash>`. Merged the two notice strips (Secured-by-KYC + Tax) into ONE iconized panel. Added **`KycLock`** line-art (a padlock over an ID silhouette) to the verify-first gate. `name="amount"` unchanged → withdraw money path intact. tsc·test:wallet 8·test:numeric 26·i18n 1216³·ui-reg 158/158; drove withdraw EN 1280 + SW 360 + deposit no-regression. **Card-consistency fixes (Ali flagged live):** category glyph on MarketCard was gold→boxed→now a **bare neutral 13px glyph + 10px `text-subtle`**, byte-identical to the /live dense card (was gold badge, then a box, then 9px — three rounds to converge). **App-wide gold-discipline sweep** (31 files): 15 nav/browse `btn-gold`→`btn-primary` (RG dismiss→ghost), systemic `hover:border-gold-700`→`brand-400`, form controls (select/date-picker) gold→brand, non-earned chips (comments role, VOIDED, leaderboard DEMO)→neutral, nav-progress + i18n loader gold→brand. Kept legit gold (reward burst, resolved seal, #1/win/gold-tier, best-win crest, +P&L, deposit/bonus/cashback, propose/invite earnings). **→ next unchecked = C2f** (`/proposals*` — PageHero(A4)/proposals EmptyState/propose banner/category tint; some already in place).
- **2026-07-09 · C2d performance crest + streak chain (`371eb5a`)** — `/positions/performance`: the page already computed `bestMarket` / `currentStreak` / `longestStreak` (real aggregation); C2d turns the two plain stats into the spec visuals — a **best-win gilt crest** (a gilt trophy medallion with glow + `GiltCorner` accent, beside the win amount in gold mono + the market title) and a **streak pip-chain** (`StreakChain`: filled gilt `I.hot` flames up to the current streak, muted pips out to the longest — your run vs your best, with current/longest labels). avg-stake + total-staked stay as plain KPI cards. Gold is legitimate here (earned money / earned standing). No new i18n → **1216³**. Verified against REAL settled data (extended the uncommitted `resolve-seed-markets` endpoint to give the demo user 4 wins with opposing bets so payout>stake, 1 loss ordered oldest so the current streak = 4, + 5 open) → 80% win rate, +TZS 40,890 net, best win TZS 21,385, streak 4, at 360 SW / 768 / 1280. **Regression:** tsc PASS · i18n 1216³ · **ui-regression 158/158 (fresh)**. **⚠ ui-reg gotcha re-confirmed:** an orphaned prior `next dev` on :3000 (seeded store) made ui-reg report 57 false fails (all `navigator.vibrate` warnings) — `taskkill //F //IM node.exe` + ONE clean server → 158/158. Always ensure a single fresh server before ui-regression. **→ next unchecked = C2e** (`/wallet/withdraw` — KYC-lock line-art, merge 3 notices, route amount/balance through kit controls — money-path audit).
- **2026-07-09 · C2c positions ring + exposure bar (`0c42b35`)** — `/positions`: (1) a new **`CountdownRing`** (`components/positions/countdown-ring.tsx`, client) on each open card — a depleting arc over the position's placed→deadline window (aqua, → gold when < 1h), compact remaining in the centre (5d/4h/42m), seeded with the server's `now` prop so SSR and first client render match (no hydration mismatch), beside the localized "Selection closes {date}" line. (2) a **YES/NO exposure bar** above the open grid — green/rose split of open stake with **end-labels** ("YES · TZS 18K / AT RISK / NO · TZS 12K"), never colour-only (a11y §6b). Reuses `positions.atRisk` + `common.yes/no` → **1216³**. **Real-data verification** (this session added a dev-only `resolve-seed-markets` endpoint — 404 in prod, UNCOMMITTED, to be deleted at cleanup — that bets on real seed markets + two-officer-resolves them, and seeds 5 open positions for the demo user): drove /positions at **360 SW / 768 / 1280** — ring shows 5d/4d aqua arcs, exposure bar splits 18K/12K with NDIO/HAPANA end-labels, 0 h-overflow, no real console errors (only the documented benign `navigator.vibrate` warning). **Regression:** tsc PASS · i18n 1216³ · **ui-regression 158/158 (fresh)**. **Standing user directives this session:** (a) production shows ONLY live data — dev endpoints are 404 in prod + the new UI hides when aggregates are empty; (b) **responsiveness-test mobile (360) first on every item**. **→ next unchecked = C2d** (`/positions/performance` — streak pip-chain + best-win gilt crest).
- **2026-07-09 · C2b results donut + featured card (`4dbf8e6`)** — `/results`: (1) an aggregate **YES/NO donut** in the header — pure SVG, green YES · rose NO · neutral void arc + a compact "YES n / NO n" legend; the count·volume text hides on mobile so the header **stays lean** (spec: the donut is data, not a masthead). (2) One **"notable result" featured card** above the grid = the highest-volume settled market, carrying the resolved **gilt seal chip** + a crown "Notable result" label + a full resolved TippingBar + volume/predictors; **excluded from the page-1 grid** so it isn't shown twice (page 1, no active search only). (3) **Removed the sidebar "Outcome summary" bars** — the header donut is now the single source of the outcome breakdown (no divergence). New `results.notableResult` ×3 → **1216³**. **Verification note:** the dev store has NO real resolved markets (base seed + `stress-regulator-grade` don't register via `listMarkets({status:"RESOLVED"})`), so verified the donut proportions + featured card (gilt seal, resolved bar) + SW labels by briefly injecting synthetic resolved markets, then reverted. **Regression:** tsc PASS · i18n 1216³ · **ui-regression 158/158 (fresh)** · 0 h-overflow, 0 console errors. **→ next unchecked = C2c** (`/positions` — mini countdown-ring + YES/NO exposure bar).
- **2026-07-09 · C2a home animated stats band (`4f021cb`)** — `/` home: the A7 category row was already present, so C2a = the **animated stats band** below the trust strip. New client **`StatsBand`** (`components/home/stats-band.tsx`): two mono counters bound to **REAL platform aggregates** — markets settled (`listMarkets({status:"RESOLVED"}).length`) and TZS paid out (sum of confirmed `BET_PAYOUT`+`CASHOUT` ledger txns, the same basis the finance reports use; **never fabricated**). Counts up from 0 (rAF easeOutCubic) when the band scrolls into view (IntersectionObserver), with the kit gilt `value-flash` on settle; **reduced-motion** (`motionReduced()`) → final values render immediately. **Hidden until `settledCount>0`** so a brand-new deploy never shows a hollow "0 · TZS 0" band. Payout sum is a full-txn scan → flagged in a comment to materialise/cache before high-traffic launch. New `home.statsSettled/statsPaidOut` ×3 → **1215³**. **Verification note:** `listMarkets({status:"RESOLVED"})` genuinely returned 0 in the dev store (the `stress-regulator-grade` "resolved" markets do NOT register via `listMarkets`/`/results`/`/fairness` — a stress-endpoint quirk, not a bug), which correctly kept the band hidden; verified the component visual + count-up + SW labels by temporarily forcing values (142 / TZS 49M) then reverting to real-aggregates-only. **Regression:** tsc PASS · i18n 1215³ · **ui-regression 158/158 (fresh)** · 0 h-overflow, 0 console errors. **→ next unchecked = C2b** (`/results` — aggregate YES/NO donut + one featured result card; keep header lean).
- **2026-07-09 · C1e /live aqua hero + dense wall (`bb6e164`)** — `/live` identity fix so it stops being "/markets with a different URL": (1) slim header → **PageHero glow=aqua** masthead that features the genuinely **most-contested** market (closest to 50/50 via `abs(yesPct−50)`) with a full TippingBar + royal "Open market" CTA — fixes the old soonest-closing-implied labelling; the duplicate bottom "Most contested" section is folded into the hero. (2) `PulseCard`: full `MarketCard` → a **dense bar-card** (category + time · title · bar · YES/NO @ prices; no spark/trader-crest/KPI/buttons) — a fast scannable wall, uniform height via a 2-line title clamp. (3) Dropped the dead per-market `getCardChart` fetch from the snapshot (the dense card needs no spark/move — waste on a wall of thousands). (4) Gold→royal on the page's 2 nav CTAs (Open market, Browse all — navigation, not money-in). Aqua live-pulse (was red) matches the B5 aqua identity. No new i18n → **1213³**. **Regression:** tsc PASS · i18n 1213³ · **ui-regression 158/158 (fresh)** · drove the seeded wall at 360/768/1280/1920 EN + SW (aqua hero + dense cards, uniform heights, NDIO/HAPANA @ prices localize, 0 h-overflow, 0 console errors). **→ next unchecked = C2a** (`/` home — category row (A7) + animated stats band with real aggregates + count-up-flash).
- **2026-07-09 · C1d fairness 5-step chain (`f511647`)** — `/fairness`: added a horizontal **5-node provably-fair chain** (`FairnessChain`) above the detail list — Created (flag) → Stake (coins) → Stage 1 (user) → **Stage 2 attest (shieldcheck, gilt)** → Settlement (wallet), in the glyph idiom. **Gilt lands ONLY on the attestation seal node** (the sanctioned earned/verified exception); the other four are royal. Step labels live in HTML (not baked into the SVG). Static → reduced-motion safe. Wrapped in `overflow-x-auto` so the long trilingual labels scroll **within the panel** on narrow screens instead of forcing page h-overflow. PageHero glow=info was already in place (A4). **Five-lens call:** aligned the chain to this page's **existing compliance-reviewed 5-step model** (Created/Stake/Stage1/Stage2/Settlement) rather than rewriting the FATF/POCA regulatory copy to the spec's literal step names (opens/lock/attest/objection/payout) — so the chain and the numbered detail list stay ONE coherent story with zero divergence. (The B3 fairness EmptyState is a *different* 3-node commit→reveal→attest crypto illustration meant for empty states — correctly not reused here.) No new i18n — reuses `common.fairness*` → **1213³**. **Regression:** tsc PASS · i18n 1213³ · **ui-regression 158/158 (fresh)** · drove EN 1280 (full chain, gilt only on attest) + SW/ZH 360 (labels scroll in-panel, page overflow 0), 0 console errors. **→ next unchecked = C1e** (`/live` — mount `PageHero glow=aqua` (B5 built) + most-contested hero by `abs(yesPct−50)` + dense TippingBar-wall card; treat as a real per-page call — check the dense card wall at 4 widths).
- **2026-07-09 · C1c sessions device card (`2a15281`)** — `/profile/sessions`: replaced the sessionId/IP/issued/expires/role/KYC **debug grid** with a single **device card** — smartphone glyph, honest **"Chrome on Android"** (browser-on-OS parsed from the UA; no city, because we don't geolocate the IP and fabricating "Dar es Salaam" would be a trust breach — same never-fake discipline as the regulator seal), relative mono time ("Issued 2h ago · Expires <date>"), a **2px aqua-400 left rule** + "This device" chip marking the live session. **Sign out** = `btn-ghost` with **claret** (`no-300`) text — the destructive action (inline colour beats `.btn-ghost`'s own `color`). **IP masked** (first octet) with **reveal-on-tap** (new `IpReveal` client component, eye/eyeOff, no-op when unknown). **Integration reality:** the app enforces **single-active-session per account** (session-registry — a new login invalidates all prior sessions everywhere), so there is only ever ONE real session; the card shows that one truthfully rather than inventing a multi-device list. The single-session model stays explained in the retained "Session security" note. New `profile.revealIp/hideIp` ×3 → **1213³**. **Money paths untouched.** **Regression:** tsc PASS · i18n 1213³ · **ui-regression 158/158 (fresh)** · drove desktop (Chrome on Windows) + Pixel-5/Android (Chrome on Android) in EN + SW, IP reveal toggles `::1`, 0 h-overflow, 0 console errors. **→ next unchecked = C1d** (`/fairness` — PageHero glow=info + 5-step provably-fair SVG chain; consumes the B3 `fairness` EmptyState illustration, don't redraw).
- **2026-07-09 · C1b KYC verification rail + ID silhouettes (`5eda4a7`)** — `/profile/kyc`: replaced the 3-card step grid with a single **connected 4-node progress rail** (ID → Selfie → Review → Verified) — a **gilt fill** runs the connectors up to the current node, completed nodes go green (the page's done colour), the live node carries the gilt ring, future nodes are muted line-art (purely presentational, reflects server-derived `done` flags, no motion). Doc-upload slots now show **per-slot silhouette line-art** — `idCard` for NIDA front/back, `user` for the selfie — instead of a generic camera. The other two C1b asks were **already shipped and verified, not re-done**: canonical `<Input>` on every field (the local `Field` delegates to `KitField`/`Input`, so the `admin-focus` leak is already killed) and the APPROVED **reward-burst** (A5, `shieldcheck`/`idVerified`). No i18n change — rail labels reuse `profile.nida/selfie/review/idVerified` → **1211³**. **Money paths untouched** (rail + slot icons are presentational; KYC gating unchanged). **Regression:** tsc PASS · i18n 1211³ · **ui-regression 158/158 (fresh store)** · drove the NIDA step (node-0 gilt, no fill) + docs step (node-1 gilt, NIDA green + gilt fill, ID/person silhouettes) in EN + SW at 360/640 (rail labels wrap cleanly in SW, 0 h-overflow, 0 console errors). **→ next unchecked = C1c** (`/profile/sessions` — device cards, this-device aqua rule, claret revoke, IP reveal-on-tap; replaces a debug dump).
- **2026-07-09 · C1a market-detail hero states (`8ed7b83`)** — **PART C begins.** `/markets/[id]` hero now expresses its lifecycle: (1) a slim **gilt hairline** above the question (1px gold-deep @40%, edge-faded, ~2% surface) — a **sanctioned gilt exception** per refinement-spec §C1a ("seal of the real": it frames the real market, not a CTA, so it doesn't breach the earned-money gold discipline); (2) the **LIVE chip** now shows only while actually accepting predictions (open/closing); (3) a new **"Closing soon · N min"** pill (`hourglassHalf`) when <1h to the next deadline, **pulsing aqua→gold** (base + reduced-motion rest on the gold urgent end-state, meaning preserved); (4) a new **waiting badge** (`hourglassOff`) — gold "Selection closed — waiting for results" while selection-closed-but-live, warning "Closed · Awaiting settlement" once the resolution clock elapses — **fills the prior gap** where a selection-closed LIVE market showed a misleading plain LIVE chip. Resolved state unchanged (ResolutionPanel + resolved chip already shipped). `heroState` is **server-computed** (page is force-dynamic + 15s RefreshPoller keeps the <1h window + minute readout fresh). Removed dead `timeLeftStr` helper. **Money paths untouched** — header is presentational; the bet-widget gating is unchanged. New CSS `.gilt-hairline` + `.closing-pill`/`@keyframes closing-pulse` in `state-tokens.css` (reduced-motion static). New i18n `market.closingSoon` ×3 → **1211³**. **Regression:** tsc PASS · i18n 1211³ · test:wallet 8 · test:numeric 26 · test:markets 18 · **ui-regression 158/158 (fresh store)** · drove open/closing/waiting in EN+SW at 360–1920 (gilt hairline on every state; pills wrap cleanly in SW; 0 h-overflow, 0 console errors). **→ next unchecked = C1b** (`/profile/kyc` — consumes the B3 `kyc` EmptyState illustration, don't redraw).
- **2026-07-09 · B9 landmark redraw (`0049213`)** — redrew the `landmark` glyph to the kit's canonical geometry (shared with `bank`): the old apex-roof + separate entablature line merged into a blur at the 12–14px it renders at (category chips + market-detail watermark); the redraw uses one closed pediment + a gap before the columns. Completes B9 (percent/activity + 12 controlled-poll were done in F0) → **PART B is now fully complete (B1–B12; B10 code was pre-shipped in `5fc3784`, B10 bitmap ⊘)**. **Regression:** tsc PASS · **ui-regression 158/158 (fresh)** · read landmark at 14/24/48/96px via a temp in-app glyph route (crisp, no roofline merge, consistent with `bank`), then deleted the route. No i18n change (**1210³**). **→ next unchecked = PART C (C1a; or the quick C1e = mount `PageHero glow=aqua` on /live).**
- **2026-07-09 · B10 + B11 (`0b0ddc6`)** — **B10:** verified the two orphan hero components were already deleted in `5fc3784` (`hero-constellation.tsx` 655L + `hero-slideshow.tsx` 220L w/ its ~20-slide array — that commit's own message says "deleted two orphan hero components"); confirmed zero lingering refs to `components/landing`, tsc PASS, dir gone → B10's in-code half is complete, only the ⊘ `hero-bg.webp` replacement (F1 shot, sourced by Ali) remains. **B11:** swept every BrandTopo call site to 0.09 — route-error 0.06→0.09 + component default 0.07→0.09 (live/auth-shell were already 0.09 from B5/A2); PageHero is a FiftyMark-watermark surface, not BrandTopo, so out of scope. **Regression:** tsc PASS · test:i18n (**1210³**) · **ui-regression 158/158 (fresh)** · read auth rail 360/1280 (topo depth on rail+form, YES 64% bar crisp, royal CTA, no contrast loss). No i18n change. **→ next unchecked = B9 landmark redraw (~) then PART C (C1a first, or C1e = mount PageHero glow=aqua on /live).**
- **2026-07-09 · Logo/brand-mark audit + favicon sRGB (`<next>`)** — audited every logo context: header (FiftyMark mobile / FiftyLockup desktop ✓), footer (FiftyMark + "50pick" text ✓), favicon.svg / icon-192 / apple-touch-180 (royal tile) / maskable-512 (full-bleed + safe-zone) all the **current needle mark** ✓, manifest icons all present ✓. One fix: `favicon.svg` used `oklch()` (some browsers don't render it in favicon context) → converted to exact sRGB hex (#009447/#d6464d/#304ccc/#dcb149) for a bulletproof tab icon, zero colour drift.
- **2026-07-09 · B8/A8-breadth admin gold purge (`3597111`)** — removed gold from ~21 admin KPI values (financials/counts → neutral) + switched net-flow charts gold→royal, across ~13 admin screens; the admin-shell AdminFunnel was already neutral. Admin gold-discipline: gold only on the resolved seal. **Regression:** tsc PASS · admin-grids-smoke 125/125 · **ui-regression 158/158 (fresh)** · drove /admin + /admin/finance (KPIs neutral, chart royal), zero console errors. No i18n change (**1210³**). **Also this session: full regression run** (tsc · functional suite 42/42 · ui-reg 158/158) + sign-up CTAs gold→royal (`3556845`). **→ next unchecked = B10 (delete orphan hero components; hero webp is ⊘ bitmap).**
- **2026-07-09 · B7 modals — win reward-burst + pool-share invariant (`34687b4`)** — win-celebration now uses the shared A5 `RewardBurst` crest (12 gilt rays + trophy medallion), unifying the earned-win moment with proposal-approved/KYC-verified and completing the A5-deferred win pairing (pure SVG, no bitmap needed). Bet-confirm sheet now shows the side-aware §10.2 pool-share invariant ("If YES wins, you share the pool / NDIO ikishinda... / 若"是"获胜...") prominently. Celebrate discipline held — WIN only. New key `poolShareIfWins` → parity **1210³**. **Regression:** tsc PASS · test:i18n (1210³) · **ui-regression 158/158 (fresh)** · drove bet-confirm EN+SW + win crest, zero console errors. **→ next unchecked = B8 (admin KPI/tables/charts upgrade via A8) — note this overlaps the A8-breadth carry-over.**
- **2026-07-09 · B6 wallet real Methods/Limits (`2de92d9`)** — money caps are now a single source of truth: `validators.ts` consts (`DEPOSIT/WITHDRAW_MIN/MAX_TZS`) drive both the zod enforcement and the wallet Limits-tab display (threaded via a `limits` prop) + the deposit/withdraw input bounds, so the shown caps can't drift from what the server enforces (the "mock = launch blocker" risk). Methods = real supported MNOs; A9 spark already covered the balance-spark half. **Regression:** tsc PASS · test:wallet(8) · test:numeric(26) · **ui-regression 158/158 (fresh)** · drove /wallet Limits tab, zero console errors. No i18n change (**1209³**). **→ next unchecked = B7 (OperationResultModal success → A5 celebrate on payout/earned only + bet-confirm pool-share sentence).**
- **2026-07-09 · B5 PageHero aqua glow + /live topo (`83e5108`)** — added a 5th PageHero `glow=aqua` (hue 195, LIVE accent) ready for C1e; bumped /live `BrandTopo` 0.04→0.09 (subtle depth, cards stay crisp). /live keeps its slim header (full aqua PageHero = C1e). **Regression:** tsc PASS · **ui-regression 158/158 (fresh)** · /live 360+1280 read, zero console errors. No i18n change (**1209³**). **→ next unchecked = B6 (wallet real Methods/Limits data — mock = launch blocker).**
- **2026-07-09 · B4 buttons/chips gold-discipline (`227d56d`)** — the button/chip state recipe (per-size padding 12/16/20/24, `--r-md`/`--r-lg`, 0.7s spinner, hover/focus-ring/active/disabled/loading, nowrap; Chip status palette + brand-400 selected ring) was already implemented, so B4 = the flagged gold leak: `SubmitButton` default gold→primary (fixes auth/kyc/rg/sof/withdraw at the root), deposit re-opts into gold (sole money-in submit). SW no-wrap satisfied. **Flag to Ali:** header "Sign up/Jisajili" still gold (site-wide growth CTA). **Regression:** tsc PASS · **ui-regression 158/158 (fresh)** · live-drove login EN+SW 360 (royal, fits), deposit (btn-gold), kyc (btn-primary), zero console errors. No i18n change (**1209³**). **→ next unchecked = B5 (PageHero glow=aqua for /live + BrandTopo 0.09).**
- **2026-07-09 · B3 empty states (`0f7daf2`)** — redrew the `markets` empty illustration (scales weighing a colour-free YES/NO pip pair) and added 5 kinds to the `EmptyState` system in its existing 56-grid one-gold-accent idiom: proposals (ballot+quill), kyc (ID+progress rail), fairness (attest chain), rg (sunrise), admin (clipboard+lens). Wired /proposals→proposals and admin default empties→admin; kyc/fairness/rg reserved for PART C. Box/ring already kit-correct (360px, no badge) from an earlier pass. The 64-grid glyph-kit empty-state entries are unused specimens — component art stays the single source. No new i18n (**1209³**). **Regression:** tsc PASS · test:i18n (1209³) · **ui-regression 158/158 (fresh store)** · live-drove a temp preview of all 12 kinds + real /proposals (360/1280) + /admin/invites empty — render + contextually correct, zero console errors. **→ next unchecked = B4 (button/chip state recipe, incl. the flagged gold SubmitButton CTA leaking onto auth).**
- **2026-07-09 · B1 ConvictionDial refinements (`c2f305d`)** — grab-pip (grip dots + 2px `--brand-400` `:focus-visible` ring on the knob), one-time coach hint (`dragToSetStake`, localStorage-dismissed on first drag/key/focus), RG mitigations for the 200× range (soft magnetic detents 1×/2×/5×/10× with 4px window + edge-triggered `haptics.select()`; single-gesture 50× gate needing a deliberate 2nd drag), keyboard rebuilt to detent-ladder stepping (←→ one detent, PgUp/PgDn ±10×, Home/End extremes) with spec aria-valuetext "TZS X on YES/NDIO/是", localized+widened poles, and reduced-motion needle-jump. New keys `dragToSetStake`+`onSide` → parity **1209³**. Dial runs **locked-only** live (SidePicker gates side first) so all visuals verified in locked mode. **Regression:** tsc PASS · test:i18n (1209³) · test:trilingual (36) · **ui-regression 158/158 (fresh store)** · live-drove /markets/[id] EN+SW 360/768/1280 — **detent snaps to exactly 5,000 (10×), fresh single drag gates to 25,000 (50×) then 2nd drag reaches 94,100**, focus ring + coach + NDIO/HAPANA fit confirmed, zero console errors. **→ PART B underway (B1 done); next unchecked = B3 empty states.**
- **2026-07-09 · A10 leaderboard podium (`15f600e`)** — top-3 podium (gilt ring + crown on #1, muted rings #2/#3, rank badges, ROI, hot-flame streak chips) above the table; table streak cells → flame chips. `kp-rise` + `podium-crown` keyframes (RM-static). Canonical TierBadge already in use. No new i18n. **Regression:** tsc PASS · **ui-regression 158/158** · live-verified 1280 + 390. **→ PART A cross-cutting is now complete (A1–A10)**, except A8-breadth (primitives built, ~12 admin screens + gold purge still to adopt).
- **2026-07-09 · A9 invite share-card + wallet spark (`c592bc7`)** — invite share-card (royal field, FiftyMark, "You've been invited", code in GiltCorner, server-gen QR) + wallet 30-day aqua balance spark above activity (hidden when empty). New keys `youveBeenInvited`+`days30` → parity **1207³**. **Regression:** tsc PASS · test:i18n (1207³) · **ui-regression 158/158 (fresh store)** · live-verified share-card (code+QR) + spark (real deposits).
- **2026-07-08 · A8 admin primitives (`4753241`, in-flight)** — built AdminMeter + AdminBarList + AdminSpark in admin-charts.tsx; wired AdminSpark into AdminKpi's `series` slot; shared admin gold-discipline fixes (live pulse→aqua, delta/funnel→brand); adopted on players/cohorts (distributions→AdminBarList, health meters, spark) + fixed a dev-only `db.user.list().catch` crash. **Regression:** tsc PASS · **ui-regression 158/158 (fresh store — 36 transient fails on a seeded store were `navigator.vibrate` haptics warnings from notifications, not code)** · admin-grids-smoke 125/125. Remaining ~12 screens + admin gold purge = follow-up.
- **2026-07-08 · A7 category-art layer (`ccd1db6`)** — promoted the shared `categoryGlyph()` sigils onto 3 surfaces: 14px glyph in TOPIC filter chips (/markets + /results), 96px 7% watermark behind the /markets/[id] question, and a new home "Browse by topic" row (8 tiles). Royal-tinted, never gold. New key `browseByTopic` → parity **1205³**. **Regression:** tsc PASS · test:i18n (1205³) · **ui-regression 158/158** · live-verified all three surfaces (desktop + mobile).
- **2026-07-08 · A6 payment-tile system (`1c2803f`)** — new shared `payment-logo.tsx` (48 slot; Card/Bank in-house glyphs, MNO hued-initials placeholders pending Ali's ⊘ official SVGs); `ProviderRadioGrid` reworked into the tile system with royal selection ring + check pip + disabled state; Methods tab + deposit trust-strip slot aligned. New key `temporarilyUnavailable` → parity **1204³**. **Regression:** tsc PASS · test:i18n (1204³) · **ui-regression 158/158** · live-verified deposit (desktop+mobile), withdraw, Methods, disabled state.
- **2026-07-08 · A5 reward-burst (`1a8561e`)** — new shared `reward-burst.tsx` crest ported from the kit A5 living standard (12 gilt rays + corner-bracketed medallion + gold-mono amount + Sora caption); wired into /proposals/[id] approval-bonus, /profile/kyc APPROVED, and an `OperationResultModal` celebrate opt (for B7/payout). Fixed a ray-centering bug (negative-margin centering vs. animation-transform clobber) caught in screenshot review. **Card-consistency spot-check (Ali flagged):** /markets board seeded (30 live), audited 360/768/1280/1920 — uniform 356px card heights, columns reflow 1/2/3/3, zero h-overflow, zero console errors. **Regression:** tsc PASS · test:i18n (1203³) · test:trilingual (36) · **ui-regression 158/158**. Deferred: win-celebration pairing (⊘ win-seal.png bitmap).
- **2026-07-08 · Foundations (F0–F3)** — merged glyph kit (49 glyphs + 5 empty-states, G64 wrapper, removed interim controlled-poll block, percent/activity redraws now win); wired `state-tokens.css` + `micro-patterns.css`; added reusable `scripts/ui-regression.mjs`.
  **Regression:** `tsc` PASS · test:i18n (en=sw=zh=1202) · test:trilingual (36) · test:markets (18) · test:ledger (69) · test:audit (15) · test:emergency (30) · test:bonus (59) · test:proposals (44) · test:admin-roles (33) · admin-grids-smoke (125) · markets-retest (18) · switcher-test (5/5, fresh store) · **ui-regression 158/158** across 360/768/1280/1920, 52 screenshots read (markets, wallet, admin verified — no breaks). ALL GREEN.
