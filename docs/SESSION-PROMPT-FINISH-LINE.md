# SESSION PROMPT — THE FINISH LINE · ⚪ SPENT, retired 2026-08-15

> ⛔ **THIS PROMPT IS DONE. DO NOT PASTE IT INTO A SESSION.**
> A live-looking prompt costs a session the time to discover it is finished, which is the
> exact failure this file's own §0 warned about and which is why four prompts were retired
> into it in the first place.

**Commissioned by Ali, 2026-08-15**: *"meet all the remaining issues so we clean out — all at
once, in one session."* Twelve units, A–L. **Eleven closed; one — unit H — is open and named.**

**Where the work is now recorded, in the files that own each subject:**

| What | Where it lives now |
|---|---|
| The RED fleet, measured — 68 harnesses, 27 that had never run, the harness rewriting tracked source | [`FAILURE-INVENTORY.md`](FAILURE-INVENTORY.md) **§8** |
| The refusal seam — the last prose route, the wrong modal heading, the six dead code rows | **§3.10**, **§3.11**, **§3.12** · rule at [`RULES.md`](RULES.md) §2.9 |
| The labelling tail — the landing's null outcome, notification titles | **§7.2c** (both now ✅) |
| UD-20 — measured as already shipped in `209a97da`, not re-done | **§7.2c** |
| ⛔ **What is still open**, including unit H | [`LIVE-QA-CAMPAIGN.md`](LIVE-QA-CAMPAIGN.md) **§6b**, session 47 block — the topmost one |
| 🔴 `tsc` does not typecheck the test suite | **§7.2b-tsc** |

⚠️ **The one item this prompt commissioned and this session did NOT close is `qa:refusal-frames`'
drive half (unit H).** Its reasoning is preserved in the §6b handoff rather than here, because
that is where the next session looks — and because a spent prompt keeping a live instruction is
how two files come to disagree about what is true.

⛔ **The visual-sweep prompt is a SEPARATE, still-live commission** —
[`SESSION-PROMPT-VISUAL-SWEEP.md`](SESSION-PROMPT-VISUAL-SWEEP.md). It was deliberately not
touched by this session, and retiring this file does not retire that one.
